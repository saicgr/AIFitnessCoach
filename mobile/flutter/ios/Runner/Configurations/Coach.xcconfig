// Reppora Coach flavor — iOS build settings.

#include "Generated.xcconfig"

PRODUCT_BUNDLE_IDENTIFIER = com.reppora.coach
DISPLAY_NAME = Reppora for Coach
CURRENT_PROJECT_VERSION = 1
MARKETING_VERSION = 0.1.0
ASSETCATALOG_COMPILER_APPICON_NAME = AppIcon-RepporaCoach
PROVISIONING_PROFILE_SPECIFIER =
DEVELOPMENT_TEAM =
