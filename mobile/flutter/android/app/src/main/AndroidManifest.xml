<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Network access -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Notification permissions -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- Block exact alarm permissions from being merged by dependencies -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" tools:node="remove"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" tools:node="remove"/>

    <!-- Foreground service for persistent workout notification -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>

    <!-- Speech recognition permission -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>

    <!-- Camera permission for food photo logging and barcode scanning -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Storage permissions for saving workout images to gallery -->
    <!-- For Android 9 and below -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <!-- For Android 13+ (scoped storage) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

    <!-- Location permissions (for gym profile auto-switch) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- BLE Heart Rate Monitor permissions — feature disabled, permissions
         commented so the app doesn't request "Nearby Devices" access. Keep
         here for easy re-enable alongside the service/UI. -->
    <!--
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    -->

    <!-- Health Connect permissions -->
    <uses-permission android:name="android.permission.health.READ_WEIGHT"/>
    <uses-permission android:name="android.permission.health.WRITE_WEIGHT"/>
    <uses-permission android:name="android.permission.health.READ_BODY_FAT"/>
    <uses-permission android:name="android.permission.health.WRITE_BODY_FAT"/>
    <uses-permission android:name="android.permission.health.READ_HEART_RATE"/>
    <uses-permission android:name="android.permission.health.READ_RESTING_HEART_RATE"/>
    <uses-permission android:name="android.permission.health.READ_HEART_RATE_VARIABILITY"/>
    <uses-permission android:name="android.permission.health.READ_STEPS"/>
    <uses-permission android:name="android.permission.health.READ_DISTANCE"/>
    <uses-permission android:name="android.permission.health.READ_ACTIVE_CALORIES_BURNED"/>
    <uses-permission android:name="android.permission.health.READ_TOTAL_CALORIES_BURNED"/>
    <uses-permission android:name="android.permission.health.READ_SLEEP"/>
    <uses-permission android:name="android.permission.health.READ_BLOOD_GLUCOSE"/>
    <uses-permission android:name="android.permission.health.READ_EXERCISE"/>
    <uses-permission android:name="android.permission.health.WRITE_EXERCISE"/>
    <uses-permission android:name="android.permission.health.WRITE_ACTIVE_CALORIES_BURNED"/>
    <uses-permission android:name="android.permission.health.READ_FLOORS_CLIMBED"/>
    <!--
        Missing permissions added 2026-04-25 — without these, Health Connect
        silently denies the matching READ types in our requestAuthorization()
        call, which is why workouts arrived as "summary only" with no in-window
        HR samples / SpO2 / BMR / etc, and why the Today's Health card stayed
        empty while competitors like GymBeat showed steps + cal + avg HR.
        The Dart side already requests BASAL_ENERGY_BURNED, BLOOD_OXYGEN,
        BODY_TEMPERATURE, RESPIRATORY_RATE, WATER, and elevation_gain on
        SLEEP_STAGES — manifest now matches.
    -->
    <uses-permission android:name="android.permission.health.READ_BASAL_METABOLIC_RATE"/>
    <uses-permission android:name="android.permission.health.READ_OXYGEN_SATURATION"/>
    <uses-permission android:name="android.permission.health.READ_BODY_TEMPERATURE"/>
    <uses-permission android:name="android.permission.health.READ_RESPIRATORY_RATE"/>
    <uses-permission android:name="android.permission.health.READ_HYDRATION"/>
    <uses-permission android:name="android.permission.health.WRITE_HYDRATION"/>
    <uses-permission android:name="android.permission.health.READ_ELEVATION_GAINED"/>
    <uses-permission android:name="android.permission.health.READ_SPEED"/>
    <uses-permission android:name="android.permission.health.READ_POWER"/>

    <application
        android:label="Zealova"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:networkSecurityConfig="@xml/network_security_config"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules">
        <!-- FCM default notification icon (monochrome, white-on-transparent) -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_launcher_monochrome" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/notification_color" />
        <!-- PostHog Analytics -->
        <meta-data android:name="com.posthog.posthog.API_KEY"
            android:value="phc_4bE9QftoDcFxLm7xb46uZ8QeB0i9lCZ8YXgBpqm0mAJ" />
        <meta-data android:name="com.posthog.posthog.POSTHOG_HOST"
            android:value="https://us.i.posthog.com" />
        <meta-data android:name="com.posthog.posthog.TRACK_APPLICATION_LIFECYCLE_EVENTS"
            android:value="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            android:resizeableActivity="true"
            android:supportsPictureInPicture="true">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Deep link handling for widget actions.
                 fitwiz:// is the LOCKED widget URL scheme (must match widget
                 infra). zealova:// is the rebrand-aligned scheme for new
                 share/email links. Both routes hit the same handler. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="fitwiz"/>
                <data android:scheme="zealova"/>
            </intent-filter>

            <!-- HTTPS invite deep links (https://zealova.com/invite/{code}).
                 autoVerify is INTENTIONALLY OFF — enabling it requires a
                 Digital Asset Links JSON hosted at
                 https://zealova.com/.well-known/assetlinks.json containing
                 the release keystore + Play App Signing SHA-256 fingerprints.
                 Without that file Android silently fails verification on
                 every install. With autoVerify off, Android still routes
                 these links via the disambiguation chooser (user gets the
                 "Open with Zealova" option). To upgrade to autoVerify later:
                   1. keytool -list -v -keystore android/keystores/release.keystore -alias fitwiz
                   2. Grab the upload SHA-256 + the Play App Signing SHA-256
                      from Play Console → Setup → App Signing.
                   3. Host {package: com.aifitnesscoach.app, sha256s: [...]}
                      at https://zealova.com/.well-known/assetlinks.json with
                      content-type application/json (NO redirects).
                   4. Re-add android:autoVerify="true" below.
                 The custom fitwiz:// scheme above keeps working regardless. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https"
                      android:host="zealova.com"
                      android:pathPrefix="/invite"/>
                <data android:scheme="http"
                      android:host="zealova.com"
                      android:pathPrefix="/invite"/>
            </intent-filter>

            <!-- Public single-workout share links (https://zealova.com/w/{token}).
                 Mirrors the iOS associated-domains entitlement. autoVerify is
                 OFF for the same reason as /invite — flip it on once
                 assetlinks.json is published with the production SHA256 cert. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https"
                      android:host="zealova.com"
                      android:pathPrefix="/w"/>
                <data android:scheme="https"
                      android:host="zealova.com"
                      android:pathPrefix="/p"/>
                <data android:scheme="https"
                      android:host="zealova.com"
                      android:pathPrefix="/u"/>
            </intent-filter>

            <!-- Supabase email-verify + magic-link callbacks
                 (https://zealova.com/auth/callback?code=...). Captured by
                 IncomingLinkService → exchangeCodeForSession so the user
                 returns from their email client and lands inside the app
                 instead of stuck on a Safari/Chrome confirm page. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https"
                      android:host="zealova.com"
                      android:pathPrefix="/auth"/>
                <data android:scheme="http"
                      android:host="zealova.com"
                      android:pathPrefix="/auth"/>
            </intent-filter>

            <!-- Long-press app-icon shortcuts (Android 7.1+) — staged
                 alongside the meal-suggestion widget (see Settings →
                 Coming Soon). Uncomment to surface on the launcher.
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
            -->

        </activity>

        <!-- Transparent activity for quick log overlay from widgets -->
        <activity
            android:name=".QuickLogDialogActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:excludeFromRecents="true"
            android:taskAffinity="">
        </activity>

        <!-- Photo Capture Activity -->
        <activity
            android:name=".PhotoCaptureActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen"
            android:screenOrientation="portrait" />

        <!-- FileProvider for sharing workout share images to Instagram Stories.
             Instagram requires a content:// URI with FLAG_GRANT_READ_URI_PERMISSION;
             a plain file:// path triggers FileUriExposedException on Android 7+. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.instagramshare.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/instagram_share_paths" />
        </provider>

        <!-- Barcode Scanner Activity -->
        <activity
            android:name=".BarcodeScannerActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen"
            android:screenOrientation="portrait" />

        <!-- UCrop Activity for image_cropper plugin -->
        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

        <!-- Handles permission request from Health Connect -->
        <activity-alias
            android:name="ViewPermissionUsageActivity"
            android:exported="true"
            android:targetActivity=".MainActivity"
            android:permission="android.permission.START_VIEW_PERMISSION_USAGE">
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE"/>
                <category android:name="android.intent.category.HEALTH_PERMISSIONS"/>
            </intent-filter>
        </activity-alias>
        <!-- Google Maps removed for v1 — re-add when Maps API key is configured
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="YOUR_GOOGLE_MAPS_API_KEY_HERE"/> -->

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Required for scheduled notifications -->
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <!-- Home Screen Widgets -->

        <!-- Today's Workout Widget -->
        <receiver android:name=".widgets.WorkoutWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/workout_widget_info" />
        </receiver>

        <!-- Streak & Motivation Widget -->
        <receiver android:name=".widgets.StreakWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/streak_widget_info" />
        </receiver>

        <!-- Quick Water Log Widget -->
        <receiver android:name=".widgets.WaterWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/water_widget_info" />
        </receiver>

        <!-- Quick Food Log Widget -->
        <receiver android:name=".widgets.FoodWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/food_widget_info" />
        </receiver>

        <!-- Stats Dashboard Widget -->
        <receiver android:name=".widgets.StatsWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/stats_widget_info" />
        </receiver>

        <!-- Quick Social Post Widget -->
        <receiver android:name=".widgets.SocialWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/social_widget_info" />
        </receiver>

        <!-- Active Challenges Widget -->
        <receiver android:name=".widgets.ChallengesWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/challenges_widget_info" />
        </receiver>

        <!-- Achievements Widget -->
        <receiver android:name=".widgets.AchievementsWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/achievements_widget_info" />
        </receiver>

        <!-- Personal Goals Widget -->
        <receiver android:name=".widgets.GoalsWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/goals_widget_info" />
        </receiver>

        <!-- Weekly Calendar Widget -->
        <receiver android:name=".widgets.CalendarWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/calendar_widget_info" />
        </receiver>

        <!-- AI Coach Chat Widget -->
        <receiver android:name=".widgets.AICoachWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/ai_coach_widget_info" />
        </receiver>

        <!-- Meal Suggestion (one-tap "what should I eat?") Widget —
             staged under Settings → Coming Soon. Uncomment to enable.
        <receiver android:name=".widgets.MealSuggestionWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/meal_suggestion_widget_info" />
        </receiver>
        -->


        <!-- Wearable Data Layer Listener Service for Watch Sync -->
        <service
            android:name=".wearable.PhoneDataLayerListenerService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
                <data
                    android:host="*"
                    android:pathPrefix="/fitwiz"
                    android:scheme="wear" />
            </intent-filter>
        </service>

        <!-- Declare capability for watch discovery -->
        <meta-data
            android:name="com.google.android.gms.wearable.capability"
            android:value="fitwiz_phone" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Required for url_launcher to open https/http links on Android 11+ -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
        <!-- Required for url_launcher to open mailto links on Android 11+ -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="mailto" />
        </intent>
        <!-- Required for speech recognition on Android 11+ -->
        <intent>
            <action android:name="android.speech.RecognitionService"/>
        </intent>
        <!-- Health Connect package visibility (required for Android 11+) -->
        <intent>
            <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
        </intent>
        <package android:name="com.google.android.apps.healthdata" />
        <!-- Samsung Health package visibility (for launching Samsung Health app) -->
        <package android:name="com.sec.android.app.shealth" />
        <!-- Required for Instagram sharing -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="instagram-stories" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="instagram" />
        </intent>
        <!-- Instagram Stories ADD_TO_STORY intent (used by native MethodChannel
             handler). Without this, resolveActivity() returns null on Android
             11+ and the share silently no-ops. -->
        <intent>
            <action android:name="com.instagram.share.ADD_TO_STORY" />
            <data android:mimeType="image/*" />
        </intent>
        <package android:name="com.instagram.android" />
        <!-- Required for share_plus to work with all apps -->
        <intent>
            <action android:name="android.intent.action.SEND" />
            <data android:mimeType="image/*" />
        </intent>
    </queries>
</manifest>
