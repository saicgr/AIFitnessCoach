<?xml version="1.0" encoding="utf-8"?>
<!--
  Zealova launch background.

  Layered:
    1. Solid brand-dark color (#0A0A0A via @color/splash_background) —
       replaces the previous bitmap-tile background so the splash never
       flashes white if the bitmap fails to decode on low-memory startups.
    2. Centered Zealova logo (drawable/splash) — generated by
       flutter_launcher_icons from assets/icon/. Keep regenerating from
       there; do NOT hand-edit the splash.png variants.
-->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <color android:color="@color/splash_background"/>
    </item>
    <item>
        <bitmap android:gravity="center" android:src="@drawable/splash"/>
    </item>
</layer-list>
