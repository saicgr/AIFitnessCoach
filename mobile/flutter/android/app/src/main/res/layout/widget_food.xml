<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_background"
    android:padding="20dp"
    android:gravity="center">

    <!-- Calories Display -->
    <TextView
        android:id="@+id/food_calories"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="1,250"
        android:textColor="#FFFFFF"
        android:textSize="48sp"
        android:textStyle="bold"
        android:fontFamily="sans-serif-medium"
        android:letterSpacing="-0.02"
        android:includeFontPadding="false" />

    <TextView
        android:id="@+id/food_calories_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="cal today"
        android:textColor="#94A3B8"
        android:textSize="14sp"
        android:fontFamily="sans-serif"
        android:layout_marginTop="2dp" />

    <!-- Spacer -->
    <View
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <!-- Macros Row (for medium/large widgets) -->
    <LinearLayout
        android:id="@+id/macros_row"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginBottom="16dp"
        android:visibility="gone">

        <TextView
            android:id="@+id/macro_protein"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="P: 85g"
            android:textColor="#22D3EE"
            android:textSize="12sp"
            android:fontFamily="sans-serif-medium"
            android:layout_marginEnd="16dp" />

        <TextView
            android:id="@+id/macro_carbs"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="C: 120g"
            android:textColor="#A78BFA"
            android:textSize="12sp"
            android:fontFamily="sans-serif-medium"
            android:layout_marginEnd="16dp" />

        <TextView
            android:id="@+id/macro_fat"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="F: 45g"
            android:textColor="#FB923C"
            android:textSize="12sp"
            android:fontFamily="sans-serif-medium" />
    </LinearLayout>

    <!-- Log Button -->
    <TextView
        android:id="@+id/food_log_button"
        android:layout_width="wrap_content"
        android:layout_height="44dp"
        android:gravity="center"
        android:text="Log Dinner"
        android:textSize="15sp"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        android:fontFamily="sans-serif-medium"
        android:background="@drawable/widget_button_food"
        android:paddingStart="28dp"
        android:paddingEnd="28dp"
        android:elevation="4dp"
        android:clickable="true"
        android:focusable="true" />

</LinearLayout>
