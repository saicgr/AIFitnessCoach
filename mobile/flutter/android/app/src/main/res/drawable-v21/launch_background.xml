<?xml version="1.0" encoding="utf-8"?>
<!--
  FitWiz launch background — Android 5+ (v21+) variant.
  Mirrors drawable/launch_background.xml. Solid brand-dark color so the
  splash never flashes white if the bitmap tile fails to decode.
-->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <color android:color="@color/splash_background"/>
    </item>
    <item>
        <bitmap android:gravity="center" android:src="@drawable/splash"/>
    </item>
</layer-list>
