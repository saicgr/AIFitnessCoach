# Accent-source gate baseline — per-file counts of hardcoded
# accent-family colours. Grandfathered backlog; the gate fails
# only when a count grows or a new file appears. Regenerate with
#   dart run lib/core/theme/accent_source_gate.dart --refresh-baseline
# Lower these numbers as screens migrate to context.accentColor.
