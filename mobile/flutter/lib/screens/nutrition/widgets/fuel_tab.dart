/// DEPRECATED — the "Fuel" sub-tab was removed in the Surface 3 minimalist
/// redesign (2026-05-26). Water moved to the Daily tab as a slim tile below
/// the calorie card; Nutrients are reachable via the "More nutrients" accordion
/// on the calorie card; Fasting is contextual (active-bar at the top of the
/// calorie card and a Settings entry).
///
/// File kept as an empty stub to avoid breaking any in-flight import the
/// analyzer would otherwise flag. Safe to delete once no references remain.
library;
