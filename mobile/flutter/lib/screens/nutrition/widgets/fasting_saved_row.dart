/// DEPRECATED — the Fasting + Saved split row at the top of the Daily tab
/// was removed in the Surface 3 minimalist redesign (2026-05-26).
///
/// Fasting is now contextual: a slim "Fasting active" bar appears at the top
/// of the calorie card only when a fast is in progress. Idle = hidden.
/// Saved moved into the Nutrition header kebab and is also reachable from the
/// Recipes sub-tab's Favorites quick action.
///
/// File kept as an empty stub to avoid breaking in-flight imports. Safe to
/// delete once no references remain.
library;
