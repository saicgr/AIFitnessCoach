CHROME_EXECUTABLE="/Applications/Microsoft Edge.app/Contents/MacOS/Microsoft Edge" flutter run -d web-server --web-port=8080


# Kill any existing emulator processes
pkill -f "emulator" 2>/dev/null || true
sleep 2

# Start fresh emulator with cold boot (wipe data)
~/Library/Android/sdk/emulator/emulator -avd Medium_Phone_API_36.1 -no-snapshot-load -wipe-data &
sleep 30

# Check devices
~/Library/Android/sdk/platform-tools/adb devices

/opt/homebrew/bin/flutter build apk --debug 2>&1