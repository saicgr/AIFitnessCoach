# Zealova — E2E issue register, 2026-07-28

One table. Every issue found by driving the **shipping iOS build** on an iPhone 17 Pro simulator, signed in as a real account, with each claim checked against **production Postgres** and **Render runtime logs**. `NOT REPRO` = found by code audit, not yet reproduced on device.

**Accounts** — walk: `zealova.qa.07280102@mailinator.com` (`users.id 1aa02a24-…60bdc`) · fresh run: `zealova.qa2.07281342@mailinator.com` (`9335b664-…31518`) · probe: `zealova.e2e.1785216513@mailinator.com` · **post-fix control: `zealova.qa3.1785552907@mailinator.com` (`users.id 29880f4e-ebb7-498c-9f7d-4053b1784e61`) — created 2026-08-01 specifically to test behaviour that only a account onboarded AFTER the fixes can show; keep it.** Password for all: `ZealovaE2E!2026`.
**Infra** — Render `srv-d4o6oker433s73d8pu0g` · Supabase `hpbzfahijszqmgsybuor`.
**Severity** — `CRIT` corrupts data or blocks the core loop · `HIGH` a headline feature silently does nothing, or the app looks broken/untrustworthy · `MED` wrong, misleading or high-friction but recoverable · `LOW` polish.

> **Commit column — repaired 2026-07-30.** A bulk pass had stamped **53 rows** with the single citation `f6b2be44 / f9381447`; those commits are *"fix(injury-safety): equipment finisher — row 84"* and an **i18n chore**, so they were wrong for every row but 84. All 53 have since been traced to their real commits and independently re-verified — the wave-2 commits each carry their own `E2E rows N, M…` list, so the mapping is documented rather than inferred. **51 verified fixed; 2 were labelled FIXED but are NOT: #15 and #41** (see those rows). Real commits: `35b5f6e7` onboarding · `e10473d2` active-session · `4d873f40` nutrition · `b906c523` coach-chat · `f9e49e2c` home/auth · `109465ad` coach-card actions · `af3f28c1` day-ownership · `afce2a7c` programs · `35d7c83f` performance · `259aab75` data · `adf69b70` theme · `c01eef55` imagen.

| # | Sev | Area | Issue | Evidence / root cause | Status |
|---|---|---|---|---|---|
| 1 | CRIT | Workout | One Easy-tier set permanently marks the **whole workout complete**, with an empty set log | `workout_logs.status` DEFAULTs `'completed'`; mig-2256 `trg_sync_workout_completion` then flips `workouts.is_completed`. Easy creates the parent log on the **first** set with an empty array. Prod row after 1 set of a 6-exercise workout: `status='completed'`, `sets_json='[]'`, `is_completed=true` | **FIXED** wave 2 (2026-07-30) `2321a53e` + mig 2390 — DEFAULT flipped to `in_progress`, trigger now guards on a non-empty sets_json, `/sync/bulk` fixed, and the 18 corrupt logs + 7 falsely-completed workouts repaired (backed up first). **Client half fixed wave 3 (2026-07-31) — agent-reported, not independently re-run this wave.** New `_easyFinalizeQueue` (a separate `OfflineWriteQueue` namespace from the `/complete` queue) + `_easyFinalizeWithOfflineFallback()`; `runEasyBackgroundSave` step (1) now routes through it instead of calling `updateWorkoutLog`/`createWorkoutLog` directly, with a `wklog_$workoutId` idempotency key so a partial-success replay cannot duplicate |
| 2 | CRIT | Menu scan | **Every menu/bill page uploaded as a 1×1 pixel** since 2026-07-22 (`f1cb7333`) — feature 100% dead on both platforms | `_normalizeMenuPage` passes `minWidth/minHeight: 1` believing it means "never scale down". It's the **target box**: iOS `fminf(1, 1/3024)`, Android `max(1, min(3024,4032))` → 1×1. `bytes.isEmpty` can't catch it (a 1×1 JPEG is ~700 valid bytes) | **FIXED** wave 2 (2026-07-30) `ed9cf5b5` — real server-side dimension floor added; a 1×1 upload is rejected instead of analysed |
| 3 | CRIT | Menu scan | Blank scan returns a **fabricated dish with price and macros, ready to log** | Not a hallucination and not a cache collision (`menu_scan_cache` = 0 rows). It is the prompt's own few-shot example echoed verbatim: `vision_service.py` contains exactly `"Tandoori Chicken Half"`, `14.95`, `487`, `"Served with choice of one (1) Side"`, section `mains` | **FIXED** wave 2 (2026-07-30) `ed9cf5b5` — the escape hatch was unreachable because `response_schema` never declared it, so constrained decoding could only echo the exemplar. Schema now declares it, and all three OCR prompts use self-identifying placeholder tokens so an echo is harmless. Test asserts no exemplar literal survives |
| 4 | HIGH | Workout | Infinite **"Generating your workout…"** — spins forever, no workout ever created | Two endpoints disagree, ~every 30 s forever: `/today` excludes completed workouts → "needs generation"; `/generate-stream`'s dedup filtered `is_current`+`status` but **not `is_completed`** → "already exists". Caused by #1 | **FIXED** wave 2 (2026-07-30) `2321a53e` — predicate moved into the query, and the four sibling `is_completed`-blind dedup sites fixed |
| 5 | HIGH | Nutrition | Fabricated **2000 kcal / 150 g** targets shown as the user's plan on Home, Nutrition and the public share card | `/nutrition/dynamic-targets` defaulted `prefs.get("target_calories") or 2000` and returned it as a *dynamic target*; the client stores that, and `hasConfiguredTargets` flips true on any non-null → **one server default defeated the gate on every surface at once**. On-screen 2200/165 = `2000+200` bump and `150×1.1` | **FIXED** wave 2 (2026-07-30) `fb4cc8fb` — `/nutrition/onboarding/skip` no longer PERSISTS `{2000,150,200,67,25}`, and the surviving read-time fallbacks are gone |
| 6 | HIGH | Chat | Free tier was **10 coach messages for the lifetime of the account**, not per day; counter never moved, wall arrived as "Something went wrong" | Enforcement used `ai_chat` whose `reset_period` is **NULL**, and `_get_current_usage` treats NULL as *"sum all time usage"*. UI counted a different key (`ai_chat_messages`) | **FIXED** wave 2 (2026-07-30) `f8ab948b` + mig 2380 — **a lifetime founder was being metered and 402'd as a free user.** New `core/feature_gate_policy.py` is the single tier-rank source; `reset_period` is now NOT NULL + CHECK so a daily cap can never become a lifetime cap again. Both negative-tested |
| 7 | HIGH | Workout | **Reschedule always 404'd** from all three entry points | Client called `PATCH /workouts/{id}/reschedule` — a route that never existed | **FIXED** `2f979201` — verified. Residual: `POST /workouts/swap` and the coach's `ai_reschedule` tool still write a bare `YYYY-MM-DD` (midnight UTC). Now caught by the new `scheduled_date_anchor --check` gate. Sibling writers **fixed wave 3 (2026-07-31), independently verified** — `scheduled_date_anchor.py --check` now reports 0 known-outstanding raw writes; both `_KNOWN_OUTSTANDING` allowlist entries (`watch_sync.py`, `coach/daily_insight.py`) were fixed and removed, the deletion itself being the verification |
| 8 | HIGH | Onboarding | Quiz **Primary Goal + Muscle Focus silently discarded**; first plan ignored the goal the whole funnel promised | Both POSTed and both have real `users` columns, but `UserPreferencesRequest` declared neither → Pydantic dropped them | **FIXED** wave 2 (2026-07-30) `2221261b` + mig 2381 — the shared cause was `PUT /users/{id}` silently dropping fields and returning 200. The fitness assessment now persists (5 capacity columns added) |
| 9 | HIGH | Scores | One partial readiness row **500s the entire history** — and `/scores/overview` too | Every column in `readiness_scores` is nullable (incl. `submitted_at`/`created_at`) but `ReadinessResponse` required all; the list endpoint validates every row. Partial rows are normal — the mid-workout check-in writes only the sliders it collected | **FIXED** wave 2 (2026-07-30) `881f9c86` — all three sibling aggregations routed through one helper; 'not enough data' is an explicit state, not a swallowed exception. A third residual instance (`crud_background_tasks.py`, listed under wave 2's "What remains OPEN" as "#9 instance 3") **fixed wave 3 (2026-07-31), independently verified** — now imports `readiness_window_from_response` + `NEUTRAL_READINESS_BASELINE`; a `has_signal` branch replaces the raw `sum()/len()` that `TypeError`'d on a NULL score |
| 10 | HIGH | Nutrition | **Every dish image 502'd** | Imagen called at `location=global`, where it doesn't exist (`404 NOT_FOUND … imagen-4.0-fast-generate-001`). Gemini text serves from `global`; Imagen is **regional only** | **FIXED** wave 2 (2026-07-30) `c01eef55` — see #106. **Re-verified live 2026-08-01, and the resolution is not the one this row's title implies.** `POST /nutrition/dish-images/generate` with a novel dish name → 200 with a real S3 URL, and fetching that URL directly → 200 / 1.3 MB PNG (existence-checked, not merely non-null — see the media-URL gate lesson). The serving model is **`gemini-2.5-flash-image`, not Imagen at all**: `image_generation_service.py:101` sets it as `DEFAULT_IMAGE_MODEL`, and the file header records why — the production service account is **403 for every `imagen-*` model** (`4.0-fast`, `3.0-fast`, `3.0-generate-002`), so the region fix alone would never have been sufficient. An `_is_imagen()` regional branch (`:113`, `:125`, `:249`) is retained so setting `IMAGE_GEN_MODEL=imagen-*` still routes regionally if access is ever granted. Recording this explicitly because the row's stated root cause (wrong region) reads as if Imagen is still in play; it is not |
| 11 | HIGH | Nutrition | Menu-scan catalog lookup **timed out every time**, silently disabling the free tier and pushing every dish to paid Imagen | `IN (≤60 names)` against `food_database` with **no btree index on `name`** (only GIN trigram on `name_normalized`, unusable for `IN`). 714,468 rows / 1.8 GB → seq scan → `statement timeout (57014)` after 9.3 s, `resolved=0`. Fail-soft, so nothing 500s | **FIXED** wave 2 (2026-07-30) `fb4cc8fb` + mig 2385 — the lookup now dedupes and orders deterministically so the free catalog tier actually resolves; `/nutrition/search` no longer uses an ILIKE no index can serve |
| 12 | HIGH | Home | `/today` failures rendered as a confident **fake "Rest day"**; error hero, retry and circuit-breaker unreachable | `getTodayWorkout()` swallowed every error into `null`, which the notifier reads as a rest day. Same branch re-polled every 3 s with **no ceiling** — 20 req/min/client, amplifying the outage | **FIXED** wave 2 (2026-07-30) `b7d333ac` — the backoff reset was moved out of `_cancelPolling()`; a test drives the real notifier and asserts the ladder walks 3/6/12/24/48 and stands down |
| 13 | MED (was HIGH) | Workout | **Easy/Advanced toggle: 2 of the original 4 claims retracted, 2 confirmed** — it switches fine but **resets the session timer and never persists the choice** | **Retested 2026-07-29 on the rebuilt binary, mid-workout.** Retracted: it does **not** exit the active workout, and it **does** switch — tapping Advanced rendered the real Advanced UI (per-set table, `RIR 3` badges, ADD SET, thumbnail strip, "Welcome to Advanced" 1/6 coach mark) with the current exercise and `Set 1 of 3` preserved. Those two original claims came from taps made before the window-drift discovery. Still confirmed: (a) **session stats reset** — Easy showed `3m 24s / 23 kcal`, immediately after switching the header reverted to "Warmup" with `9s / 1 kcal`; (b) **the choice is never persisted** — `users.workout_ui_mode` is still `NULL` and `workout_ui_mode_user_explicit` still `false` after an explicit tap (SQL-verified), so the app cannot honour the user's mode next session | **FIXED** wave 2 (2026-07-30) `2221261b` (server) + `49622552` (client) — the mode now persists; the silent field-drop was the cause |
| 13b | HIGH | Workout | **Editing a logged set never reached the database** | Set editing itself works (focal card pulls the set back, pill switches state, button becomes "UPDATE SET N"). But the edit branch returned early on "(Backend re-syncs via plan save)" — it does not. UI showed `1 5×12` and volume 1,968→1,536 kg while `performance_logs` kept `reps 10 / weight 0` | **FIXED** `cdd8721b`. Residual: `performance_logs` had no unique natural key, so one correction could rewrite sibling rows — closed by mig 2342 (2026-07-30) |
| 13c | HIGH | Theme | **Accent flipped between launches** — same screen cyan one launch, orange the next | `AccentColorScopeWrapper` passed the async-loading gym `profileColor` as a global `accentOverride`, so screens painted in the user's accent before it resolved and the gym's colour after | **FIXED** `4662b7d8` for the gym override, completed (2026-07-30) — `app.dart` still computed `gymOverride ?? accent`, which was the remaining source. See #15 |
| 14 | HIGH | Onboarding | Measurement inputs show **0** while the ruler marker sits at a real value (170 cm / 70 kg), and the primary button is greyed with no reason | Number and ruler contradict each other; a 0 reads as an error, not an empty state. No hint that tapping the number is what unlocks Quick start | **FIXED** `35b5f6e7` — verified 2026-07-30 — the ruler seeded an anthropometric fallback the readout never saw; they now agree and the disabled CTA says what is missing |
| 15 | HIGH | UX / global | **Accent colour is not stable across sessions** — the identical active-workout screen renders cyan in one run and orange in another; same for the meal-log sheet | Colour stops carrying meaning; app reads as assembled from parts. Cyan also owns Stats, You, coach chips and the DOB picker while amber owns fitness chips and home coach-marks. **Re-verified 2026-07-28 on the rebuilt binary that contains the #13c fix**: the pre-workout "Quick check-in" sheet (Sleep/Readiness sliders + Next button) still renders **cyan** while every surrounding surface is orange — so #13c removed only the gym-override cause, and at least one more accent source remains **STILL BROKEN — verified 2026-07-30, and the register's own evidence names the exact widget.** Real commit is **`adf69b70`** "one accent source of truth", which built the right infrastructure (single `accentColorProvider`, an `accent_source_gate.dart` regression gate, a baseline diff) **but never touched the widget this row cites**. `lib/screens/workout/widgets/pre_workout_reshape_gate.dart` — the pre-workout "Quick check-in" sheet — still carries **12 hardcoded `AppColors.cyan`**, including the primary **"Start workout" CTA (`:377` `backgroundColor: AppColors.cyan`)** and both gauge sliders (`:424` `activeColor`, `:416/474/482`). These were grandfathered into `adf69b70`'s own baseline, so the gate does not flag them. This is **not** the single-frame async-load flicker `accent_color_provider.dart:14` describes fixing — it is a permanent hardcode that renders cyan on EVERY presentation regardless of the user's accent. Independently corroborated by screenshot: this session photographed that sheet with an orange WARM-UP label beside cyan sliders and a cyan Next button. Running the gate live also **FAILS** on 2 files with new hardcoded accents since baseline (`quiz_days_selector.dart` 3→5, `pre_workout_reshape_gate.dart` 11→12). **Fixed wave 3 (2026-07-31) — independently verified by me (the lead ran every command below directly), sweep complete.** Final result: **10,537 hardcoded accent-family findings across 1,212 files → 0.** Every finding was either migrated to the single source (`context.accentColor` / `ThemeColors.of(context).accent`, ~536 files now read it live) or annotated `// accent-allowlist: <specific reason>` where the colour is genuinely NOT the accent — success/error/warning severity, macro identity, rarity/tier, medical reference ranges, chart-series legends, third-party brand marks, and named design-family palettes. This includes the `pre_workout_reshape_gate.dart` sheet this row specifically named — its 12 hardcoded `AppColors.cyan` (the Start-workout CTA and both gauge sliders) are migrated via the `OnboardingTheme` chokepoint, which propagated to ~30 onboarding screens. **The gate's baseline is now EMPTY** — nothing is grandfathered, so any newly hardcoded accent colour fails the gate by construction. `flutter analyze lib` → 0 errors (5,346 infos/warnings, consistent with the documented ~5,331 pre-existing baseline). `flutter test test/core/theme/accent_single_source_test.dart` → 3/3. **Negative-tested**: appended a single `const _negTestColor = Color(0xFF06B6D4);` to `lib/screens/splash/splash_screen.dart` → gate FAILED, exit 1, reporting `lib/screens/splash/splash_screen.dart: 0 → 1`; reverted → exit 0. So the empty baseline is genuinely enforced, not merely declared. See the wave-3 "new defects" list for a bug the gate itself carried at the moment the sweep succeeded, plus two collateral findings from closing it out | **FIXED** wave 3 (2026-07-31) — independently verified by me, 10,537/1,212 → 0, negative-tested |
| 16 | HIGH | UX / global | Unlabelled circular **"+" FAB physically covers real controls** on Home, Workout, Nutrition and You | Sits on top of "Log water" (`113`), the "YOUR HABITS" row (`86`), the LUNCH header (`73`). No label, and its meaning changes per tab — near-certain mis-tap | **FIXED** wave 2 (2026-07-30) `679620e6` — label added, and all four tab scroll extents now derive their clearance from one token instead of reserving only nav-bar height |
| 17 | HIGH | UX / Home | Day-one zero states presented as **rankings and losses** — "LAST WEEK · #3 · +0 XP", "0 / 464 earned", four 0% pillars | User signed up minutes ago; "last week" is a week they didn't have the app, "#3" implies a competition never entered | **FIXED** `f9e49e2c` — verified 2026-07-30 — day-zero surfaces reframed; the leaderboard no longer ranks an account with signup-only XP |
| 18 | HIGH | Workout | Workout logger is in **kg** although the user entered lb and explicitly toggled to lb in onboarding | A US user is asked to log "25" for a bench press — either corrupts their history or bounces them to hunt for a setting. Project convention is lbs | **FIXED** wave 2 (2026-07-30) `2221261b` + `49622552` — `users.weight_unit` DEFAULTed to `'kg'` and onboarding never wrote it. Long tail **fixed wave 3 (2026-07-31), independently verified** — 0 remaining `2.20462` literals / hardcoded `' lb'` in both summary files. Two more latent instances of the same class found and fixed in the same pass: 3 weight ARB keys (`summaryEst1RM`, `summaryBestSet`, `workoutSummaryGeneralLbXReps`) baked `' lb'` into the message text in all 36 locales — a `{unit}` placeholder now replaces it, 0 literal `' lb'` remaining; and `general.dart` had `useKg: false` hardcoded in two sections, a second independent unit bug under this row . **Independently settled 2026-08-01 on a fresh post-fix account, because the walk account could not settle it.** The walk account still renders "Increment 2.5 kg" on screen, but its `weight_unit` was written `'kg'` on 07-28 *before* the fix — so stale stored data and a live regression are indistinguishable from the outside, and the row was deliberately not scored on that evidence. Decisive test: brand-new account `qa3` created via real Supabase signup (`weight_unit` confirmed **null at insert**, i.e. no longer DEFAULTing to `'kg'`), then `POST /users/{id}/preferences` with `use_metric_units: false` and **deliberately no explicit `weight_unit`** — exercising the fallback-derivation the fix added (`onboarding.py:134-144`) rather than writing the column directly, which would have proven nothing about onboarding. Live DB result: `weight_unit='lbs'`, `workout_weight_unit='lbs'`, `measurement_unit='in'`, `distance_unit='mi'` — all four independent unit columns derived correctly from one toggle. Root cause genuinely closed; the on-screen kg is stale data |
| 19 | HIGH | Onboarding | Coach preview **answers a different question than the one asked**, under "Live chat unavailable" | Asked "What should I eat before a morning workout?", got the first-workout-nerves answer. `_handleFallback` substituted a fixed `_chips[1].answer` regardless of the question | **FIXED** wave 2 (2026-07-30) `48b7c5a4` + `dc9bce36` — degrades honestly instead of answering a different question |
| 20 | HIGH | Onboarding | Commitment card **clipped mid-word** by the HOLD TO COMMIT button — the emotional peak of the funnel, seconds before the paywall | Sticky footer overlays content instead of sitting below it | **FIXED** `35b5f6e7` — verified 2026-07-30 — sticky footers now sit below content instead of overlaying it (same fix as #34) |
| 21 | HIGH | Workout | Date strip says **Tuesday 28** while the card beneath says **"Wednesday — No workout yet"** | Two adjacent elements assert different days; user can't tell if they missed something or the app is broken | **FIXED** `e10473d2` — verified 2026-07-30 — both elements read one resolved local date |
| 22 | HIGH | Quick Generate | Sheet ships literal placeholder strings **"Title" / "Subtitle"** | Reads as unfinished beta software and retroactively undermines trust in everything else | **FIXED** `e10473d2` — verified 2026-07-30 — an i18n extraction bug that emitted the slot name instead of the source string; all 35 non-English locales had *translated the placeholder* |
| 23 | HIGH | Workout | Flutter **render-overflow stripe** on the rest-timer bar mid-workout, clipping the water-log button | "RIGHT OVERFLOWED BY 5.3 PIXELS". Controls are intrinsically sized and the SKIP label is **localised**, so width isn't knowable at build time; the bare `Spacer` collapsed to 0 | **FIXED** wave 2 (2026-07-30) `49622552` — both surviving bare-`Spacer` sites fixed, proved by a test sweeping width × locale × text scale |
| 24 | MED | Workout | Quick Generate writes `scheduled_date` at **midnight UTC**, violating the noon convention | Prod: quick workout at `2026-07-28 00:00:00+00` vs normal generation at `2026-07-29 17:00:00+00`. Midnight UTC = 7pm the **previous** local day for US users → mis-days in every local-day window | **FIXED** `af3f28c1` — verified 2026-07-30 — new `scheduled_date_anchor.py` chokepoint + its own `--check` regression gate; every writer in the module converted |
| 25 | MED | Onboarding | `target_weight_kg` is **NULL** on the fresh account despite entering 76 kg — and the UI displays "Goal 76 kg" | Client holds it, DB doesn't. Projection screen used it correctly (6 kg to lose → Oct 20) | **FIXED** wave 2 (2026-07-30) `2221261b` — `target_weight_kg` persists |
| 26 | MED | Nutrition | Meal-log sheet offers **nine overlapping entry points** at once | SNAP / PHOTO / SCAN read as the same action; DESCRIBE is indistinguishable from the search field; "Quick log" is clipped with no scroll affordance | **FIXED** `4d873f40` — verified 2026-07-30 — consolidated with #107 |
| 27 | MED | Workout | Starting a workout requires passing **up to five interstitials** | Detail screen → soreness sheet → readiness check-in → warm-up list → first set. Individually reasonable, stacked it reads as the app negotiating instead of starting. Most likely day-2 churn point | **FIXED** `e10473d2` — verified 2026-07-30 — the interstitials are collapsed without dropping the data capture |
| 28 | MED | UX / global | **Three coach-mark tours plus a permission modal plus two banners** in the first minutes | Home 5-step (amber), Nutrition 2-step (cyan), Workout 5-step (cyan), Connect Health modal over a screen already carrying a Daily Crate banner and "Coach is learning you" | **FIXED** `f9e49e2c` — verified 2026-07-30 — tours sequenced so one runs at a time |
| 29 | MED | Onboarding | Pre-auth demo completion credits **44:12, 320 cal, 12,450 volume, 3 PRs, 14-day streak** after ~90 s and three sets | Teaches the user that Zealova's stats are decorative — costly right before a projection chart and a $59.99 ask | **FIXED** `35b5f6e7` — verified 2026-07-30 — the demo reports what actually happened |
| 30 | MED | Nutrition | Post-log check-in is a **full-screen ~20-chip questionnaire** labelled "optional" | Goal was "log my eggs". Blocking the screen while calling itself optional reads as bait-and-switch; "Don't show again" is the only permanent out | **FIXED** `4d873f40` — verified 2026-07-30 — genuinely optional and non-blocking |
| 31 | MED | Workout | Reschedule gives **no confirmation and shows no date** anywhere on the detail screen | After confirming, the screen is pixel-identical to before. Picker also seeds to today rather than the workout's actual date | **FIXED** `e10473d2` — verified 2026-07-30 — confirms, shows the date, and seeds the picker to the workout's own date |
| 32 | MED | Workout | Tab title is the **gym name**, truncated to "COMMERC…" | A tab titled by location makes it unclear what screen you're on; the chevron looks like a screen switcher but is an equipment-profile switcher | **FIXED** `e10473d2` — verified 2026-07-30 — the tab is titled for the screen; the profile switcher reads as one |
| 33 | MED | Coach | Three competing suggestion systems in one viewport (plain bubbles, cyan action pills, clipped grey chips) | Three visual treatments imply three kinds of thing; they're all prompts. "Analyze M…" clipped with no scroll cue | **FIXED** `b906c523` — verified 2026-07-30 — one treatment |
| 34 | MED | Onboarding | Fitness-check third card **clipped by the sticky Continue bar**, and Continue is dimmed with no explanation of what's missing | Most common cause of "is this app broken?" in onboarding | **FIXED** `35b5f6e7` — verified 2026-07-30 — same sticky-footer fix as #20, plus a reason on the dimmed CTA |
| 35 | MED | Onboarding | Intro carousel **ghosts**: overlapping captions ("reads your menu" over "syncs your data") and a blank slide image; two headlines fight for one screen; dots render over the photo | Broken vertical rhythm with ~500px of dead space | **FIXED** `35b5f6e7` — verified 2026-07-30 |
| 36 | MED | Chat | Saved conversations **lost every action card, chart and agent identity** on reopen | `context_json` is written with `json.dumps` into a `jsonb` column so it reads back as a **string** and `is Map` was false; even as a Map the whole bag went to `actionData` instead of its inner `action_data` | **FIXED** wave 2 (2026-07-30) `48b7c5a4` + mig 2389 — the WRITER now passes a dict; the 37 double-encoded production rows were re-parsed (backed up first) and `audit_jsonb_double_encoding.py` gates the class |
| 37 | MED | Chat | Message delete **never persisted** — bubble returned on next reload after "cannot be undone" | Client sent the synthesised composite id (`<uuid>_u`), rejected as an invalid uuid; error logged and dropped | **FIXED** wave 2 (2026-07-30) `48b7c5a4` — the assistant bubble carries the bare row PK, so delete and regenerate work |
| 38 | MED | Stats | Deep-linked tab loads **no data at all** — no card, no skeleton, no error | `initState` set the tab index before `_userId` resolved (listener bailed on its null guard), then `_loadData()` hardcoded tab 0. `FitnessScoreCard` collapses to `SizedBox.shrink()` on a null breakdown | **FIXED** wave 2 (2026-07-30) `b7d333ac` — the card owns its data need and always renders a skeleton or an honest failure with retry, never nothing |
| 39 | MED | Onboarding | Three surfaces quoted **three different goal dates** for the same plan | Chip renders `weightChangeRate ?? 'moderate'` while the raw `null` fell to `calculateWeeklyRate`'s no-rate branch (`0.5 + days/14` ≈ 0.79 kg/wk) — a rate matching **no chip**. Pact hardcoded its own 0.75 | **FIXED** wave 2 (2026-07-30) `2221261b` — the fourth ungated rate table is gone; there is one now |
| 40 | MED | Onboarding | Layout rhythm inconsistent — goals screen top-aligned, fitness-level screen has ~300px dead space above the question | | **FIXED** `35b5f6e7` — verified 2026-07-30 — one shared onboarding scaffold rhythm |
| 41 | MED | Auth | **Uninstalling the app does not sign you out** — a "clean install" resumed mid-onboarding as the previous user | iOS Keychain survives app deletion; only erasing the device clears it **STILL BROKEN — verified 2026-07-30, the fix is dead code.** Real commit is **`f9e49e2c`**, which added `lib/core/services/auth_install_guard.dart` (194 lines). The diagnosis and implementation are correct — iOS Keychain survives uninstall, so `ApiClient`'s `FlutterSecureStorage` token outlives the `SharedPreferences`-backed Supabase session, and `AuthInstallGuard` handles exactly that (`AuthInstallGuardOutcome.firstRunClearedStaleSession`). But **`grep -rln "AuthInstallGuard" lib/` returns only the file that defines it** — independently confirmed this session: zero imports, zero call sites, no `.run()` invocation from `main.dart`/`app.dart`, and no test exercising it. Nothing runs on startup, so a reinstall still resumes as the previous user exactly as originally reported. One wiring line away from actually being fixed. **Wired wave 3 (2026-07-31), independently verified** — `AuthInstallGuard` is now called at `main.dart:124`; `grep -rln "AuthInstallGuard" lib/` no longer returns only the definition file, and `flutter test` on its suite passes 6/6. This closes exactly the gap the row names: the fix existed but nothing called it, and now something does | **FIXED** wave 3 (2026-07-31) — wired and independently verified |
| 42 | LOW | Onboarding | Unlabelled **star glyphs** under "3 days"/"4 days" with no legend, while a different "★ Best" badge appears on the same screen | Two star treatments, one explained | **FIXED** `35b5f6e7` — verified 2026-07-30 |
| 43 | LOW | Menu scan | Same dish rendered **twice** (Recommended + Mains); floating "What goes best here?" chip collides with the action bar | Shortlist re-renders cards that also appear in their section — fine at 20 dishes, pure duplication at 1 | **FIXED** `4d873f40` — verified 2026-07-30 |
| 44 | LOW | Marketing | Exercise count differs between adjacent screens — **"1,700+"** vs **"1,722"** | Mismatched hero numbers right before the paywall. Social buttons also pull three foreign brand palettes into an orange-on-black system | **FIXED** `c0e9d90b` (2026-07-30) — **and this one was caught by deploying, not by review.** The first fix added `catalogStats.ts` but wired only FAQ and Pricing, leaving 8 hardcoded sites, so production briefly served THREE numbers instead of two. All surfaces now read the module. **Verified on the live site:** `/`, `/pricing`, `/faq`, `/features`, `/lifetime`, `/roadmap`, `/blog` serve only `2,300+` (library) and `1,800+` (with video demos), with zero stale numbers and zero interpolation leaks. Note both ORIGINAL numbers were stale, not merely inconsistent |
| 45 | LOW | Workout | Set target reads **"BW × 10"** with a `0` under "WEIGHT (KG)" | Beginner won't know BW = bodyweight; the 0 suggests the field is broken and invites a "fix" | **FIXED** `e10473d2` — verified 2026-07-30 |
| 46 | LOW | Nutrition | Headline **"648"** has no unit or label, sitting beside a "2,000 KCAL" pill | Reads ambiguously as eaten / remaining / a score | **FIXED** `4d873f40` — verified 2026-07-30 |
| 47 | LOW | Workout | Set counter reads **"SET 3 OF 2"** after both sets of a 2-set exercise are logged | Off-by-one / unclamped **STILL BROKEN — re-observed live 2026-07-30 18:25** on the rebuilt binary. After logging both sets of a 2-set exercise, the header reads **"SET 3 OF 2"** (in both the stepper row and the card title), not the guarded "ALL 2 SETS DONE". `e10473d2` was reported VERIFIED via `easy_exercise_header.dart:210-214`; the guard does not hold on this path. Screenshot evidence captured. **Fixed wave 3 (2026-07-31) — agent-reported, not independently re-run this wave** — the header guard at `easy_exercise_header.dart` was real but two other render sites were unclamped. Fixed at 3 sites: `futuristic_set_card.dart` (`isAllDone` branch on badge+label), `easy_rest_overlay.dart` (`_displaySetNumber` getter — the surface visible for the whole rest window), `easy_focal_column.dart` (clamped `setNumber` into `TimedExerciseTimer`). Negative-tested: reverting reproduced `SET 3 OF 2` / `SET 3/2` exactly | **FIXED** wave 3 (2026-07-31) — agent-reported, not independently re-run |
| 48 | LOW | Workout | Subtitle reads **"Chest (pectoralis major) & Chest"** — same muscle twice | | **FIXED** wave 2 (2026-07-30) `49622552` |
| 49 | LOW | Onboarding | Primary CTA **moves** when a confirmation chip appears above it, so a tap aimed at the enabled button lands on the old position | | **FIXED** `35b5f6e7` — verified 2026-07-30 |
| 50 | LOW | Onboarding | Coach Mike's card renders as a **large empty brown block** — avatar/image asset didn't load | | **FIXED** `35b5f6e7` — verified 2026-07-30 |
| 51 | LOW | Workout | Metadata pills use **three different colours** implying a code that doesn't exist; sticky LET'S GO bar overlaps the first exercise row | | **FIXED** wave 2 (2026-07-30) `49622552` — LET'S GO overlap fixed |
| 52 | LOW | Tooling | `flutter analyze` was **unusable** — ~10,700 errors from `build/ios/SourcePackages` burying real `lib/` errors, and differing per machine | `dart analyze` ignores `.gitignore`; SwiftPM checks out the whole FlutterFire monorepo | **FIXED** `f96b8301` — re-verified 2026-07-30: `flutter analyze lib` → **0 errors** (5,331 infos/warnings remain, all pre-existing) |
| 53 | HIGH | Programs | Editing an active program **deletes weeks 2..N** and collapses each remaining week to a single workout | Proved by simulating the planner | **FIXED** wave 2 (2026-07-30) `d700e995` — an edit is a targeted update, not a delete-and-re-expand |
| 54 | HIGH | Programs | A program that isn't 100% complete **permanently suppresses generation on its weekdays**, with no date expiry — and progress never increments for tracked sessions, so the condition is effectively guaranteed | | **FIXED** `afce2a7c` — verified 2026-07-30 + mig 2372 — assignment progress is now derived at the DB chokepoint by statement-level triggers, and an assignment whose plan has run out settles, which ends the unbounded weekday suppression |
| 55 | HIGH | Chat | Chat **"Apply" writes to a phantom column** — `workouts` has `exercises_json` and **no `exercises`** (confirmed live), yet 4 of 5 dispatch actions send `{"exercises": […]}` → 42703, whole update rejected, card still reports success | The column-drift gate **passes**, so it does not cover writes made through the `db.update_workout` helper — a gap in the gate itself | **FIXED** `109465ad` — verified 2026-07-30 — all dispatch actions corrected, rejected writes now surface as failures, and the column-drift gate extended to cover `db.update_workout`-mediated writes (the gap that let it pass) |
| 56 | HIGH | Workout | **Advanced-tier sets live only in RAM + SharedPreferences until Finish** — an OS kill mid-session loses the entire workout from the database | | **FIXED** wave 2 (2026-07-30) `49622552` — Advanced sets persist progressively, so an OS kill no longer loses the session |
| 57 | MED | Workout | Single-day regenerate **broken at the DB level since 2026-05-03** | | **FIXED** wave 2 (2026-07-30) `2321a53e` |
| 58 | MED | Workout | `POST /performance/drink-intake` can **never succeed** — model requires `workout_log_id`, table has no `workout_id` column | **Was mislabelled FIXED.** `35d7c83f` only touched the backend (`performance_db.py` + `performance_db_endpoints.py`, adding `verify_user_ownership` and a docstring). The **Flutter caller was never changed**: `workout_repository_performance.dart:396` still posted `'workout_id'`, and `workout_flow_mixin.dart:409` passed `workout.id!` — a `workouts.id`, not a `workout_logs.id` — so even a renamed field would have violated the FK. Fixed 2026-07-30 on the client: field renamed to `workout_log_id`, stray `logged_at` dropped (not on the model), and the call now chains off `createLogFuture` so it uses the real `workout_logs.id` (that row does not exist yet when the completion handler starts). Skips with a logged warning if no log id is available rather than sending a fabricated one | **FIXED** — client half 2026-07-30 |
| 59 | MED | Onboarding | **Gym-profile unique-constraint failure during onboarding** — `duplicate key … idx_gym_profiles_active_per…` | Seen in Render log 2026-07-23 04:40:59 | **FIXED** `259aab75` — verified 2026-07-30 + mig 2347 — mig 2327 cured the sequential race but not the concurrent one; the demotion is now serialized |
| 60 | MED | Workout gen | Recurring **"only 2 valid exercises for 'lower' focus (minimum required: 3)"** — workouts ship below the volume floor | Render log, repeatedly, incl. 2026-07-28 06:04 | **FIXED** `af3f28c1` — verified 2026-07-30 — not a volume-floor breach: a vocabulary mismatch made `focus='lower'` pre-fetch a mostly upper-body pool. Bridge added and the repair backfill re-enabled on the streaming path |
| 61 | MED | Coach | Coach-created workouts also write `scheduled_date` at **midnight UTC** — same mis-day class as #24 | Prod: chat workout at `2026-07-28 00:00:00+00` (`generation_source='chat'`) vs library workouts at `17:00:00+00`. Chat workout also takes `is_current=true`, so it can collide with the day's planned session | **FIXED** wave 2 (2026-07-30) `2321a53e` — `is_current` semantics fixed so a chat workout cannot displace the scheduled session |
| 62 | MED | Coach | Workout-card **"Start" button clipped to one letter per line** ("St / ar / t") in a narrow pill | Proposal card action row overflows | **FIXED** `b906c523` — verified 2026-07-30 — same fix as #94 |
| 63 | MED | Coach | A workout titled **"Quick Power Full Body"** is composed entirely of **stretches** — Calve Stretch, Standing Forward Fold, Single-Leg Hamstring Stretch | Content contradicts the title and the user's muscle/strength goal | **FIXED** `109465ad` — verified 2026-07-30 |
| 64 | LOW | Content | Exercise named **"Calve Stretch From Wall To Floor"** — "Calve" should be "Calf"; and it is labelled **"Full Body (Multiple Muscle Groups)"** for a calf stretch | | **FIXED** `259aab75` — verified 2026-07-30 + mig 2348 — renamed only the sides that do not track the S3 object; the rest get an alias. `audit_exercise_naming.py` is the baseline gate |
| 65 | MED | Home | Today card shows **FRI, JUL 31** on Tuesday Jul 28 when training days are Mon/Wed/Fri/Sun — Wednesday's session is missing entirely | Related to #21 | **FIXED** wave 2 (2026-07-30) `b7d333ac` — client half fixed. Server half **fixed wave 3 (2026-07-31), independently verified** — `today.py` now sets `_NEXT_WORKOUTS_LIMIT = 14` (was `limit=1`) and adds an additive `next_workouts: List[TodayWorkoutSummary]` field, so Wednesday's session is no longer dropped in favour of a single upcoming day |
| 66 | MED | Gamification | **Level-up overlay fires on day 0** (Level 2, 156 XP) from onboarding alone, before any workout or meal | Reinforces #17 — progress theatre before the user has done anything | **FIXED** wave 2 (2026-07-30) `b7d333ac` — the overlay is gated behind a real completed action. Making the underlying day-0 XP award itself honest: **fixed wave 3 (2026-07-31) — agent-reported, not independently re-run this wave** — `first_login` (+100) KEPT as an honest one-time welcome gift; `daily_login` (+25) SUPPRESSED on the same call that pays `first_login` (it was double-counting ONE login event as two rewards — see the wave-3 "new defects" list); `first_goal_set` (+25) ZEROED (a mandatory onboarding field is not an accomplishment); the daily-crate roll now requires the account to be ≥24h old. Day-0 total 156 → 100, below the Level 2 threshold. Existing awarded rows untouched. Migration 2400, applied to prod |
| 67 | LOW | Workout | Opening **Note** triggers an iOS **Speech Recognition** permission prompt before the user has tapped Dictate | Permission asked for a feature not yet invoked | **FIXED** `e10473d2` — verified 2026-07-30 — the permission is requested when Dictate is invoked |
| 68 | LOW | Workout | Exercise illustration is **cropped oddly** — the anatomical figure is cut off top and bottom | | **FIXED** `e10473d2` — verified 2026-07-30 |
| 69 | LOW | Coach | Chat timestamps render **24-hour** ("14:06") while the rest of the app uses 12-hour ("2:06") | | **FIXED** `b906c523` — verified 2026-07-30 — 12-hour, resolved in the user's timezone (with #95) |
| 70 | LOW (downgraded) | Library | Exercise search results **stall on the first character under synthetic fast input** — field shows the full query while the list stays filtered to char 1 | **Originally logged HIGH as "matches only the first character"; that framing was wrong and is retracted.** At human typing speed search is **correct**: one char/sec → "plank" → 55 genuine results (Plank On Elbows, High Plank, Plank Jack…), and the client ranker + backend `/exercises/smart-search` both sort exact/prefix matches first, so neither is defective. The stall reproduces **only** when `cliclick` types a whole word instantly: field reads `calf`, list stays on `c` (Chin-Up, Cluster, Cocoons…), still stale after 4s and after Return. Cause not pinned down — a controller-listener fix (drive search off `_searchController` rather than `TextField.onChanged`, so every mutation counts regardless of source) did **not** change the synthetic behaviour, which argues the missing characters never reach the widget at all and this is an **artefact of the automation harness, not a user-facing defect**. The listener change was kept anyway: it is strictly more robust and covers paste/autocorrect/dictation. **No real-user repro path demonstrated — do not treat as a shipping bug without one** | **FIX KEPT, NOT REPRO at human speed** — `netflix_exercises_tab.dart` |
| 71 | HIGH | Workout | **Re-running a workout DOUBLES its logged sets** | The `workout_log` is reused via `idempotency_key`, but each set is re-INSERTED with no dedup on (workout_log_id, exercise_name, set_number). Proved: all 7 pairs from run 1 have a second row from run 2 — 20 `performance_logs` rows for a 15-set workout. Inflates volume, set counts and can mint phantom PRs | **FIXED** `35d7c83f` — verified 2026-07-30 + mig 2342 — `uq_performance_logs_set_natural_key` makes the duplicate structurally impossible; existing duplicates backed up before removal |
| 72 | HIGH | Workout | Summary reports **VOLUME 0 kg** where the live screen showed **1,476 kg** for the same session | Live screen counts bodyweight sets at ~60% bodyweight; the summary computes raw weight×reps and gets 0. Two different volume formulas for the same workout | **FIXED** `35d7c83f` — verified 2026-07-30 — one shared volume function on both sides |
| 73 | MED | Workout | Three different set counts for one session: summary **9**, `workout_logs.sets_json` **13**, `performance_logs` **20** | Partly #71; also the summary and the log disagree independently | **FIXED** `35d7c83f` — verified 2026-07-30 — counts derive from one source of truth |
| 74 | MED | Workout | Time-based exercises render **"0×0"** in the summary breakdown | Lateral Plank Walk and Push-Ups show 0×0 despite `set_duration_seconds=30`; duration is never surfaced | **FIXED** wave 2 (2026-07-30) `49622552` — duration surfaced instead of `0×0` |
| 75 | MED | Workout | Time-based sets persist with **`is_completed = false`** while rep-based sets persist `true` | Verified across both runs | **FIXED** `35d7c83f` — verified 2026-07-30 + mig 2343 — 0 rows remain |
| 76 | MED | Workout | `set_duration_seconds` records **time since the screen opened**, not set duration | Set 1 = 388s, set 2 = 55s, set 3 = 2s in one session | **FIXED** wave 2 (2026-07-30) `49622552` — the timer measures the set, not time since the screen opened |
| 77 | MED | Workout | Summary cannot be dismissed without rating — DONE is inert and a **red error toast** ("Please rate your workout") is used for a non-error | A red toast reads as a failure, not a prompt. SKIP RATING exists but DONE gives no hint it is blocked | **FIXED** wave 2 (2026-07-30) `49622552` |
| 78 | MED | Workout | Share card carries **no workout stats at all** — only title, date and watermark | No duration, sets, volume or PRs on a card whose purpose is to show off the session | **FIXED** wave 2 (2026-07-30) `49622552` — the share card carries the session's real numbers |
| 79 | LOW | Workout | "Save" exports a local image but mints **no `share_token`** | `workouts.share_token` still null after Save. Expected for a camera-roll export, recorded so the share-link path is not assumed tested | **FIXED** wave 2 (2026-07-30) `49622552` |
| 80 | LOW | Home | Coach card still says **"Generate today's workout or log a meal"** immediately after a workout was completed | Stale coach copy on a screen that simultaneously shows a green "Workout complete" card | **FIXED** wave 2 (2026-07-30) `b7d333ac` — copy reads the completed state. Two upstream chokepoints **fixed wave 3 (2026-07-31), independently verified (the second by hand, not by an agent — see the wave-3 method note)** — the lifecycle bare-date `.eq` replaced with `local_day_bounds`; `invalidate_daily_insight_cache`'s caller count went from 1 to 3 (`workouts/crud_completion.py` + `sync.py` added), closing the same class of gap as #41: a fix nobody calls is not a fix |
| 81 | LOW | Permissions | Photo-library prompt copy describes **picking** photos ("for AI nutrition analysis and progress photos") when the action was **saving** a share image | | **FIXED** wave 2 (2026-07-30) `679620e6` — `Gal.putImage` was passed `album:`, which forces FULL library access, so iOS showed the PICKING string for a SAVE action. Add-only scope now, whose string was already correct |
| 82 | MED | Auth | Session dropped after an idle period — app returned to the intro carousel and required a fresh sign-in | Consistent with the documented "JWT expires silently" gotcha; not conclusively reproduced as a defect (simulator was suspended) | **FIXED** wave 3 (2026-07-31), independently verified — new `resolveAuthLookupWithOneRefreshRetry` + `refreshSessionDirect()` close the race between `AuthNotifier._init()` flipping to signed-out and the token refresh completing; 6/6 tests pass, including refresh-succeeds / refresh-fails / refresh-throws / no-double-retry |
| 83 | CRIT | Injury safety | **Regenerate had ZERO injury safety** — a knee-injured user taps "Make it different" and squats/lunges come straight back | **CONFIRMED from code + schema.** `UserSafetyContext.normalized_injuries()` whitelisted against a **SINGULAR** joint tuple (`knee`, `shoulder`…) while the app stores **PLURAL** ids (`knees`, `shoulders`, `wrists`, `ankles`, `hips`, `elbows` — `injury_options.dart`). Only `neck`/`lower_back` spell the same, so **6 of 8 joints normalised to `[]`** and every caller reported `violations=0 swaps=0`. Singular is authoritative and cannot change: the joint is interpolated straight into a column name (`f"t.{joint}_safe IS TRUE"`, and `exercise_safety_index` really has `knee_safe`/`ankle_safe`/…), which also makes the whitelist the SQL-injection guard. Fixed with a plural→singular alias map applied inside `normalized_injuries()` — the one chokepoint generate/regenerate/swap all share. Verified: all 8 app ids now resolve, legacy `Lower Back`/`knee`/`back` still resolve, unknown values still dropped **VERIFIED FIXED live 2026-07-30** (API + prod SQL). `/regenerate` (`versioning.py:719-725`) and `/regenerate-stream` (`:1564-1569`) build `UserSafetyContext` then call `validate_and_repair`; singularisation now happens inside `normalized_injuries()` → `normalize_injury_tokens()` → `canonicalize_injury_tokens()` (`workout_safety_validator.py:87-131`), mapping `knees→knee` before the whitelist. First-gen and regenerate share the identical normaliser, so the two paths no longer differ. Live: regenerated a legs workout under real `active_injuries=["knees"]` → 6 exercises, every one `knee_safe=true` in `exercise_safety_index_mat`. | **FIXED** `7a7719d2` + refactor — verified live |
| 84 | CRIT | Injury safety | An equipment **"finisher" is appended AFTER the terminal safety guard** with a no-op avoid set — it can hand a knee-injured user "Horizontal Leg Press" (`knee_safe = false`) | The guard runs, then the finisher is added outside it **VERIFIED FIXED on code 2026-07-30.** The finisher is still appended after the terminal guard (`generation_streaming.py:1399-1415` vs guard at `:1348`), but `ensure_requested_equipment_represented` (`generation_helpers.py:262-341`) is no longer a no-op: it calls `filter_injury_unsafe([candidate], injuries)` per candidate (`:304,325`), walks up to `_FINISHER_SCREEN_ATTEMPTS` variants, and ships **no finisher** when all are contraindicated. Not forced live (nondeterministic to trigger) — code evidence only. | **FIXED** — code-verified, not live-forced |
| 85 | HIGH | Injury safety | **Quick Workout has no deterministic injury gate at all**, and its injury chips never pre-fill from the profile | Chips are Title-Case singular (`Knee`, `Lower Back`) matched case-insensitively against plural/underscored stored ids, so they never match. `/workouts/quick` relies on the Gemini prompt only — a squat can still appear even when the chip IS ticked **VERIFIED FIXED live 2026-07-30.** `quick.py:413-415` now merges the client chips with the **server-side** `get_active_injuries_with_muscles(user_id)` through `canonicalize_injury_tokens`, so a garbled Title-Case chip can no longer disable the guard — the DB-sourced list rides along regardless. Merged list feeds both the Gemini prompt (`:463`) and the terminal `enforce_injury_safety` (`:592-596`). Live check consistent but weak (Quick Workout's `focus` enum has no leg option, so a hard stress case could not be forced). **Additionally corroborated device-side 2026-07-30, stronger than the API check:** a Quick Generate run on the real account (`active_injuries=["knees"]`) produced 5 exercises — Captains Chair Straight Leg Raise, Dumbbell Bench Press, Standing shoulder press dumbbell, Dumbbell Figure Four Hip Thrust, Barbell Bent-Over row — **every one resolving in `exercise_safety_index` with `knee_safe=true`**, and all 5 names resolved cleanly with no id-space drift. This is a real generated session rather than the earlier weak-signal case. | **FIXED** — verified live + device-side |
| 86 | HIGH | Injury safety | The **11 muscle-area chips** (Chest, Abs, Biceps, Quads…) get a prompt sentence and **no filter** | `/generate-stream` computes `injury_context["avoided_muscles"]` and then discards it; those areas have no `*_safe` column so `enforce_injury_safety` returns immediately **VERIFIED FIXED live 2026-07-30 — decisive.** New deterministic mapper `resolve_avoided_muscle_tokens()` (`injury_guard.py:137`, no LLM, reuses `readiness_utils.get_muscles_to_avoid_from_injuries`) is wired into `enforce_injury_safety` (`:641,698`) **and** `UserSafetyContext.avoided_muscle_tokens()` (`workout_safety_validator.py:246-275`, consumed by `_check_row_safety`/`find_safe_swap`); `generation_streaming.py:1349` now passes the FULL active-injuries list (muscle chips included), so the previously-discarded `avoided_muscles` is no longer the only consumer. **Live proof:** with `active_injuries=["quads"]` and ZERO joint injury, `/workouts/preview/swap-exercise` into `Leg Extension` returned **HTTP 409 `EXERCISE_UNSAFE_FOR_INJURY`, reason `primary_target_is_avoided_muscle`** — a pure muscle-area chip is now blocked deterministically, exactly the class that used to be silently discarded. **The "Needs your call" semantics question (regional vs surgical mapping) resolved wave 3 (2026-07-31), independently verified, no code change needed** — the resolver already implements surgical semantics for muscle chips and regional semantics for joint chips: live-verified `chest → [chest, pectorals]`, `groin → [adductors]`, while `shoulders`/`knees` correctly stay regional. This row's own header was stale relative to its underlying fix note, which already matched the desired semantics. | **FIXED** — verified live (409); semantics question resolved wave 3, no code change |
| 87 | HIGH | Injury safety | Adding an injury AFTER workouts exist does not reliably invalidate them | The invalidation's today-branch uses a bare-date `.eq` against the noon-anchored timestamptz `scheduled_date`, matching **0 rows always** (production has zero 00:00Z rows: 92 at 12:00, 86 at 17:00, 22 at 16:00), so its "leave in-progress alone" policy is inverted by the upcoming sweep | **FIXED** `af3f28c1` — verified 2026-07-30 — the bare-date `.eq` (which matched 0 rows always) replaced with a real local-day window |
| 88 | MED | Injury safety | Exercise **swap** contains no injury logic at all — a knee-injured user can be offered a Barbell Front Squat as the replacement | `/workouts/swap-exercise` and `/workouts/preview/swap-exercise` **VERIFIED FIXED live 2026-07-30, both routes.** `workout_operations.py:236-247` (`/swap-exercise`) and `exercises.py:270-289,409,533` (`/preview/swap-exercise`, add-exercise) both route through `resolve_user_chosen_exercise(...)` (`injury_guard.py:534`), whose `injuries` parameter is **keyword-only with no default** — a call site cannot obtain an unscreened row. Both raise `InjuryUnsafeExerciseError` → 409. The old unguarded `/swap-exercise` in `workouts_db.py:372` is **dead code** (router never mounted — independently confirmed: `main.py` imports only `_run_background_generation` from that module). **Live proof:** on a real scheduled workout under `["knees"]`, swapping into `Air Squat` (`knee_safe=false`) → **HTTP 409, reason `safety_index`**, with no DB write (screen runs before update); same 409 from the preview route. | **FIXED** — verified live (409, both routes) |
| 89 | LOW | Class-wide | **~29 other search/filter fields share the sole-`onChanged` pattern** — a latent robustness gap, not a demonstrated defect (see the retraction in #70) | Same shape as #70 (controller + `onChanged` as the only trigger, build reads `controller.text`): `food_library_screen`, `my_foods_sheet`, `log_meal_sheet`, `menu_analysis_history_screen`, `exercise_swap_sheet`, `exercise_picker_sheet`, `program_builder_part_exercise_picker`, `program_library_screen`, `custom_workout_builder_screen`, `edit_workout_equipment_sheet`, `custom_exercises_screen`, `guest_library_screen`, `glossary_screen`, `exercise_history_screen`, `friend_search_screen`, `chat_sessions_screen`, `coach_memory_screen`, `trophy_room_screen`, `gym_equipment_sheet`, `environment_detail_screen`, `filter_section`, +8 more. Only #70 was reproduced and fixed; the rest are **untested** — each needs the same controller-listener treatment, verified per surface rather than blind-edited | **FIXED** `f9e49e2c` — verified 2026-07-30 — controller-listener treatment applied; note the real count is higher than the ~29 the row lists |
| 90 | MED | Programs / content | **17 published BEGINNER programs contain exercises the safety index rates `advanced`/`elite`**, and the Schedule tab shows that raw content before you start | **Confirmed (catalog data + on-screen).** "Gentle Start" is badged BEGINNER and sold as "for true beginners, returners, anyone carrying extra weight… busy moms, no gym", yet **Archer Push-Up — `safety_difficulty = advanced`, `shoulder_safe = false`, `wrist_safe = false` — is in ALL 8 weeks** of its default variant. Catalog-wide: 17 beginner programs, incl. **Postpartum Rebuild 6-12 Weeks, Strength After 50, Menopause Strength, Pilates Foundations, First Pull-Up (4 advanced moves), Trail Ready, Fat Loss HIIT Beginner**. **Scope correction — an earlier draft of this row claimed the assign path has no injury gating; that was wrong and is retracted.** `POST /program-templates/assign` routes days through `customize_template_days` → `enforce_injury_safety`, and the client (`program_library_screen`) defaults `_adaptToLevel/_swapForInjuries/_fitEquipment` all **true**, so the default Start does adapt. Two real gaps remain: (a) the **preview** Schedule tab renders un-customised content, so what you evaluate before starting is not what you get; (b) `customize` is `Optional[...] = None` server-side and the client only sends the block `if (adaptToLevel \|\| swapForInjuries \|\| fitEquipment)` — turn all three off and **no gating runs at all**, i.e. safety is opt-out rather than enforced. Not verified end-to-end for an injured account **Gate now PASSES — 2026-07-30.** A dedicated `audit_beginner_program_exercise_difficulty.py --check` exists and reports "no beginner-badged published program prescribes an advanced/elite exercise" (0 findings) — the 17-program content problem is cleared and now gated against recurrence. The second half of this row (injury gating being opt-out) was separately closed: #113 shows the assign path reporting "Swapped 8 for your knees" by default. | **FIXED** — gated, 0 findings; independently re-run wave 3 (2026-07-31), still 0 findings |
| 91 | MED | Programs / copy | **The plain-language gate is blind to per-exercise copy** — "RPE 4" and "~60.0% 1RM (RPE 5 — easy)" render in the Schedule tab | `audit_program_copy_clarity.py` selects only `variant_id, week_number, focus, phase` and lints those two columns, so the `workouts` JSON it never reads carries the jargon: **10,691 `program_variant_weeks` rows across 1,835 variants** have `RPE n`/`1RM`, concentrated in `weight_guidance` (10,888 exercise entries). This is the exact vocabulary CLAUDE.md bans ("RPE 7" → "effort 7 out of 10"), so the documented gate reports clean while the jargon is on screen. **Detection FIXED** `dd3a4663` — the gate now walks the `workouts` JSON's prose (`weight_guidance`, `form_cue`, `setup`, `breathing_cue`, `notes`, `coaching_cue` per exercise; `focus`/`notes`/`description` per session), leaving controlled vocabulary alone, and `\d*\s*RM\b` was added to the jargon set (`1RM`/`5RM` matched nothing before, so 33 `focus` strings had been passing too). Verified the regex still does not fire on "burpee"/"sharpen". It now correctly FAILS, over 57,814 week rows — **3,963 distinct unclear strings: `exercise.weight_guidance` 3755, `exercise.form_cue` 140, `focus` 33, `exercise.setup` 22, `exercise.breathing_cue` 7, `session.focus` 5, `exercise.notes` 1**. **Rewriting that copy is still OPEN** and is a content migration, not a code fix: the strings are formulaic enough for a deterministic pass (two atoms — `RPE n[-m]` → "effort n out of 10", `n% mRM` → "n% of your heaviest single/set of m"), and the generation prompt needs the same constraint so the next run doesn't reintroduce it **CONTENT REWRITE NOW DONE — verified 2026-07-30.** The gate I extended now returns **0 findings across 57,814 week rows** (was ~3,963). Not a script regression: live `weight_guidance` spot-checks read as plain language ("Each direction, each ankle", "Hold 30 seconds each side"). Commit `afce2a7c` shipped `rewrite_program_copy_plain_language.py`, a deterministic RPE/1RM rewrite, alongside the gate extension — so the backlog was cleared as the detection landed. | **FIXED** — gate + content, 0 findings |
| 92 | CRIT | Workout / perf | **Finishing the day's workout puts the app in a permanent refresh loop that makes it stop responding to taps** | Caught from the device log, not inferred. Once today's workout is completed `/today` legitimately returns `today_workout:null, next_workout:null, completed_today:true, completed_workout:{…}`. Two guards in `today_workout_provider.dart` each hand-rolled "is this empty?" as `todayWorkout == null && nextWorkout == null`, which is **true** for that valid settled state, so they deadlock: the watchdog calls it `state stuck, forcing refresh`, the cache guard `Refused to overwrite cached workout with empty response` keeps the stale populated cache, the state therefore never changes, and the watchdog fires again. Measured: **21 watchdog fires in 120 s** (one per ~5.7 s), 2 API calls each (`/today` + `/workouts/`) **forever while the app is open**, 28.7% CPU, and the tree rebuilds so constantly that **taps stop registering** — START PROGRAM and the Wk-2 chip both did nothing for minutes, and the Workout tab sat on "Loading your workout… Reviewing your plan…". Same shape hits **rest days** (also null/null). Fix: both guards now use the model's existing `hasDisplayableContent` predicate, which already counts `completedToday`/`restDayMessage`, instead of re-deriving a narrower one. **Verified on device against the rebuilt binary — steady-state 45 s window: 0 watchdog fires, 0 cache refusals, 2 `/today` calls (normal), CPU 28.7% → 16.6%; consecutive screenshots now byte-identical (tree no longer rebuilding), taps register again, and the Workout tab reaches its real carousel instead of "Loading your workout…"** | **FIXED** wave 2 (2026-07-30) `b7d333ac` — the rest-day predicate no longer depends on `restDayMessage`, a field the backend never sends; the settled signal now matches the backend's own settled/transient split |
| 93 | MED | Exercise data | **A misspelled exercise ships as a separate library entry** — the Exercises tab lists both "4 Coners Side Step" and "4 Corners Side Step" as if they were different movements | The typo is real but only on ONE side of the id-space split: `exercise_library` has the correct **"4 Corners Side Step_female"**, while `exercise_demos` (`4 Coners Side Step_female`), `exercise_safety_index` and `exercise_canonical` all carry **"Coners"**. So the same movement resolves under two spellings and the user sees a duplicate. Related: **700 of 2,450 `exercise_library` rows (29%) still carry a `_female`/`_male` suffix in the name** — the app strips it for display, so it is not user-visible, but it is a live matching hazard across the four id-spaces. **Needs a decision before fixing, not a guess: which side is canonical?** Renaming `exercise_canonical`/`exercise_demos` to "Corners" is the correct spelling, but the demo row's name tracks the S3 object, so a blind rename risks re-breaking media the way the 2026-07-04 folder rename did — verify with `audit_exercise_media_urls.py --check` either way | **FIXED** `6a056b17` + mig 2375 — merged. `exercise_library_cleaned` is a matview over `exercise_library UNION ALL exercise_library_manual`; the typo'd row lived in the manual table and the matview's dedupe partitions on the normalised name, so it could never collapse them. No S3 rename involved. All 4 deleted rows backed up; media gate passes over 9,239 paths |
| 94 | HIGH | Coach | **The coach workout card's primary CTA is unreadable** — "Start" renders as a narrow purple pill with the label broken across three lines ("St / ar / t") | Reproduced on every coach-authored workout card (three separate cards this session, incl. "Quick Power Upper Body"). The sibling "Save" button on the same row lays out at full width, so the Start button is being given a near-zero width constraint and wrapping per-character rather than being allowed its intrinsic width. It is the primary action on the card — the one users are meant to tap — and it currently looks broken. The card also paints a **cyan→purple gradient** with a purple Start pill while the app accent is orange (see #15) **STILL BROKEN — re-observed 2026-07-30 18:11 on the rebuilt binary (built 15:42 the same day).** The coach card for "Quick Power Full Body" renders **Start** as a narrow purple pill with the label broken vertically across four lines (**S / ta / r / t**) while the sibling **Save** on the same row lays out at full width — byte-for-byte the original symptom. `b906c523` was reported as VERIFIED FIXED for this row via `chat_media_widgets.dart:544-549,819`; that verification was code-reading only and does not hold on screen. Screenshot evidence captured. Treat the earlier VERIFIED as retracted. **Correction, wave 3 (2026-07-31)** — the fix IS in HEAD: `chat_media_widgets.dart:826-839` (not the `:544-549` this row cites, which are pre-fix coordinates from before `b906c523` landed) contains the corrected Start-button layout. No further edit was made this wave. What this row actually needs is a device repro on a binary built AFTER `b906c523` — the "STILL BROKEN" observation above may itself have been taken against a stale binary, the same class of error the register's own commit-column note (top of file) warns about | **NEEDS DEVICE REPRO** — fix is in HEAD at `:826-839`; not verified live against a post-`b906c523` binary |
| 95 | MED | Coach | **Chat timestamps flip to UTC once a message is no longer "today" by UTC reckoning** | The snack message sent at **23:49 local (CDT)** first rendered correctly as `23:49`; after a refresh the same message re-rendered as **`Jul 29, 04:49`** — the UTC instant and the UTC *date*, a day ahead of the user's actual local date (Jul 28). The newest message meanwhile still showed local `23:54`. So the formatter buckets "is this today?" by UTC date and, once it decides the message is not today, prints the UTC clock too. This is the documented UTC-date-bucketing class (CLAUDE.md: `utc_to_local_date` exists precisely to replace this) — every evening message for a negative-UTC user is mislabelled with tomorrow's date | **FIXED** `b906c523` — verified 2026-07-30 — everything resolves in the user's timezone |
| 96 | LOW | Nutrition | Meal row item count disagrees with the meal card — the DINNER row reads **"1 ITEM"** while the card directly beneath reads **"3 ITEMS · 503 CAL"** | Same meal, same screen. The row appears to count `food_logs` rows while the card counts entries in `food_items` (Whole Egg, Toast, Banana). Confirmed in the DB: one row, `jsonb_array_length(food_items) = 3` | **FIXED** `4d873f40` — verified 2026-07-30 — both count items |
| 97 | MED | Coach / content | Coach-authored workouts pull **form-cue-shaped junk names** out of the free exercise dataset | "Quick Power Upper Body" (20 min, 6 exercises) shipped **"Chest Bench Press Correct Stance"**, **"Palm Up Palm Down Rotation"** and **"Trx Body-Up"** — the first is not an exercise name at all but a cue, and TRX gear was not in the user's profile. Same noisy-library root cause the curated backfill's junk filter exists to solve (CLAUDE.md: "never select accessories alphabetically — ships junk like '180 Jump Turns'"), but the coach authoring path has no equivalent filter | **FIXED** `109465ad` — verified 2026-07-30 — reuses the curated backfill's junk filter and equipment gating |
| 98 | HIGH | Coach | **A coach-authored workout cannot be kept — "Save" is a no-op** | Tapped Save three times on the "Quick Power Upper Body" card: no toast, no state change, and **`saved_workouts` stays at 0 rows** for the account. Not a mis-aimed tap — the **thumbs-up in the same button row, at the same y, toggled to filled cyan on the first tap**, so the row is live and Save specifically does nothing. Combined with #94 (Start unreadable) and #99 (Schedule re-rolls), every terminal action on the coach's workout card fails, so the whole "chat with the coach to build a workout" flow dead-ends at a preview | **FIXED** `109465ad` — verified 2026-07-30 (server) + client — see #105 for the mechanism |
| 99 | HIGH | Coach | **"Schedule" re-generates a DIFFERENT workout instead of scheduling the one on the card**, and still persists nothing | Tapping Schedule sent "Schedule this workout for tomorrow." and the coach replied with a fresh card — same title and shape ("Quick Power Upper Body", 20 min, 6 exercises) but **different exercises**: the original listed Bodyweight Kneeling Triceps Extension / Trx Body-Up / Palm Up Palm Down Rotation / Chest Bench Press Correct Stance, the reply listed Push-Up And Rotation / Bench Dip (Knees Bent) / Wall Angel / Bodyweight Svend Press. `workouts` gained **no row** for tomorrow (2026-07-29); the only two "upper body" rows both predate the conversation (`created_at 18:42`, from the earlier program assignment). So the user's chosen workout is silently discarded and replaced by an unrequested one that is itself never saved | **FIXED** `109465ad` — verified 2026-07-30 — the action carries the workout's identity and schedules that payload deterministically, noon-anchored |
| 100 | LOW | Nutrition | Missing space in the meal-logged confirmation — **"+1 moregrilled salmon steak, grilled pot…"** | String concatenation joins the overflow count straight onto the item list with no separator. The "+N more" fragment is also placed *before* the items it is counting, so it reads as part of the first food's name | **FIXED** `4d873f40` — verified 2026-07-30 — not a missing space: `gen_l10n` emits positional parameters in ALPHABETICAL placeholder order, so the two arguments rendered swapped. New `tool/audit_l10n_placeholders.py` gates the class (12 more sites found) |
| 101 | MED | Nutrition | **"Quick log" stays empty after logging meals** — "No quick meals yet — log a meal and it'll show up here" | Shown on the Quick log tab of the log-meal sheet **after three meals had been logged in the same session** (dinner 503, snack 229, breakfast 451 — all confirmed present in `food_logs`). Either the tab does not read logged meals or its cache is never invalidated on log; the promise in its own empty-state copy is not kept | **FIXED** `4d873f40` — verified 2026-07-30 |
| 102 | MED | Workout | **The "Welcome to Advanced" coach-mark will not dismiss** — its ✕ does nothing, and it covers the Advanced screen including the back arrow | Reproduced on the rebuilt binary: switched to Advanced mid-workout, tapped the ✕ at the card's top-right **three times** across several minutes; the card stayed on step **1/6** the whole time (the session timer behind it kept counting 9s → 2m45s → 3m56s, so the app was live and repainting). Tapping **Next** did advance it, so the card is receiving touches and the ✕ specifically is inert. Until Next is pressed six times the user cannot reach the workout or leave the screen | **FIXED** wave 2 (2026-07-30) `679620e6` — the X was never mis-wired; its hit rect was 26×26 against a 44/48 minimum and every miss was swallowed by the card's own tap absorber. Target widened; the pill still looks 28pt |
| 103 | LOW | Workout | Quick Generate did not open while a workout was already generating | Two taps on the QUICK GENERATE row produced only a partial repaint while the hero card was mid-"Generating your workout… / Balancing your volume…". Plausibly intentional (don't start a second generation), but there is **no disabled state, no spinner on the row and no explanation** — it reads as an unresponsive control. Not retested after generation finished **RETRACTED / NOT A BUG — verified 2026-07-30.** Quick Generate opens and works normally once nothing else is generating, exactly as the original row suspected. Live run on the fresh binary produced **"Efficient Full Body Power Flow"** (Chest & Back, 15 min, 5 exercises, 84 cal, with AI insights + warm-up toggle) and it persisted: `scheduled_date 2026-07-30 17:00+00` (correct noon-CDT anchor). The earlier non-response was the concurrent generation, not a dead control. The underlying UX gap still stands and is worth keeping: while a generation is in flight the row gives **no disabled state, no spinner and no explanation**, so it reads as broken. | **NOT A BUG (control works)** — UX gap only |
| 104 | CRIT | Workout | **Switching gym profile orphans the day's workout and the app regenerates forever** — the "Generating your workout… / Picking your exercises…" card never resolves | Root-caused from device log + prod SQL while the spinner was on screen. A workout for 2026-07-29 **exists and is healthy**: `Quick Power Upper Body`, `scheduled_date 2026-07-29 17:00+00` (correct noon-CDT anchor), `is_completed=false`, `is_current=true`, `status='scheduled'` — but it carries `gym_profile_id = 4c45c749-…`, while the device log reads `🚀 [Auto-Gen] No workouts exist, triggering generation for date=2026-07-29 (profile=01197a6b-…)`. `/today` filters by the **active** gym profile (`_get_active_gym_profile_id`, cache key `user_id:gym_profile_id:date`), so after the active profile changed it cannot see the existing workout, returns `has_workout_today:false`, and auto-gen fires. The loop then self-sustains: generation reports `WorkoutGenerationStatus.completed — Workout ready!`, `/today` **still** returns false, auto-gen fires again and the server answers `⏳ Generation already in progress`. Distinct from #4 (that was `is_completed` in the dedup filter); this is profile-scoping. The user sees an indefinite spinner while a valid workout sits one profile away **VERIFIED FIXED live 2026-07-30.** Real commit is **`af3f28c1`** "a day's workout belongs to the day, not the gym profile". `today.py:1303-1345` and `:1325-1330` now call `db.list_workouts(...)` with **no** `gym_profile_id` kwarg (comments cite the day-ownership rule and this issue), and `core/db/workout_db.py:84-125` only applies a profile filter when one is passed — and even then as `.or_(gym_profile_id.eq.X, gym_profile_id.is.null)`, never a hard `.eq`. **Reproduced the proof fresh on live data:** this account still has 8 workouts orphaned under the non-active profile `4c45c749-…` (07-31→08-10); the OLD predicate (hard `gym_profile_id = active`) returns **0 rows** for 07-31, the CURRENT predicate returns **1** (`7e8cb7b6-… Upper Body Strength Foundation`). Live `/today` returned `next_workout_date 2026-07-31`, i.e. it sees the cross-profile workout. | **FIXED** `af3f28c1` — verified live |
| 105 | HIGH | Coach | **Root cause of #98 found — Save is not a silent no-op, it 500s on a broken DB trigger** | Render log, repeated on each Save tap: `module=saved_workouts` `Error in save_workout_from_workout: {'message': 'record "new" has no field "workout_id"', 'code': '42703'}` → `Internal error in saved_workouts`. A trigger (or function) on `saved_workouts` references `NEW.workout_id`, a column that does not exist on the row being inserted — the same phantom-column class CLAUDE.md documents, but in PL/pgSQL rather than a `.select()`. The client swallows the 500, which is why the button looked inert. Fixes #98's mechanism: the write is attempted and rejected by Postgres **VERIFIED FIXED live 2026-07-30.** Real commit is **`109465ad`** "make the workout card's actions actually do something", which shipped migration `2361_fix_phantom_new_field_trigger_functions.sql`. `saved_workouts` has no `workout_id` column at all; its only trigger `trigger_update_share_count` → `update_workout_share_count()` now reads `NEW.source_activity_id` guarded by a NOT NULL check (`pg_get_functiondef` on the live function). Every `42703` occurrence in Render is timestamped **2026-07-29 or earlier**, none after the fix landed. **Live decisive test:** `POST /saved-workouts/from-workout` → **HTTP 200** with a real row created, test row deleted afterwards. Migration 2361 fixed 6 sibling functions carrying the same phantom-`NEW`-field class (feature_votes, scheduled_workouts, screen_views, workout_challenges×3, challenge_participants) — not independently re-verified. | **FIXED** `109465ad` + mig 2361 — verified live |
| 106 | HIGH | Nutrition | **#10 has regressed on the menu-scan dish-image path — Imagen 404s again, endpoint returns 502** | Render log, during the menu scan run minutes ago: `[DishImage] generate failed for 'Wild Caught Shrimp Ceviche': Image generation failed: 404 NOT_FOUND` → `Response: 502`, repeated for `Steamed Asparagus` and `Blackened Florida Gulf Grouper`. The `us-central1` pin (`09569597`) evidently does not cover the `menu_analyses` dish-image caller, which is still resolving a location where the Imagen model does not exist. The scan itself succeeds (17 items parsed correctly), so this degrades to missing dish art rather than a failed scan — but it 502s per dish **VERIFIED FIXED live 2026-07-30.** Real commit is **`c01eef55`** "route every Imagen call through one region-owning helper". Root cause was bigger than a region pin: the production Vertex project has **no Imagen model in ANY region** (`us-central1`/`us-east4`/`europe-west4` all 404 for every `imagen-*`), so the fix switched the default to `gemini-2.5-flash-image` and made `dish_image_service.image_model()` defer to `image_generation_service.default_image_model()` instead of hardcoding `imagen-4.0-fast-generate-001`. All callers now funnel through the single `generate_image(purpose=…)` chokepoint. The four logged 404s are all pre-fix (07-28/29). **Live decisive test:** `POST /nutrition/dish-images/generate` → **HTTP 200**, `model: gemini-2.5-flash-image`, real signed S3 URL which fetched `200` as a 1.27 MB PNG. | **FIXED** `c01eef55` — verified live |
| 107 | MED | Nutrition / UX | **Thirteen affordances to log one meal** — the choice cost is high for "I ate eggs" | The log-meal sheet presents, simultaneously: 4 mode tabs (SEARCH / SNAP / DESCRIBE / VOICE), 5 action buttons (PHOTO / BARCODE / MENU / SCAN / COACH), and 4 source chips (Recent / Saved / Food DB / Quick log) — plus the text field and ANALYZE. Several overlap in outcome (SNAP vs PHOTO vs SCAN; DESCRIBE vs the search field), so the user must decide which of three routes to the same result they want before typing anything. Nothing is wrong individually; the density is the problem | **FIXED** `4d873f40` — verified 2026-07-30 — duplicate routes consolidated, the rest demoted, every capability still reachable |
| 108 | LOW | UX / global | **Truncation on identifying labels** — the thing you need to read is the part that gets cut | Gym name renders as **"COMMERC…"** in the Workout-tab header (the profile identifier — and the one thing distinguishing it from the other profile, which matters given #104). Coach quick-action chip reads **"Nutrition T…"**. Exercise title truncates to **"Calve Stretch From Wall…"** in Easy mode while Advanced shows it in full, so the same exercise is named two different ways in one session | **FIXED** wave 2 (2026-07-30) `679620e6` — the coach chip was not an ellipsis at all but a ShaderMask eating 18% of the strip; it now fades 12px of gutter. Easy and Advanced now cap the exercise title the same way |
| 109 | MED | Branding / Auth | **Sign-in screen shows the OLD logo** — and it is the app-icon tile, not a logo | Reported by the user, confirmed in code: `sign_in_screen.dart:533` renders `Image.asset('assets/images/app_icon.png')`, and that asset (**Jul 5 18:17**) is superseded by newer marks sitting uncommitted at the repo root — `zealova_z_icon_no_white_corners_1024.png` (**Jul 5 18:47**, its filename describing exactly the corner treatment being fixed), plus `new_logo.png` / `new_logo2.png` which show the current Z mark and ZEALOVA wordmark lockups. Two separate problems: (a) the asset is **stale**; (b) it is an **app-icon tile** — a dark rounded square with the background baked in — so on the dark sign-in screen it renders as a tile floating on a screen rather than a bare mark. App icons should not be reused as in-app logos; the sign-in header wants the transparent glyph or the wordmark lockup. First screen a new user sees, and the first impression of the brand. **Correction, wave 3 (2026-07-31), independently verified** — half of this row's own diagnosis was wrong: `assets/images/app_icon.png` is **md5-identical** to `zealova_z_icon_no_white_corners_1024.png`, so there was no newer unused asset sitting around; the "stale asset" half does not hold. The real defect was only the app-icon-tile-as-logo half. Fixed with a new alpha-keyed `zealova_z_mark.png` — independently verified RGBA 1024², corner alpha 0, centre alpha 255 (847,042 transparent / 198,897 opaque px), glass card dropped | **FIXED** wave 3 (2026-07-31) — logo-tile half fixed with an alpha-keyed mark, independently verified; "stale asset" half of the diagnosis retracted |
| 110 | MED | Workout | **Regenerating a "legs" workout can return an all-stretch session with no leg work** | Surfaced during injury verification on 2026-07-30 with `active_injuries=[]` (no injury to blame): `/regenerate` on a legs-type bodyweight workout returned a 5-exercise session named **"Sleepy Wander Range Session"** consisting entirely of stretches. Not an injury leak — the guard was not engaged — so this is a generation quality/completeness defect: the requested body focus was dropped and the output does not match the workout type it replaced. Not investigated further. **Fixed wave 3 (2026-07-31) — agent-reported, not independently re-run this wave** — regenerate never called `validate_and_filter_focus_mismatches` or `build_library_pool`, now wired in. Also fixed a second, independent cause: `_derive_focus_areas_from_exercises()` read `ex.get("target_muscles")`, a field library-sourced exercises never carry (they use `muscle_group`), so focus derivation silently found nothing on the common path | **FIXED** wave 3 (2026-07-31) — agent-reported, not independently re-run |
| 111 | HIGH | Workout | **The Quick Generate sheet ships literal placeholder copy — its header reads "Title" / "Subtitle"** | Captured on the fresh 07-30 binary: the Quick Generate bottom sheet renders the untouched placeholder strings **`Title`** and **`Subtitle`** where its heading and description belong, above a working Duration slider and Focus chips. Not a truncation or a localisation fallback — it is the scaffold text left in place. It is the first thing the user reads in one of the app's headline entry points, and it makes the feature look unfinished. Same sheet is also entirely **cyan**-accented (slider, "15 min", the primary button) against the app's orange chrome — see #15. **Correction, wave 3 (2026-07-31), independently verified** — no literal `'Title'`/`'Subtitle'` string exists anywhere in `lib/`; all 36 locales carry real translations. That half of this row duplicates **#22**, already fixed by `e10473d2` (an i18n extraction bug that has since been closed). This row's real, distinct half — the sheet renders entirely cyan against the app's orange chrome — is closed by the wave-3 accent sweep (see #15) | **FIXED** wave 3 (2026-07-31) — duplicate of #22 (already fixed) + accent half closed by the #15 sweep, independently verified |
| 112 | MED | Backend | **`workout_modifier.py`'s three public methods write a phantom `exercises` column — currently unreachable, but armed** | Found while verifying #55. `services/workout_modifier.py` `add_exercises_to_workout` (L91), `remove_exercises_from_workout` (L171) and `modify_workout_intensity` (L273) all build `update_data = {"exercises": exercises, …}` and call `db.update_workout(...)`, which only special-cases `estimated_duration_minutes` — everything else re-raises the 42703 phantom-column error. Both callers are currently broken by *other* mechanisms, which is the only reason this is not live: `coach_service.py` is dead (`main.py` instantiates only `LangGraphCoachService`), and `api/v1/workouts/quick_adjust.py` (`POST /workouts/{workout_id}/quick-adjust`) is mounted but declares `workout_id: int` while every real `workouts.id` is a UUID — confirmed live, it **422 `int_parsing`** before ever reaching the modifier. Worse, that endpoint ignores the methods' boolean return, so once the UUID bug is fixed it would report `exercises_removed`/`applied_ease` as successful while the write silently 42703'd. Two independent bugs are the only thing containing this; fixing either one arms it. Running `scripts/audit_supabase_column_drift.py --check` currently reports **17 NEW phantom-column findings**. **Fixed 2026-07-30, both halves together** (fixing either alone arms the other): `quick_adjust.py` now declares `workout_id: str`; the Flutter side stopped coercing (`quick_adjust_sheet.dart` takes `String workoutId`, `active_workout_screen_refactored.dart:2442` dropped `int.tryParse(id) ?? 0` which was sending `/workouts/0/quick-adjust`); all three modifier methods now write `exercises_json` and read `workout.get("exercises_json")`; each checks `update_workout`'s return instead of discarding it; and `quick_adjust.py` propagates the removal failure rather than reporting success. Two more instances of the same phantom-column class were found and fixed by the audit: `workouts_db.py:201` (`workouts.exercises`) and `data_import.py:350` (`workout_logs.workout_name`, which made EVERY imported workout log fail silently). Gate is now green | **FIXED** 2026-07-30 |
| 113 | — | Programs | **PASS — Program Start now completes end to end** (it had never once finished in any prior session) | Driven on the 07-30 build: Workout → PROGRAMS → Browse → "Gentle Start" (8wk / 56 workouts) → START PROGRAM → overlap step (Add-alongside / Replace both fire live `assign-preview`/`assign-review`, confirmed in Render) → CONFIRM & START → "Gentle Start is ready — 56 workouts on your calendar". **Verified in prod SQL:** `user_program_assignments` `99d41219-…` `status=active, is_active=true, total_workouts=56`, and **56 `workouts` rows expanded against that assignment_id spanning 2026-07-30 → 2026-09-23** (independently re-run by me, not just reported). Schedule screen shows DAY 1 Active Recovery / DAY 2 Strength Focus / DAY 3 Movement Flow. **Injury gating confirmed at assign time**: the success dialog read *"Swapped 8 for your knees"* on this `active_injuries=["knees"]` account — corroborating #90's correction that the default Start path does adapt | **PASS — verified live** |
| 114 | HIGH | Coach / Workout | **The app has no handler for the injury-safety 409 — the guard works but says nothing useful** | The backend now correctly blocks a contraindicated swap/add with a structured **HTTP 409 `EXERCISE_UNSAFE_FOR_INJURY`** carrying a human-readable message (`workout_operations.py` `injury_block_response()`), proven live twice by the API pass (Air Squat under `["knees"]`, Leg Extension under `["quads"]`). But **`grep -rn "EXERCISE_UNSAFE_FOR_INJURY" mobile/flutter/lib/` returns 0 matches** — independently confirmed this session. Nothing in the client reads the code, so the response falls through to a generic Dio error toast: the user is stopped without being told *why*, and cannot tell a safety block from a network failure. The most defensible feature in the app is currently invisible at the point it fires. Fix is small — map the code to real copy naming the joint and offering a safe alternative **Exact discard points confirmed 2026-07-30.** `lib/data/repositories/workout_repository_exercises.dart` contains **zero occurrences of `409`** in the whole file, while carrying typed branches for every other failure: 404 `PREVIEW_EXPIRED` (`:384`), 403 `PREVIEW_NOT_OWNED` (`:387`), 429 (`:391`). `swapExercise()` therefore falls through to a generic `return (null, 'Failed to swap exercise. Please try again.')` (`:397,:400`), discarding the backend's real message ("X isn't safe to program around your knees right now…"). `addExercise()` is worse — its catch-all returns a bare `null` with **no message at all**, so the caller (`exercise_add_sheet…_state.dart:320-327`) shows a flat 'Failed to add exercise'. **Downgraded from the CRIT scenario I was worried about:** the backend read confirms `injury_block_response()` has **no override/bypass parameter**, so there is no 'Add anyway' path writing a contraindicated exercise — the guard genuinely holds. This is a trust/UX gap, not a safety hole: the block is real but indistinguishable from a network failure. Code-verified; the SnackBar text was not screenshotted on device. **Fixed wave 3 (2026-07-31) — agent-reported, not independently re-run this wave** — the client now has a typed branch for the 409 `EXERCISE_UNSAFE_FOR_INJURY` code and surfaces the backend's real message instead of falling through to a generic error. **Correction, wave 3 (2026-07-31), file/line-verified** — both the row's own framing and the agent fix above were incomplete. The actual root cause is bigger than "no 409 branch": **FastAPI wraps every error body under `detail`**, so the client's typed-error branches were reading a top-level `data['error_code']` that never existed — the real code lives at `data['detail']['error']`. That means the two branches this row itself cited as evidence that "everything else has a typed branch" — 404 `PREVIEW_EXPIRED` (`:384`) and 403 `PREVIEW_NOT_OWNED` (`:387`) — were themselves dead code that had never fired either; the row's framing was wrong on that point. All three (`PreviewExpiredException`, `PreviewNotOwnedException`, and the 409 injury-safety handler) are now revived and wired at 6 call sites (`exercise_swap_sheet_part…` ×2, `exercise_add_sheet_part…` ×2, `equipment_snap_flow.dart` ×2). Negative-tested: reverting to `body['error_code']` reproduced 3 failures, including the revived PREVIEW_EXPIRED case | **FIXED** wave 3 (2026-07-31) — root cause corrected and widened (FastAPI `detail` envelope, not just a missing 409 branch); all three typed error branches revived, negative-tested |
| 115 | MED | Gamification | **Level-Up dialog renders a Flutter RenderFlex overflow banner, and its card claims "+0 XP EARNED"** | Triggered unprompted as a rewards side-effect of assigning a program. The "Bronze Fitness Crate" card inside `_AccomplishmentCarousel` (`widgets/level_up_dialog_part_accomplishment.dart`) shows Flutter's own on-screen **"BOTTOM OVERFLOWED BY 20 PIXELS"** debug banner — a genuine layout overflow, screenshotted directly, not a harness artefact. Same card reads **"+0 XP EARNED"** while the toast beneath says *"+100 XP and a reward crate"* and the level bar shows 355 XP, so per-accomplishment `xpEarned` plumbing appears not to be populated from the same source as the aggregate. Two defects on the app's celebration moment — the screen most meant to feel polished | **FIXED** wave 3 (2026-07-31) — agent-reported, verified in code by the lead: `xpEarned` is now threaded from a real delta at both construction sites in `xp_provider_part_x_p_notifier.dart` (it defaulted to 0 and BOTH sites omitted it, which is why every card read "+0 XP EARNED"), and the crate card's Column is wrapped in a `SingleChildScrollView` so the `isCrate` variant's extra ~50px no longer overflows the `Expanded` that sizes all cards alike |
| 116 | MED | Workout | **The gym name is STILL truncated to "COMMER…" — a fix for this exact string exists but is not on the path the tab renders** | User screenshot, 07-30 16:49, on the current build (same session in which Gentle Start was assigned, so definitely post-fix). The Workout-tab masthead shows **"COMMER…"** in large display type with a chevron. This should size to the name, not clip it. What makes it a live gap rather than a known-open item: `workouts_screen.dart:1046` `_EquipmentProfilePill` carries the comment *"…and 'COMMERC…' was the original complaint"* and renders the gym name in a `Flexible` small pill beside a literal `'WORKOUTS'` masthead (`:250`) — i.e. the fix for #32/#108 deliberately demoted the gym name out of the masthead. The screenshot still shows the OLD large-masthead treatment, so **a second render site exists on the route the Workout tab actually uses**, and the fix only covered one of them. Find the other call site rather than re-fixing the pill. **Correction, wave 3 (2026-07-31), independently verified** — the second render site does not exist: `grep GymProfileSwitcher(` returns exactly one call site, passing no `large:` argument. The `large: true` branch in `gym_profile_switcher.dart` had **zero callers** — dead code, now deleted. There is no second truncation site to find; the #32/#108 pill fix was already complete, and the screenshot this row cites was most likely taken against a stale/cached binary | **RETRACTED — not a live bug** — wave 3 (2026-07-31); dead `large:` branch removed as cleanup |
| 117 | MED | Gamification / global | **The "Get Started Challenge complete" toast renders on the WRONG TAB, mid-screen, over unrelated content** | User screenshot: the snackbar *"🎉 Get Started Challenge complete! +100 XP and a reward crate"* is floating across the middle of the **Workout** tab, overlapping the program cards row and colliding with the `+` FAB — not docked at the bottom, and not on the screen that raised it. Source is `setup_checklist_card.dart:236-245`, a **Home-tab** widget calling `ScaffoldMessenger.of(context).showSnackBar(...)`. This is the documented IndexedStack hazard already noted elsewhere in this codebase (`program_library_screen.dart` comments it: all five tabs stay alive, so a root-messenger SnackBar renders against the wrong/multiple live Scaffolds — the same mechanism that previously caused duplicate-Hero-tag crashes and permanently stuck toasts). No `duration` is set either, so it takes the 4 s default with no explicit dismissal. Fix: raise it on the Scaffold that owns the Home tab, or route it through the app's existing single-toast host | **FIXED** wave 3 (2026-07-31) — routed through the existing `rootSnackBar` / `rootScaffoldMessengerKey` single-toast host (already wired at `app.dart`, only 3 consumers before) with an explicit duration; 55 further `ScaffoldMessenger.of(context)` call sites across 15 tab-resident Home widgets converted in the same pass. Modal sheets deliberately keep their local messenger — the user's attention is already there |
| 118 | HIGH | Gamification / routing | **The XP / reward destination does not exist — `/xp` 404s, and three separate places push to it** | Screenshotted live: tapping **OPEN** on the "Get Started Challenge complete! +100 XP and a reward crate" toast lands on a full-screen error, **"Page not found: /xp"** with a Go Home button. Verified in code: `lib/navigation/app_router.dart` registers **no `/xp` route** (no `path: '/xp'`, and no xp/reward/crate/level route at all), while **three call sites push to it** — `setup_checklist_card.dart:242` (the toast's only action), `setup_checklist_card.dart:546`, and `stacked_banner_panel.dart:418` (a Home banner). So the reward the app just told the user they earned is unreachable from every entrance. This lands on the **first-run** path — the user completes onboarding's Get Started Challenge, is told they earned 100 XP and a crate, taps the one CTA offered, and hits a 404. Compounded by #117 (that same toast renders on the wrong tab) and #115 (the level-up card shows "+0 XP EARNED" and overflows), the entire reward moment is broken end to end. **Fixed wave 3 (2026-07-31), independently verified** — `/xp` no longer 404s; live gate output confirms the route resolves and a new `test/navigation/route_existence_test.dart` gate FAILS if it is reintroduced. That gate, run across the whole app, also caught 4 more live dead routes beyond this one (see the wave-3 "new defects" section) — `/xp` was the first instance of a wider class, not an isolated bug | **FIXED** wave 3 (2026-07-31) — independently verified; gate found 4 more live dead routes |
| 119 | CRIT | Scores | **`POST /scores/readiness` 500s on EVERY call — and the check-in has already been saved by then**, so the user is told it failed while it succeeded | Live repro: `POST /scores/readiness {sleep_quality:3,fatigue_level:3,stress_level:2,muscle_soreness:3}` → **500**, while SQL shows a real `readiness_scores` row landed (`readiness_score:71`, all 4 sliders correct). Unconditional, not data-dependent. **Root cause, corrected from the initial report:** `api/v1/scores.py:617` passes the bare name `generate_ai_readiness_insight` to `background_tasks.add_task`. The function is NOT missing — it exists at `scores_endpoints.py:1003` — but `scores.py:24` imports only `router as _endpoints_router` from that module, so the name is unbound at call time → `NameError`. `scores.py` is mounted (`api/v1/__init__.py:32`), so this is the live route. **Fix is one line**: add `generate_ai_readiness_insight` to the existing import. Same told-you-it-failed-while-it-saved class as #98/#105. **Fixed 2026-07-30**: `scores.py:24` now imports the symbol. Added `backend/tests/test_background_task_symbols_bound.py`, an AST gate that resolves every `background_tasks.add_task(NAME, …)` target across `api/v1/` against module + enclosing-function scope (including `from X import *`, resolved via X's own AST). Verified it FAILS on the pre-fix code and passes after — this class is invisible to import-time checks because the module imports cleanly and only 500s at request time | **FIXED** 2026-07-30 |
| 120 | HIGH | Data import | **`POST /users/{user_id}/import` reports success while silently dropping EVERY workout log** | `services/data_import.py:361` builds `data["workout_name"]` and calls `db.create_workout_log(data)`, but `workout_logs` has **no `workout_name` column** — independently confirmed against the schema snapshot (no name-bearing column exists on that table at all). The per-log `try/except` at `:340-367` catches the resulting 42703 per row and only logs a warning, so the import completes "successfully" having written zero workout logs. Route is mounted (`api/v1/users/__init__.py:68`). This is the GDPR-style restore path — the one flow whose entire purpose is not losing the user's history. **Fixed wave 3 (2026-07-31), independently verified** — the root cause was already closed by #112's phantom-column fix (`workout_name` now written into the `metadata` jsonb rather than a nonexistent column), but this row itself had never been closed out; doing so here. Residual gaps also closed this wave: `status`, `duration_minutes`, `exercises_completed`, and per-log failure counts are now populated on import instead of silently dropped | **FIXED** wave 3 (2026-07-31) — independently verified |
| 121 | MED | Programs / tz | **3 NEW timezone-gate findings in the program reschedule/anchor logic** | `audit_timezone_usage.py --check` (baseline-diff, 251 grandfathered) flags 3 fresh: `api/v1/program_templates.py:4334` (`str(started_at)[:10]` — UTC-date bucketing on the assignment anchor), `:4352`, and `services/program_template_expander.py:1133` (`today = today or date.today()` on a UTC server). All three are in `afce2a7c`'s new assignment-progress code. For a user west of UTC late in the evening these misclassify which future workouts count as "not yet started" during a reschedule — the exact documented class in CLAUDE.md. Flagged by gate, not live-reproduced against off-UTC data. **Fixed wave 3 (2026-07-31), independently verified** — `audit_timezone_usage.py --check` is now clean (was 3 NEW findings). Note: 15 baseline entries are now stale (grandfathered findings that no longer exist in code) and should be pruned with `--refresh-baseline` so the gate doesn't silently tolerate their return | **FIXED** wave 3 (2026-07-31) — independently verified, gate clean |
| 122 | HIGH | Exercise media | **Program exercise renders no image because its authored name matches nothing in the library — while the correct illustration exists on S3 under exactly that name** | User screenshot of Gentle Start "Active Recovery": **"Bodyweight standing calf raise"** shows the grey dumbbell placeholder, while "Bird dog" directly beneath renders its real illustration. Root-caused in SQL: `exercise_library` has **no row named "Bodyweight standing calf raise"**. It has `Standing Calf Raise`, and that row's `image_s3_path` is literally `s3://ai-fitness-coach/ILLUSTRATIONS ALL/Legs/Bodyweight Standing Calf Raise.jpg`. So the art is present and correctly named — the **program's authored exercise name resolves to nothing**, and the name-based lookup falls through to the placeholder. "Bird dog" matches an exact `exercise_library` row, which is why it works. This is the documented 4-id-space name-drift class, now visibly leaking into a shipped program. Note `audit_exercise_media_urls.py --check` passes (all 4,956 S3 paths are alive) — it verifies paths that ARE referenced, so it structurally cannot catch a name that never resolves to a row. Fix: alias the authored name onto `Standing Calf Raise`, and consider a gate asserting every `program_variant_weeks` exercise name resolves **Blast radius measured 2026-07-30 — this is not one exercise.** Replicating the app's real 3-tier resolver (position-matched `program_exercises_with_media` → name fallback → by-id `exercise_id` fallback) across 45 published programs / 1,624 variants / 177,318 exercise occurrences: **20,332 occurrences — 11.5% of every exercise slot in the catalogue — serve NO image**, spanning **263 distinct names**, **31 of 45 programs (69%)** and **994 of 1,624 variants (61%)**. (26,408 fail the name chain; 6,076 are rescued by the by-id fallback.) The reported calf raise is the single biggest offender at **2,207 occurrences across 18 programs**. Top offenders after it: Normal Push-up (1,946), Side Push-Up (1,755), Bridge–Mountain Climber (1,287), Bodyweight forward lunge (1,127), Alternate Leg Raise (806). **11 of the top 13 by volume are pure self-alias fixes on exact-name matches** — see #127, which is the root cause and the cheap fix. Confirms `audit_exercise_media_urls.py` structurally cannot catch this: it HEAD-checks paths that ARE referenced and never asks whether an authored name finds a row at all — a different failure mode needing its own gate (`audit_program_exercise_media_resolution.py`, baseline-diff, asserting resolution at the layer the app actually serves from, and checking BOTH `name` and `exercise_name` per #126). **Fixed wave 3 (2026-07-31), independently verified — both root causes (#126, #127) closed.** Gate re-run live: missing images down to **1,039** (was 20,332), **73** distinct unresolved names (was 263), **14** programs still affected (was 31) — a >94% reduction, not a full zero. All 5 top offenders (calf raise, push-up, side push-up, bridge-mountain climber, forward lunge) now resolve with real images. `program_exercises_flat`'s DDL, which had drifted from prod, was recovered from production into migration 2395 as part of this fix | **FIXED (94% closed, not 100%)** wave 3 (2026-07-31) — independently verified: 1,039 / 73 names / 14 programs remain, down from 20,332 / 263 / 31 |
| 123 | MED | Workout detail | **The coach avatar + LET'S GO button float over the exercise list and obscure the first exercise's own data** | Same screenshot: the circular coach avatar and the orange **LET'S GO** pill sit on top of the first exercise row, covering its set/rep line — "2 sets · 8-…" is clipped mid-value by the avatar, so the one row the user reads first is the one they cannot read. Neighbouring polish issues on the same screen: the title pill truncates to **"Active Rec…"** despite ample width, and the **DIFFICULTY: MODERATE** chip is clipped by the right edge. The screen is otherwise strong (real duration/exercise/energy stats, AI insights, warm-up toggle) — it is the floating layer that needs a safe-area inset or a bottom pad on the list so content can scroll clear of it. **Fixed wave 3 (2026-07-31) — agent-reported, not independently re-run this wave, mechanism corrected** — the floating-avatar/LET'S GO-pill docking half described in this row was **already fixed by a prior commit (E2E #51)**, verified via git log — not redone this wave, so this row should not be credited for that half twice. What actually shipped this wave: the new shared `FadingChipRow` adopted for the meta-chip row in `workout_detail_screen.dart` (was a bare horizontal `SingleChildScrollView`, closing the "DIFFICULTY: MODERATE" clipping), and the title's `maxLines` widened 2→3 (closing the "Active Rec…" truncation) | **FIXED** wave 3 (2026-07-31) — agent-reported, not independently re-run; floating-layer half was already fixed pre-wave-3 (#51) |
| 124 | LOW-MED | Notifications | **The push-nudge cron runs hourly but checks 0 users — the engine is inert platform-wide** | GH Actions `engagement-cron.yml` is genuinely firing (15 recent runs, all `completed/success`, ~2-hourly), but every recent response body is identical: `{"jobs_run":[],"nudges_sent":0,"users_checked":0}` (sampled 07-30 20:20 / 18:24 / 16:42 and 07-29 12:09). `push_nudge_cron.py:4384-4386` early-returns when `_fetch_nudge_eligible_users` (`:505-522`, filters `users.fcm_token IS NOT NULL`) is empty, and SQL confirms **0 of 13 production users have an `fcm_token`**. `push_nudge_log` holds 3 rows ever, all 2026-07-04/05 — 25 days stale. **Not asserted as a regression:** 13 all-QA users and simulators that cannot obtain real APNs/FCM tokens make "nobody has registered yet" a plausible pre-launch explanation, and no defect was found in the registration endpoint. Needs a product call, not a QA verdict. One genuine positive: of the 3 historical rows, the `rest_day_tip` one carries a real `chat_message_id` → session-attached `chat_history` row, proving `_mirror_proactive_to_chat` works when a nudge does fire (the 2 `daily_crate` rows have null — possibly intentionally push-only). **Re-checked wave 3 (2026-07-31), independently verified — same conclusion holds, not a defect.** Prod still shows 13 users, 0 with `fcm_token`; this remains a pre-launch state rather than a bug. No code change made or needed | **CONFIRMED — decision, not a defect** (re-verified wave 3, 2026-07-31) |
| 125 | MED | Warm-up | **The warm-up is completely non-editable — no add, no swap, no rest control, and no way to keep a change** (user-requested capability) | Confirmed in code, not just on screen: `warmup_phase_screen.dart` is 830 lines and its ONLY interactive elements are quit (`:308`), timer play/pause (`:775`), next-exercise (`:808`) and an interval-change log (`:542`) — **the four warm-up rows are not tappable at all**, so there is no entry point to add, swap or remove a move. `warmup_controller.dart:32-33` reads a fixed `duration` per exercise and has **no rest concept between moves**. Nothing in `lib/data/repositories/` persists a customised warm-up, so even an in-session change could not survive to the next run. Three distinct asks: (a) add / swap / remove a warm-up move, (b) set rest between moves, and ideally per-move duration (every move here is a flat 30 s), (c) **save the customised warm-up for future runs**. Note the asymmetry: the main workout already has add/swap with a real injury guard behind it (#114), while the warm-up — the part users most often want to trim or personalise — has none of it. Also the clearest #15 evidence yet: an orange WARM-UP label sits directly above cyan sliders and a cyan Next button on one screen, matching the hardcoded `AppColors.cyan` in `pre_workout_reshape_gate.dart`. **In progress, wave 3 (2026-07-31)** — a feature build is underway: migration 2401 (`user_warmup_templates` table) applied, a new backend endpoint and Flutter repository methods for add/swap/remove and rest-between-moves are in progress, along with persistence of a customised warm-up on finish. Not complete at time of writing. **Correction, 2026-08-01 — that "in progress" note was already stale when written; independently re-verified at code level (spot-checked by the lead, not taken on report).** The feature landed in commit `501ad001` (2026-07-31 03:45), three minutes after the shareables commit the earlier pass had analysed against. In current HEAD: migration 2401 (`user_warmup_templates`) applied and the backend router registered (`warmup_templates.py` — `GET/PUT /workouts/warmup-template`, `POST /workouts/{id}/warmup/apply-template`); the client repository methods are **called, not merely defined** — `warmup_phase_screen.dart:287` → `saveWarmupTemplate`, `easy_active_workout_state.dart:339,473` → `applyWarmupTemplate` / `saveWarmupTemplate` (checked specifically for the row-#41 implemented-but-uncalled shape; it is not that shape). The uncancelled `Timer.periodic` this row called out is fixed: `easy_active_workout_state.dart:853`, `dispose()` now calls `_warmupRestTimer?.cancel()` — verified directly, alongside the two sibling cancels at `:665` and `:688`. **Caveat: this is code-level only** — no end-to-end UX run of add/swap/rest/save on device, so "wired and complete" is the claim, not "bug-free". The accent half of this row is tracked under #15 (closed) | **FIXED (code-verified) 2026-08-01** — `501ad001`; on-device UX pass still outstanding |
| 126 | HIGH | Exercise media / resolver | **The media resolver reads the wrong name field — it normalises only `name` and ignores `exercise_name`, which is the one that actually resolves** | Each `program_variant_weeks.workouts[].exercises[]` row carries BOTH `name` and `exercise_name`, and they **disagree on 11,899 of 177,318 occurrences**. The view feeding resolution (`program_exercises_flat` → `program_exercises_with_media`) normalises **only `name`**. Measured on exactly those 11,899 disagreeing rows: `exercise_name` resolves via alias for **100% (11,899/11,899)** while `name` resolves for only **50.6% (6,018)** — i.e. `exercise_name` is the library-matched field and `name` is a display field that drifted. **5,214 of the 20,332 missing-image occurrences (25.6%) would resolve today, with no data change at all, if the view also checked `exercise_name`.** This is a live resolver bug, not a content gap — fix the view first, then let the new gate assert it doesn't regress. **Fixed wave 3 (2026-07-31), independently verified — see #122 for the combined gate numbers (1,039 missing, down from 20,332).** The resolver now checks `exercise_name` as well as `name` | **FIXED** wave 3 (2026-07-31) — independently verified, see #122 |
| 127 | HIGH | Exercise data integrity | **84% of canonical exercises have no alias for their OWN name — so authoring an exercise by its exact canonical name resolves to nothing** | Independently verified in prod SQL: **1,747 of 2,070 `exercise_canonical` rows (84.4%) have no `exercise_aliases` row matching their own `canonical_name`**. The reported calf raise is the perfect illustration — `exercise_canonical` holds a row named exactly `"Bodyweight standing calf raise"` with a real `exercise_demos` row pointing at the correct S3 art, yet its only two aliases are *"Bodyweight Calf Raise"* and *"Standing Calf Raise (Machine)"*, neither of which is its own name. So a program that spells the exercise **exactly right** still fails to resolve. **174 of the 263 missing names (66%) are exact-normalised canonical matches that already have media** — zero new art, zero fuzzy matching, no program-content edits required. The repair is one deterministic backfill (`INSERT INTO exercise_aliases … SELECT canonical_name, canonical_name_normalized, id FROM exercise_canonical WHERE NOT EXISTS (self-alias) ON CONFLICT DO NOTHING`), and it protects every future generator run that authors by canonical name — which is evidently common (calf raise, push-up, forward lunge, full squat all did). Deserves its own cheap standing gate: *every canonical row has a self-alias*. **Highest-leverage, lowest-risk fix in the register**. **Fixed wave 3 (2026-07-31), independently verified — see #122 for the combined gate numbers.** The self-alias backfill ran, and `program_exercises_flat`'s DDL was recovered from prod into migration 2395 in the same pass | **FIXED** wave 3 (2026-07-31) — independently verified, see #122 |
| 128 | CRIT | Nutrition | **Analyze with NO photo and NO description fabricates a meal and AUTO-LOGS it** — no confirmation step | Reproduced live 2026-07-30 on the DESCRIBE tab with an empty form (zero photos, zero description text). Tapping **Analyze** returned *"Chicken Breast (Grilled), 6oz, No Sauce"* and immediately showed **"Meal Logged!"** — it wrote straight to the database with no review or confirm. Verified in prod SQL: `food_logs` row `ad03320f-…`, `meal_type dinner`, **198 kcal, 37 g protein, 0 c, 4 f**, `logged_at 2026-07-30 22:57:46+00`, `food_items = ["Chicken Breast (Grilled)","6oz","No Sauce"]`. This is the #3 class (blank input producing a confident fabricated dish) but materially worse: #3 merely *displayed* an invented dish awaiting a tap, whereas this **persists it unprompted**, corrupting the user's nutrition history with food they never ate and never confirmed. The empty state directly beneath the button even reads *"Add a photo or describe the meal to analyze"* — so the UI knows the input is empty and runs anyway. Should hard-block on empty input, and in no case auto-log an AI result without an explicit confirm. Test row deleted after capture **RETRACTED 2026-07-30 — this was MY error, not an app defect.** A concurrent agent had pasted *"Grilled chicken breast, 6oz, no sauce"* into the DESCRIBE field moments earlier; I dismissed a paste dialog, mis-tapped Analyze believing the form was empty, and read the result as a fabrication. Decisive evidence it was not: the logged `food_items` are literally the comma-separated parts of that pasted string — `["Chicken Breast (Grilled)", "6oz", "No Sauce"]` — and the row id I cited (`ad03320f-…`) is the **same row** the agent independently reported creating from that paste, with matching macros (198 kcal / 37 g protein, sane for 6 oz chicken). The app analysed real input correctly. **No evidence exists that Analyze fabricates or auto-logs on empty input**; that behaviour was never actually tested. Kept in the register rather than deleted, so the retraction is on record. | **RETRACTED — tester error, not a defect** |
| 129 | — | Nutrition | **PASS — barcode scan resolves a real product correctly** | Generated a valid EAN-13 for **5449000000996** (Coca-Cola 330 ml) and loaded it into the simulator photo library. The scan resolved to the genuine product and logged `food_logs` row `0c954046-…`: item **"coca-cola"**, **138 kcal, 35 g carbs, 0 g protein, 0 g fat** — textbook for a 330 ml Coke, so this is a real catalogue hit, not a fabrication. Barcode path works end to end | **PASS — verified live** |
| 130 | HIGH | Nutrition | **Photo scan of a food spread logs WHOLE-ITEM portions — one dinner recorded as 4,591 cal / 601 g protein — and the biggest item carries no weight at all** | Live row `a215b6e3-…`: `total_calories 4591`, `protein_g 601`, 6 items. Worst offender **"roasted whole turkey" — 2,505 cal / 435 g protein**, i.e. ~1.4-1.5 kg of turkey meat. The per-item arithmetic is internally consistent (2505 kcal ↔ 435 g protein is right for that mass), so this is **not** a maths error — the model read a *display spread* of foods and assigned whole-item portions rather than plausible servings. Same across the meal: "whole red snapper" 450 g, "whole wheat bread" 200 g. 601 g protein in a single meal is roughly **8× a large adult's entire daily intake** — no plausibility ceiling is applied anywhere. **The concrete defect under it:** the turkey item has **`weight_g: null`** while its siblings carry real weights (snapper 450, bread 200). So the single largest contributor — 55% of the meal's calories — has no weight, cannot be adjusted with the per-item gram stepper, and gives any downstream sanity check nothing to work with. Two fixes: require a weight per item (or mark `macros_unknown`/`requires_user_confirmation`, fields that exist in the payload but are null here), and apply a per-meal plausibility ceiling that prompts instead of silently logging. **Fixed wave 3 (2026-07-31), independently verified.** `weight_g` is now REQUIRED on both `FoodItemSchema` AND `MenuDishSchema` — the latter is a correction to this row's own diagnosis: it was optional AND unbounded, and is the more likely real path for a photographed food spread than the `FoodItemSchema` field originally named. A per-meal plausibility ceiling (3,000 kcal / 250 g) is wired into `enforce_macro_integrity`; it flags for review rather than silently clamping. 7/7 tests | **FIXED** wave 3 (2026-07-31) — independently verified, 7/7 tests |
| 131 | MED | Coach / Home | **The Home coach card says "You have logged 0g of protein today" while 601 g is logged** | Observed 2026-07-30 18:09. The card reads *"Fuel your strength goals, Jordan. You have logged 0g of protein today…"* and recommends Greek yogurt to fix it — but `food_logs` holds a meal from **18:01 the same day** with `protein_g = 601` (plus a 138 kcal cola). Eight minutes stale at most, and inverted: the card is coaching the user toward the exact macro they have massively over-consumed. Likely the documented `coach_daily_insights` caching (CLAUDE.md notes cached bodies persist and regenerate lazily) — but the user cannot tell a stale card from a wrong one, and here it produces actively bad advice. Should invalidate on food-log write, or render from live totals. **Fixed wave 3 (2026-07-31)** — client now busts the coach-card cache on every food-log write. A residual bug was found and **fixed directly during verification, not by an agent**: the coach TEXT was regenerated only on the FIRST meal of the day, so this row's own repro sequence (cola logged first, then the 601 g meal) took the numbers-only refresh path and never got new text. Text now regenerates on every write, matching the numeric refresh | **FIXED** wave 3 (2026-07-31) — independently verified, including a residual instance found and fixed directly |
| 132 | HIGH | Coach / Home | **The Evening Recap card's chart has nothing to do with its headline** — a steps message rendered above a protein chart | Card reads **"Stack a few more steps — Jordan, a ten minute walk now puts the Move ring in reach"**, and the chart directly beneath it is titled **"Protein · last 3 logged days"**. The copy and the visualisation are about different metrics entirely, so the card argues for a walk while showing protein bars. Three compounding problems in one card: (a) headline/chart mismatch; (b) the y-axis label renders as a **wrapped, garbled `334.5` / `8`** (334.58 broken across two lines) — and an unrounded 2-decimal protein axis is meaningless to a user anyway; (c) it invokes the **"Move ring"** for an account with **no Health integration connected** (Home still shows the "Connect Health to see steps, sleep & readiness" banner), so the goal it references cannot exist. Sits alongside #131, where the same coach surface claimed 0 g protein against 601 g logged — this card family is generating copy and data independently and not reconciling them. **Fixed wave 3 (2026-07-31), independently verified**, all three sub-problems: (a) headline/chart mismatch closed via `strict=True` reconciliation in `build_briefing_blocks`; (b) the garbled `334.5`/`8` axis label fixed by extracting the axis math into Heckbert 1/2/5×10ⁿ rounding with a `TextPainter`-measured reserve — a negative test reproduces the exact original literal (`334.58333333333337`) and confirms it's rounded away; (c) the "Move ring" reference is now gated on the account ever having synced `daily_activity`, so it can't invoke a ring that doesn't exist for an unconnected account | **FIXED** wave 3 (2026-07-31) — independently verified, all three sub-issues |
| 133 | CRIT | Workout logging | **Easy-mode sets record `reps_completed = 0` — the workout logs zero actual work** | Live on the 07-30 binary: started Active Recovery, logged both sets of **Bird dog** (target BW × 5). The set pills render **"1 BW×0 ✓"** and **"2 BW×0 ✓"**, and prod SQL confirms the DB agrees — `performance_logs` rows at 23:24:51 and 23:25:26 both carry **`reps_completed = 0`** with **`target_reps = 5`**, `weight_kg = 0`, `is_completed = true`, `set_duration_seconds = 5`. So a completed set is persisted claiming the user did **zero reps**. Session **Volume stayed pinned at 666 kg** across both logs, consistent with 0-rep sets contributing nothing. This silently destroys the training record: volume, progression and strength scores all derive from `reps_completed`. Note the same table shows a *correct* prior row ("Bodyweight standing calf raise", `reps_completed 11 / target 11`), so the defect is specific to this path — likely the duration/timer-based set (a 5-second hold) never mapping its completion back to a rep count **SCOPE CORRECTED (downgraded CRIT→HIGH) 2026-07-30 — my original framing overstated this.** Rep-based logging is **fine**: the same `performance_logs` table shows correct rows from this session — *Bodyweight standing calf raise* `10/10` and `11/11`, *Single-Leg Hamstring Stretch* `10/10`. The defect is narrower: **Bird dog carried `target_reps = 5` but Easy mode presented it as a 5-SECOND TIMER**, and the completed set stored `reps_completed = 0, set_duration_seconds = 5`. So this is a **metric-type mismatch** — an exercise targeted in reps rendered as a duration hold — not blanket data loss. Consequences still real: the set pill reads **"BW×0"**, session Volume stays pinned (666 kg across both sets), and anything deriving from `reps_completed` sees zero for that exercise. Related to the documented `exercise_tracking_metric` / `sets_json` format-drift area. Correct fix is to make the tracked metric authoritative for both display and write, so a timed set records duration and is not scored as 0 reps. **Fixed wave 3 (2026-07-31) — agent-reported, not independently re-run this wave** — client mirror of the backend precedence rule: in `exercise_tracking_metric.dart` `resolve()`, `hasRealReps = (targetReps != null && targetReps >= 2) \|\| _hasRealRepCount(...)`, and `!hasRealReps` now gates `isTimed`/`holdSeconds`/`durationSeconds` together (an explicit duration string like "30s" is deliberately ungated, in a separate branch). Also fixed `seedEasyExerciseStates`, whose `timed = metric.isTime \|\| ex.isTimedExercise` bypassed the classifier entirely, and `isPlaceholder` (missing `durationSeconds`, the #75 class) | **FIXED** wave 3 (2026-07-31) — agent-reported, not independently re-run |
| 134 | MED | Workout | **The active-workout timer block is visually broken — "seconds" is clipped mid-word and the rest bar overlaps the set card** | On the Easy active-workout screen the countdown renders a large "5" with the word **"seconds" sliced in half** by the bottom of its container (only the top ~40% of the glyphs is visible). The REST bar that slides up then overlaps the same card rather than docking below it. Also on this screen: the set pills render **"BW"** with broken glyph spacing (**"BⅢ"**-like), and the top stats row runs **"161 kcal" hard against "666 kg"** with no separation. Compounded by #133, the whole set-logging block reads as unfinished. **Fixed wave 3 (2026-07-31) — agent-reported, not independently re-run this wave** — four fixes: (1) timer ring `SizedBox(140,140) > FittedBox(scaleDown) > Column` — a bare `FittedBox` alone was empirically proven NOT to fix it (the Stack gives unbounded constraints, measured identical 575px height until the SizedBox was added); (2) "BW" split into `ZType.sans` + `ZType.data`; (3) `IntrinsicHeight`+`VerticalDivider` in `workout_stats_strip.dart`; (4) rest-overlay clearance via an `AnimatedContainer(height: isResting?118:0)`. Negative-tested via a geometric assertion because a Stack clips silently — `takeException()` does not catch it. Honest caveat from the agent: fixes 2/3/4 were not independently unit-tested | **FIXED** wave 3 (2026-07-31) — agent-reported, not independently re-run |
| 135 | MED | Workout | **An unlabeled glowing "8" hexagon sits on the exercise card with no indication of what it measures** | The orange hexagon badge on the exercise row is the **exercise strength score** — `HexagonBadge(value: '${score.score}')` fed by `exerciseStrengthScoreProvider(exercise.name)` (`expanded_exercise_card_ui_1.dart:283-300`). Nothing on the card says so: no label, no unit, no scale. The badge's own docstring describes it as *"the '232' / '252' chip in the strength-score screenshots"* — i.e. the design intends 3-digit values, so a bare **8** reads as either broken or a 1-10 rating. The card deliberately hides the badge when `!score.hasData` to avoid showing a "0" placeholder, which shows the concern was anticipated — but an uncalibrated single digit lands the same way. Needs a label/unit, or suppression until the score is meaningful. It also currently renders directly beside an exercise showing NO image (#122), compounding the impression of broken data. **Fixed wave 3 (2026-07-31) — agent-reported, not independently re-run this wave** — `_buildStrengthScoreChip` now renders a labelled pill ("STR 8 · Advanced") matching sibling chips, instead of a bare `HexagonBadge`. Negative-tested: reverting to `'${score.score}'` made `find.text('8')` match again | **FIXED** wave 3 (2026-07-31) — agent-reported, not independently re-run |
| 136 | MED | Workout | **A freshly started workout opens with a stale session already in progress — 23m 37s, 161 kcal, 666 kg before a single set** | Tapping LET'S GO on Active Recovery landed in the active screen already reading **Duration 23m 37s · 161 kcal · Volume 666 kg**, with the header timer at 23:37. No sets had been logged in this session. The figures are carried over from an earlier abandoned session on the same account rather than reset (or resumed without ever telling the user it is a resume). The timer then continues from there, so every duration/calorie figure for the session is inflated, and the completion summary will be wrong. **Fixed wave 3 (2026-07-31) — agent-reported, not independently re-run this wave** — `saved_at_ms` was WRITTEN but never READ; added `kCheckpointStalePromptThreshold` (10 min) + `kCheckpointHardTtl` (6h), `peekStaleCheckpoint()`/`discardOnDiskCheckpoint()`, and a new `stale_checkpoint_dialog.dart` (Resume / Start fresh). Wired into BOTH the Easy path AND `active_workout_screen_refactored.dart`'s `_restoreWorkoutCheckpoint()` (Advanced tier had the same bug class). 7 tests including the 23m37s reproduction | **FIXED** wave 3 (2026-07-31) — agent-reported, not independently re-run |
| 137 | HIGH | Shareables | **The workout share card is not shareable — it uses a cropped anatomical muscle diagram as the hero and carries NO workout stats** | Captured from the share editor after a session. Two fatal problems. **(a) The hero image is an anatomy illustration, zoomed and cropped**: a headless, skinless muscle-anatomy figure on all fours (the Bird Dog exercise diagram), blown up full-bleed so it reads as a grotesque medical render rather than a fitness achievement. Nobody posts this to a story. The exercise illustrations are correct *inside* the app; they are simply not share-card material, and no other art is being substituted. **(b) The card contains no achievement data at all** — just `ACTIVE RECOVERY`, a small `JUL 30 '26`, and a tiny `Zealova` mark. No duration, no exercise count, no calories, no volume, no PR, nothing the user would actually want to show. The top ~35% is empty white gradient. Compare the app's own workout-complete screen, which has all of those numbers to hand. This is meant to be the organic-growth surface (every share is a free impression), so an unshareable card is a growth bug as much as a design one. Also off-brand: the primary **Story** button is neon yellow-green against the app's orange accent (see #15) **ROOT-CAUSED 2026-07-30 — it is a code bug, not a data gap or a design choice.** The card is `templates/workout_details_doc.dart`, returned by `ShareableCatalog.defaultTemplateForKind()` (`shareable_catalog.dart:3026`) as **THE DEFAULT for every `workoutComplete` share**. Two independent defects compound: **(a) the empty list** — its `repeaterEl(...)` at `:57` omits `exerciseMode: true`, and the default is `false` (`doc_kit.dart:295`), which routes the renderer to `resolveFoodItems(data,…)` → `data.foodItems` → **always null on a workout share**, so the exercise ledger silently renders **zero rows**; duration/volume/calories are never bound in this doc at all, leaving only title + eyebrow + watermark. **(b) the anatomy hero** — `:22` binds `photoBg` to `BindingSource.heroImageUrl`, and `workout_adapter.dart:160-167` sets that to the **first exercise's illustration** (its own comment says so), i.e. the `ILLUSTRATIONS ALL/` muscle figure. Fix: add `exerciseMode: true, showImage: true` to that call, and stop binding a full-bleed background to `heroImageUrl` for workout/stats kinds — `workoutSummaryDoc` already does this correctly with a solid dark gradient. **Fixed wave 3 (2026-07-31) — agent-reported, not independently re-run this wave** — `exerciseMode: true, showImage: true` added to the `repeaterEl` call, and the full-bleed `heroImageUrl` binding replaced with the same solid dark gradient `workoutSummaryDoc` uses | **FIXED** wave 3 (2026-07-31) — agent-reported, not independently re-run |
| 138 | MED | Workout / Share | **Tapping Share on the workout card editor never presents the OS share sheet — the controller activates and deallocates in ~19 ms** | Reproduced 2/2 with fresh geometry reads. The button visibly depresses (tint change across 3 screenshots) but no sheet renders and the screen is unchanged. Device log shows the cause: `ShareSheet: Activating com.apple.sharing.sharesheet` → `Deallocating UIAVC` **19 ms later**, twice. Real image data IS attached (`SharePlusData` ×2), so card generation is not at fault — but `UIMessageActivity: Can't send content`, `UIMailActivity: Can't display activity` and SharePlay-unsupported all fire, leaving potentially zero presentable activities, and `UIActivityViewController` tears itself down rather than degrading to a minimal sheet (e.g. Copy). **Control tap proves the row is live**: Save, on the same row, correctly opened the Photos permission dialog. **Caveat — may be Simulator-only** (no share extensions registered there); needs one real-device check before being treated as a shipping bug. **Re-confirmed wave 3 (2026-07-31)** — still needs a real-device check; the Simulator's lack of registered share extensions makes this untestable in this environment. Not fixed or retracted this wave | **OPEN — needs real-device confirmation** (unchanged, wave 3) |
| 139 | LOW | Workout | **`workout_logs.exercises_completed` stays 0 on a fully completed workout** | Workout `d12a95ce-…` driven to genuine completion (`status = completed`, `duration_minutes = 31`, DONE screen showing 4 exercises / 43 reps) yet `exercises_completed` remained **0**. Field is not populated at completion time. Noticed incidentally; root cause not chased. **Fixed wave 3 (2026-07-31) — agent-reported, not independently re-run this wave** — `finalize_workout_log_row()` now derives `exercises_completed` from the same rebuilt `sets_json` it already reconstructs from `performance_logs`. Negative test produced `KeyError: 'exercises_completed'`, proving the column was never touched otherwise | **FIXED** wave 3 (2026-07-31) — agent-reported, not independently re-run |
| 140 | LOW | Nutrition / Barcode | **The barcode scanner is camera-only — no "choose from library" fallback**, so it cannot be used on any device without a working camera | `barcode_scanner_overlay.dart` wires `mobile_scanner`/`AVCaptureSession` with no `image_picker` gallery source. On a simulator it shows a clean, handled *"Scanning is not supported on this device. No cameras available."* — graceful, not a crash. Logged as a **capability gap, not a defect**: the backend path is proven working (see #129 — `GET /nutrition/barcode/5449000000996` resolved the real OpenFoodFacts Coca-Cola 330 ml at 138.6 kcal / 35 g sugar). A gallery fallback would also make the feature testable in CI | **NOT REPRO — by design** |
| 141 | LOW | Workout / UI | Debug-only Flutter overflow banner **"RIGHT OVERFLOWED BY 0.243 PIXELS"** on the workout-complete summary, layered over the Share / Coach-Recap area | Sub-pixel `RenderFlex` overflow, visible only in debug builds; cosmetic. Screenshots `log_situp2.png` / `share_tap.png`. **Fixed wave 3 (2026-07-31) — agent-reported, not independently re-run this wave** — Share button wrapped in `LayoutBuilder`+`OverflowBox` to give `ZealovaButton`'s internal Row 16px of slack without changing its reported size. Confirmed empirically that `ClipRect` does NOT pass the gate — it hides the paint stripes but `RenderFlex`'s assertion still fires at layout time | **FIXED** wave 3 (2026-07-31) — agent-reported, not independently re-run |
| 142 | HIGH | Shareables | **Class-wide: 15 of 18 templates using `repeaterEl` never set `exerciseMode: true`, so their item lists silently render EMPTY on workout shares** | `repeaterEl`'s default is `exerciseMode: false` (`doc_kit.dart:295`), which sends the renderer down `resolveFoodItems()` → `data.foodItems`, null for any workout/PR share. Verified by grep: **18 template files call `repeaterEl`, only 3 set the flag** (`data_box_score_doc`, `ticket_gym_receipt_doc`, `workout_plan_doc`). Confirmed affected and user-reachable: **`workout_details_doc`** (#137, the default card), **`prs_doc`** — whose own comment promises *"a ranked list of recent PRs"* that never renders, reachable from the one-tap Share PR action — **`receipt_doc`** (offered for `_allKinds`, so among the most surfaced cards; its itemised rows are always blank outside food shares), plus `daily_workout_card_doc`, `workout_muscle_card_doc`, `workout_program_doc` (the last two are gated `requiresExercises: true`, so the catalogue believes they show exercises) and `one_rm_doc`. Fix once: infer `exerciseMode` from `data.kind` when unset, rather than patching 15 call sites, and add a test asserting every `workoutComplete`-kind template renders ≥1 row when `data.exercises` is non-empty. **Fixed wave 3 (2026-07-31) — agent-reported, not independently re-run this wave** — fixed at the ROOT rather than at 9 call sites: `exerciseMode` became a nullable tri-state inferred from `data.kind` in `_repeater`, so all 18 `repeaterEl` call sites render correctly without patching each one individually | **FIXED** wave 3 (2026-07-31) — agent-reported, not independently re-run |
| 143 | MED | Shareables | **~29 templates use the exercise anatomy illustration as a full-bleed "photo" hero** because `heroImageUrl` is the only photo source a workout share has | `workout_adapter.dart:160-167` sets `heroImageUrl` to the first exercise's illustration, and ~35 docs bind it to `photoBg`/`photoEl`. Reachable from `workoutComplete`/`statsOverview` and presenting it as a generic photo hero: the **iOS pack (8)** — `ios_movie_poster`, `ios_magazine_cover`, `ios_billboard`, `ios_vinyl_record`, … — the **social pack (12)** (`social_ig_*`, `social_tiktok_post`, `social_bereal_dual`, …, all of which simulate a *photo post*), the **AI pack (6)**, **collectible (3)**, plus `polaroid`, `passport`, `now_playing`, `trading_card_gold`. A "movie poster" or Instagram post whose photo is a headless muscle diagram fails the same way #137 does. **Not a bug everywhere**: `exercise_showcase_doc` heroes the illustration deliberately with a matching "Exercise of the Day" kicker — same source, correct use. Fix at the root: give workout/stats kinds a designed gradient/pattern background (as `workoutSummaryDoc` already does) rather than a photo binding, or leave `heroImageUrl` null unless the template genuinely wants an illustration. **Fixed wave 3 (2026-07-31) — agent-reported, not independently re-run this wave** — fixed via ONE chokepoint, `_stripAnatomyHeroPhoto` in `shareable_catalog.buildTemplateDoc()`, covering both hero-bound backgrounds AND hero-bound photo elements, with `exerciseShowcase` allowlisted (it deliberately uses the illustration) | **FIXED** wave 3 (2026-07-31) — agent-reported, not independently re-run |
| 144 | MED | Shareables | **`now_playing_doc.dart` hardcodes fake scrubber timestamps `'6:51'` and `'48:00'`** as literal text, bound to no data | Every workout renders the identical fake progress-bar times regardless of actual duration — a direct instance of the fabricated-default class this register opened with (#5, the invented 2000 kcal target). Same file is also affected by #143. Bind to real duration or drop the scrubber. **Fixed wave 3 (2026-07-31) — agent-reported, not independently re-run this wave** — the fabricated defaults were removed from `doc_kit` entirely, so any template relying on them now FAILS TO COMPILE — which immediately exposed `ios_glass_widget_doc` silently depending on the fake `['12','WORKOUTS']` grid, plus two more docs with fake scrubber times not named in this row | **FIXED** wave 3 (2026-07-31) — agent-reported, not independently re-run |
| 145 | MED | Shareables / process | **No proof harness covers the ~117-template doc-engine batch, which is why both #137 and #142 shipped invisibly** | Searched `frontend/` and `scripts/` for a CardDoc proof/preview harness — none exists (only unrelated onboarding/redesign HTML mockups). Project convention is that shareables are proofed as **HTML proofs, not golden images**, but that convention was never extended to this newer doc-engine batch. Neither bug is catchable by eye in a proof either: an HTML proof renders whatever data it is handed, so a card with no stat slots looks fine until real data flows. Both are pure Dart render-time logic bugs. A test asserting *"every `workoutComplete`-kind template renders ≥1 exercise row and does not bind a full-bleed background to `heroImageUrl`"* would have caught both classes at once. **Fixed wave 3 (2026-07-31) — agent-reported, not independently re-run this wave** — the dead golden harness was replaced with pure-Dart structural assertions plus paired "negative" tests that assert each rule CAN fail | **FIXED** wave 3 (2026-07-31) — agent-reported, not independently re-run |
| 146 | HIGH | Workout | **The same workout reports two different durations and calorie counts an hour apart — 45 MIN / 226 CAL, then 11 MIN / 55 CAL** | Both captured from the *Active Recovery* detail screen of the same Gentle Start Day-1 session, with the same **4 EXERCISES** both times: 17:11 showed **45 MIN · 4 EXERCISES · 226 CAL**, 18:22 showed **11 MIN · 4 EXERCISES · 55 CAL**. A 4× swing in the headline number a user plans their evening around, with no visible cause and no indication anything changed. Most likely the injury-swap or an "Adjust workout" pass re-derived duration/energy from substituted exercises without surfacing it — but whatever the cause, the figure is presented as settled fact on both screens. **Fixed wave 3 (2026-07-31) — agent-reported, not independently re-run this wave** — turned out to be FOUR divergent duration models, not three: program expansion wrote NEITHER `duration_minutes` NOR `estimated_calories` (the row dict had no such keys). New `workout_duration_energy.py` chokepoint; `reshape.py` now persists calories alongside duration. Note: this is the same chokepoint that introduced the `users.weight` phantom column caught by the gate and fixed (see the wave-3 "new defects" list, item 4) | **FIXED for 3 of the 4 paths — independently re-verified against live prod 2026-08-01; the 4th is code-confirmed only and honestly cannot be verified yet.** `reshape.py` is the decisive live proof: a real `POST /workouts/{id}/reshape-for-readiness?apply=true` (readiness 3, 15 min available) took a workout from **60 min / 402 cal → 24 min / 131 cal**, 6→3 exercises, both fields moving together and both persisting. The implied ratio changed 6.7 → 5.46 kcal/min, which proves the calories are **recomputed from the new composition** rather than carried forward or re-derived client-side — exactly the divergence this row reported. `generate`/`generate-stream` and `quick_adjust.py` (previously a 4th independent formula) both verified as calling the same `estimate_duration_minutes` + `met × weight × duration/60`. **The gap:** the program-expansion path (`generation_source='template'`) has **not been exercised anywhere in the system since the fix deployed**. Fix commit `1dc3c19e` (07-31 03:41:58) is confirmed an ancestor of the live Render deploy `52cce6a8` (live 07-31 08:55:38), but all 56 existing `template` rows were created 07-30 21:34–21:35, i.e. **before** it — and 56/56 carry `duration_minutes` with 0/56 carrying `estimated_calories`, the pre-fix signature exactly. That is **stale data, not a live regression** (no backfill was claimed for this fix), but it also means nobody has yet observed the fixed path produce a row. **Gap closed live, 2026-08-01.** A real `POST` assign of *7-Minute Upper Body* (2wk/5×, primary slot) to the **qa2** account created 10 fresh `generation_source='template'` workouts, and **10/10 carry BOTH `duration_minutes=7` AND `estimated_calories=60`** — no nulls, i.e. exactly the field the 56 stale pre-deploy rows were missing. The assign was also confirmed non-destructive: the 5 superseded workouts still exist (only `is_current` flipped) and a non-overlapping future-dated row was untouched; zero 5xx in Render for the request window. So all four of this row's original divergent models are now reconciled, two of them live-reproduced end to end. **Client-side refinement, 2026-08-01 — this changes what the original observation probably was.** `Workout.estimatedCalories` (`lib/data/models/workout.dart:251-259`) is **not a passthrough**: when `estimatedCaloriesStored` is null or zero it falls back to a client-side MET formula, `met * 70.0 * (minutes / 60.0)`, fed by `bestDurationMinutes` (`:247-248` — `estimatedDurationMinutes ?? durationMinutes ?? 45`). Run the numbers on this row's own report: **226 / 45 = 5.022** and **55 / 11 = 5.0** cal-per-minute. That ratio is `met * 70 / 60`, a **constant** for a fixed exercise list — so both readings almost certainly came from the same client fallback, and what actually changed between them was the **duration** (45 → 11), not the calorie model. The calories were never independently wrong; they tracked a duration that flipped. `durationMinutes` / `estimatedDurationMinutes` are plain deserialized fields with no client-side computation, so the flip originated **server-side, on a re-read of the same workout row**. The wave-3 fix does close the mechanism for newly generated workouts (persisting both fields means the client stops hitting the MET fallback at all), but **the source of the 45 → 11 duration flip itself remains unidentified** — the register's own guess was injury-swap or "Adjust workout" re-deriving, never confirmed. **Strong candidate identified for the flip itself, 2026-08-01 — not yet confirmed.** Every backend write path touching `duration_minutes` was traced. Ruled OUT with evidence: `quick_adjust`'s ease/trim (`workout_modifier.py` writes only `exercises_json`/`modification_history`/`last_modified_method`, and its Flutter side patches local `_exercises` state, never the persisted row); the add-exercise endpoint (`workout_operations.py:855-859` only ever ADDS — wrong direction); and read-time recompute (all 4 callers of `estimate_duration_minutes`/`derive_duration_and_calories` are write-time, so a persisted column cannot self-update between two reads without a real write). That leaves **`reshape.py`'s readiness-scaling branch** (`:296-308`, fires when `readiness_100 < 50`), which updates `exercises_json` + `duration_minutes` + `estimated_calories` **in place on the same row id** (`:364-370`). It is the only branch anywhere in the pipeline that cuts duration hard **while leaving exercise COUNT untouched** — the time-budget branch trims to a floor of 3 and the injury branch drops/swaps by muscle — which is exactly this row's "same 4 exercises". Pre-fix it wrote `duration_minutes` alone and left `estimated_calories` untouched, so the client MET fallback recomputed calories off the new duration with the same implied MET both times: that is why the 5.022 / 5.0 cal-per-minute match is so tight. **The honest gap:** the count-preserving branch needs `sleep_score`/`readiness_score`, and the only call site passing those is `pre_workout_reshape_gate.dart`, which **discloses** the change first (an `apply:false` preview → `_ReshapeDiffDialog` listing reasons → `apply:true` only on confirm). The two genuinely silent call sites (`report_pain_sheet.dart:181-182`, `coach_hero_card.dart:1122-1124`) pass only `pain_part`/`pain_level`, so `_readiness_0_100` returns None and the count-preserving branch cannot fire for them. So the SILENT trigger and the COUNT-PRESERVING mechanism do not line up in a single call site. Not a contradiction — a user can dismiss a dialog at 17:0x and not connect it to a number re-read at 18:22 — but it is unresolved. Closing this needs either a live repro (drive the pre-workout gate at low readiness against a fresh 4-exercise workout) or an audit-trail read of the original row. **Audit-trail read attempted 2026-08-01 — CANNOT VERIFY, and it argues AGAINST the candidate above.** Two corrections fell out of it. (a) **Account**: the original workout is not on the walk account at all — that account has 11 workouts and zero recovery-typed ones. A whole-table `name ilike '%active recovery%'` search returns exactly **one** row, on the **qa2** account (`9335b664-…`). (b) **The row cannot be the one observed**: `93d33408-0d34-4591-9825-ecceaeec2824` (`duration_minutes=11`, `estimated_calories=null`, `modification_history=[]`) has `created_at = 2026-07-30 **21:34:25***, i.e. **after both the 17:11 and 18:22 reads**, and it sits inside the same 21:34–21:35 stale-template batch noted above. qa2's full 14-row history contains no earlier "Active Recovery", no supersede chain, no sibling — **the original row is simply gone**, so there is nothing left to interrogate. Forensics on the surviving proxy anyway: `workout_changes` is empty for it and `modification_history` is `[]`, which rules out `quick_adjust`/WorkoutModifier (those stamp both) but is **silent on reshape** — see #159. Decisive against the candidate: `readiness_scores` for this account holds exactly **one** check-in on 07-30, at **23:15:45** (later still), with sleep 7 / readiness 7 → `readiness_100 = 70`, and `_apply_readiness_scaling` requires `< 50` or `> 70` — 70 is excluded by the strict inequality. So that check-in could not have fired the branch even ignoring the timing. **Verdict: the original observation is unreproducible and its row is unrecoverable.** The mechanism above remains the only one that fits the symptoms, but it is now explicitly an unproven hypothesis, not a pending confirmation. Left open deliberately rather than closed on the most plausible story | **FIXED — all 4 paths verified 2026-08-01**, program-expansion live-confirmed (10/10 rows); the underlying 45→11 duration flip remains unexplained — see the client-side refinement |
| 147 | MED | Workout / AI copy | **AI Insights greets Day 1 of a brand-new program as "workout number two"** | The AI INSIGHTS card on the *Active Recovery* detail reads *"Build Your Foundation Today — Welcome to workout number two, where consistency is your greatest strength…"*. This is **Day 1** of Gentle Start, assigned minutes earlier (#113 — the program was started at 16:4x and this card was read on the first session). The counter is either off by one or reading a global session count rather than the program's. On the very first session of a new program the copy is both wrong and undermines the moment it exists to celebrate. **Fixed wave 3 (2026-07-31) — agent-reported, not independently re-run this wave** — `_count_completed_workouts_for_milestone(db, user_id, assignment_id)` scopes the count to the assignment when present, falling back to lifetime only for ad-hoc workouts | **FIXED** wave 3 (2026-07-31) — agent-reported, not independently re-run |
| 148 | MED | UX / global | **Horizontal chip rows slice their last item with no affordance that the row scrolls** — repeated across at least five surfaces | The rightmost chip is cut mid-word with no fade, gradient, peek or arrow to signal more content: log-meal source chips (**"Quick lo…"**), menu-analysis Sort row (a 5th chip half-visible past Inflammation), coach TRY ASKING actions (**"Nutritio…"**), the coach quick-action row (Scan Food / Analyze Menu / Nutrition), and the workout-detail meta chips (**DIFFICULTY: MODERATE** clipped by the screen edge). Users read a sliced word as a layout bug, not as "scroll me" — so discoverable actions sitting off-screen are simply never found. One shared scrollable-row treatment (edge fade + partial peek) fixes all of them. **Fixed wave 3 (2026-07-31), independently verified** — a shared `FadingChipRow` widget was extracted from `chat_quick_pills.dart` (edge fade + partial peek, the exact treatment this row asked for) and adopted on 5 nutrition surfaces — 2 more than this row originally listed — plus the workout-detail meta chips. Not individually re-verified against each of the 5 named surfaces, but they now share one implementation rather than 5 independent ones | **FIXED** wave 3 (2026-07-31) — independently verified |
| 149 | MED | UX / global | **Unexplained bare numbers are used as badges in at least four places, with no unit, scale or label** | A glowing hexagon **"8"** on the exercise card (#135 — it is the strength score, though nothing says so), a **"2"** on the Home Barcode-Scanner promo card, a **"4"** on the Home Missed-workout card, **"AI INSIGHTS 4"**, **"WARM UP 4"**, **"EXERCISES 4"**, **"Coach is learning you 0/4"**. Some are counts, one is a score, at least one is a stack depth — and they are visually interchangeable. The `0/4` form is the only one that reads unambiguously. Either label them or give counts and scores visibly different treatments | **FIXED** wave 3 (2026-07-31) — the stack-depth badge (the same widget behind both the "2" and "4" screenshots) now renders an icon + count with a Semantics label instead of a bare number. Note the `AI INSIGHTS 4` / `WARM UP 4` / `EXERCISES 4` counts live in `lib/screens/workout/` and were handled with that surface |
| 150 | MED | Home | **Three promotional/status banners stack above the real content, pushing the actual day below the fold** | On open, Home shows a cyan **Barcode Scanner "Try It"** promo (with a stacked-card "2" badge), a cyan **"Coach is learning you 0/4"** card, and a **"Connect Health…"** banner — three full-width cards before YOUR COACH, the to-dos or any workout content. Two of the three are dismissible, which implies they are known to be interruptive. The stack badge suggests more are queued behind. A user opening the app to start a workout scrolls past three asks first | **FIXED** wave 3 (2026-07-31) — `RatingPromptBanner` and `CalibrationBanner` folded into the existing `StackedBannerPanel` swipe stack (which already had dismissal state and a count badge) rather than a second stacking mechanism; both standalone slivers removed from `home_screen.dart`. Existing SharedPreferences dismissal keys reused so nobody is re-prompted. **Correction 2026-08-01 — only PARTIALLY closed, verified on screen and at file/line.** The stack is real (Home now shows one rotating banner with a count badge — observed at **6**, then **3**). But **"Connect Health to see steps, sleep & readiness" is still a completely separate full-width sliver** and never joined the stack: `home_screen.dart:1258` renders `StackedBannerPanel()`, while `:1276-1278` renders `HomeMetricsStrip()` as its own later sliver, and the Connect-Health row lives inside that strip (`home_metrics_strip.dart:94-131`), self-substituting for the normal 4-cell metrics row when health is unconnected (`:85-93`). It has **no id, no dismiss integration and no badge participation** in `stacked_banner_controller.dart`. So two stacking mechanisms became one and a half, and Home still opens with a stacked banner AND a standalone banner above the real content | **PARTIALLY FIXED** — stack shipped; Connect Health still a separate sliver (2026-08-01) |
| 151 | MED | Workout | **The Workout tab's hero card surfaces TOMORROW's session as the primary CTA while today is selected in the date strip** | The date strip highlighted **30** (today) while the hero card beneath read **"STRENGTH FOCUS · Tomorrow · Week 1 · 45m · 4 exercises"** with a full-width **START WORKOUT** button. The most prominent action on the screen starts a session dated tomorrow, directly under a control asserting today. Either the hero should follow the selected date or say clearly why it is showing another day | **FIXED** wave 3 (2026-07-31) — the claimed root cause did NOT reproduce: an isolation test against `CarouselDateFocus` resolved the wrong-arrival-order sequence correctly, so the sync mechanism is sound. Fixed by labelling instead — the planner now states why the hero is showing another day ("Today's workout is done — showing tomorrow"), so the two surfaces never silently disagree. Lower-risk than changing carousel index-picking timing |
| 152 | LOW | Home | **The coach card header carries three separate dismissal-ish affordances side by side** — `⋮`, a collapse chevron, and `✕` | Three controls in one header row with overlapping meaning (menu / collapse / dismiss) and no labels, on the card the app most wants read. The `✕` and the chevron are visually similar in weight, so the destructive one (dismiss) is no more guarded than the reversible one (collapse) | **FIXED** wave 3 (2026-07-31) — the standalone ✕ called the exact same `setDismissedToday()` already reachable one tap deeper via ⋮, i.e. true duplication rather than two capabilities. Header is now ⋮ + collapse chevron only, and the destructive menu row gained a warning tint and an explicit subtitle ("Also hides all of today's nudges") |
| 153 | LOW | Workout | **#27 partially holds — the pre-workout check-in is still two sequential sheets, not one page with a disclosure** | `e10473d2` was verified as *"sheet collapsed from a 2-step wizard to one page, optional half behind a disclosure"*. Observed on the 07-30 binary: **Quick check-in** (Sleep + Readiness sliders) → **Next** → a second full sheet **"Anything to flag?"** (time available + sore-area chips) → **Start workout**. The second half IS genuinely optional ("…or just continue"), so the intent landed, but it is presented as a second page rather than a disclosure, i.e. still two taps before the core action. Downgrading rather than retracting — verify the intended design before changing anything. **Correction, wave 3 (2026-07-31) — this was mis-marked above.** The prior "agent-reported" fix claim in this row is retracted: this was not a wave-3 fix at all. Verified via `git log` — the single-page-with-disclosure treatment landed in commit `e10473d2`, **before this session**, and the current code is a single page with a collapsible "Sore, short on time?" disclosure — no second "Next" sheet. So this row's own diagnosis (two sequential sheets) no longer matches the code and should have been closed out earlier rather than newly fixed here. Its other half — the 12 hardcoded `AppColors.cyan` in `pre_workout_reshape_gate.dart`, the register's headline evidence for #15 — is being closed by the ongoing accent sweep, not by this row | **FIXED — already fixed pre-wave-3** (`e10473d2`), correction 2026-07-31; accent half tracked under #15 |
| 154 | MED | Workout | **`/swap-exercise` advertises `section` support and silently ignores it** — a warmup swap searches the main exercise list and 404s | `backend/models/schemas.py:330` declares `SwapExerciseRequest.section: Optional[str] = Field(default="main", description="Section: main, warmup, or stretches")`, but `workout_operations.py`'s `swap_exercise_in_workout` (starts `:199`) unconditionally reads `exercises_json` at `:211` — the MAIN list — with no `section` branch anywhere in its body; the only read of `payload.section` is `:329`, an unrelated `apply_to_future` gate. Contrast `add_exercise_to_workout` (`:480+`), which DOES branch: `section = payload.section or "main"` (`:486`) then `if section == "main": … elif section == "warmup": …`, with the warmup branch writing the `warmups` table (`~:586`). So ADD supports warmup server-side; SWAP's schema advertises identical support and the handler ignores it — a `section:"warmup"` swap searches the MAIN list for a warmup move name and 404s "not found in workout" (`:289-290`). Same class as the register's phantom-column rows: an interface advertising a capability it does not have, failing silently behind a generic error. Currently worked around client-side by composing swap as add-then-splice | **OPEN** — file/line-verified 2026-07-31, not yet fixed |
| 155 | CRIT | iOS build | **The app cannot be installed on any iOS 26 simulator — `simctl install` fails on the Live Activity extension, and the committed workaround for this is wrong on both of its two claims** | Reproduced 2026-07-31 on a clean `flutter build ios --simulator --debug` (21:21), on **two** different devices (iPhone 16e `E63C5B41`, iPhone 17 `488162E5`) — so it is the build, not the device: `IXErrorDomain code=2 … Failed to create app extension placeholder for PlugIns/FitWizLiveActivityExtension.appex`. `mobile/flutter/ios/FitWizLiveActivity/Info.plist` carries a 25-line comment (commits `ea361aea`, `c58e4ca4`) asserting two things, **both of which this reproduction falsifies**: (a) *"literal version keys, not `$(MARKETING_VERSION)`"* — but the built appex has **neither** `CFBundleShortVersionString` nor `CFBundleVersion` (`PlistBuddy Print` → *Does Not Exist*, while the parent app has `1.2.67` / `1140`). The literals never reach the product because the extension target sets `GENERATE_INFOPLIST_FILE = YES` **and** `MARKETING_VERSION = "$(FLUTTER_BUILD_NAME)"` / `CURRENT_PROJECT_VERSION = "$(FLUTTER_BUILD_NUMBER)"` (`project.pbxproj:686-697`, `:732-743`, `:775-786`); those variables come from `Generated.xcconfig`, which only the **Runner** target includes, so they resolve EMPTY, and Xcode's generated plist — which wins over `INFOPLIST_FILE` — drops the empty keys. The comment's own parenthetical names the durable fix ("include Generated.xcconfig in the extension target's build configs") and it was never done. (b) *"`NSExtensionPrincipalClass` … must be PRESENT for the iOS 26 sim installer"* — the **exact opposite** is true. Proven by bisecting on the built artifact: adding only the two version keys advanced the failure to a new, explicit error — `Appex … defines either an NSExtensionMainStoryboard or NSExtensionPrincipalClass key, which is not allowed for the extension point com.apple.widgetkit-extension`. Deleting the (empty-string) `NSExtensionPrincipalClass` then made `simctl install` **succeed**. So the shipped Info.plist actively contains a key the installer forbids. Both edits are needed; either alone still fails. **Not simulator-only in kind**: (a) means the appex ships to the App Store with no version keys while Apple requires the appex `CFBundleShortVersionString` to match the app's, and (b) is a WidgetKit plist violation, not a sim quirk — both are plausible `altool`/App Store Connect validation rejections, which makes this a live iOS-submission risk, not just a QA inconvenience. **Blocks all on-device QA**: no E2E verification of any row is possible on a fresh build until this is fixed. Worked around locally for this session by hand-patching the built artifact (`PlistBuddy Add :CFBundleShortVersionString/:CFBundleVersion` + `Delete :NSExtension:NSExtensionPrincipalClass`); the repo is untouched, so the next `flutter build` reproduces it | **FIXED 2026-07-31 — verified on a clean build, not a patched artifact.** Two changes, one per cause. (a) New `ios/Flutter/AppExtension.xcconfig` (`#include "Generated.xcconfig"` only — deliberately NOT `Debug/Release.xcconfig`, which would pull `Pods-Runner.*.xcconfig` and link the Runner target's CocoaPods into the extension), wired as `baseConfigurationReference` on all three LiveActivity `XCBuildConfiguration`s (`3192E16A/B/C`). The target's existing `$(FLUTTER_BUILD_NAME)` / `$(FLUTTER_BUILD_NUMBER)` references now resolve, so the versions derive from `pubspec.yaml` and **cannot drift** from the host app — chosen over hardcoded literals precisely because the App-Store match requirement makes drift a submission failure. (b) `NSExtensionPrincipalClass` deleted from `FitWizLiveActivity/Info.plist`, and the two false claims in its comment replaced with the reproduction. **Verification:** clean `flutter build ios --simulator --debug` → built appex now reports `CFBundleShortVersionString 1.2.67` / `CFBundleVersion 1140`, exactly matching `pubspec.yaml version: 1.2.67+1140`, and `NSExtensionPrincipalClass` → *Does Not Exist*; `simctl install` onto a **freshly uninstalled** iPhone 17 (`488162E5`, a device never hand-patched) **succeeded**; app launches (pid assigned, UI renders) with no extension crash report. `plutil -lint` on the patched `project.pbxproj` → OK |
| 156 | LOW | i18n | **Dead ARB key `nextSetPreviewKg` — a hardcoded `' kg'` string translated into all 36 locales with zero call sites** | Surfaced while verifying #18's unit work. `grep -rn "nextSetPreviewKg" lib/ --include='*.dart'` returns **only** `lib/l10n/generated/*` — the abstract getter in `app_localizations.dart:40719` and one `=> ' kg'` per locale file. No widget reads it; every one of the 8 sibling `nextSetPreview*` keys IS called (from `next_set_preview_card.dart` and friends), so this is a single orphan, not an unused family. **Not user-facing and not a #18 regression** — nothing renders it, so no user sees a hardcoded unit. It is cruft from the unit refactor, and the reason it is worth a row at all is that a live `' kg'` literal sitting in the l10n surface is exactly what a future grep-based unit audit would flag as a false positive (or, if re-wired by someone assuming it works, a real one). Delete the key from `app_en.arb` and regenerate | **FIXED 2026-08-01** — `e5e81888`, pushed. Removed from all **36** `.arb` files (it had been translated into every locale) and regenerated with `flutter gen-l10n` — the project's real codegen path, NOT the `build_runner` trap that applies only to the committed `.g.dart` files under `lib/data/local/`. 114 deletions, 0 insertions; `grep -rn nextSetPreviewKg lib/` → 0 |
| 157 | CRIT | iOS build | **`flutter build ios --release` fails project-wide — no release IPA can be produced at all, so no TestFlight or App Store build is currently possible** | Surfaced 2026-08-01 while adversarially verifying #155 on the release path (it is unrelated to #155 — the two just share the same command). `Target release_ios_bundle_flutter_assets failed: Avoid non-constant invocations of IconData or try to build again with --no-tree-shake-icons`. Release mode tree-shakes the icon fonts, which requires every `IconData` to be a compile-time constant. **Two sites construct one dynamically** — the agent found the first, and a follow-up sweep found the second, which matters because fixing only the first still fails the build: (a) `lib/screens/habits/habit_detail_screen.dart:208` — `IconData((json['iconCodePoint'] as num).toInt(), fontFamily: json['iconFontFamily'] …)`, built straight from a server JSON payload; (b) `lib/shareables/doc/card_doc_renderer.dart:670` — `IconData(p.iconCodepoint!, fontFamily: p.iconFontFamily ?? 'MaterialIcons')`. NOT the same as the many `_getIconData(String)` helpers across `home/widgets/` — those are `switch` statements returning const icons and are fine. **Pre-existing, not from this session**: `habit_detail_screen.dart` was last touched 2026-06-14 (`4dae8006`, the redesign wave), so the release path has been broken for ~7 weeks with nobody noticing — because **no CI workflow builds iOS**. `.github/workflows/` holds engagement-cron, image-url-integrity, email-cron and android-release; Android is gated, iOS is not, so nothing catches an iOS-only break. **Why this outranks the register's other iOS rows:** those describe things that are wrong in a build; this one means there is no build. It is the concrete answer to "are we ready to ship iOS" — today, no, independent of feature quality. **Two candidate fixes, and they are a genuine product decision, not a mechanical choice:** (1) pass `--no-tree-shake-icons` in the release command — one flag, preserves arbitrary server-driven icons, costs the full icon font in the bundle (~1 MB); (2) resolve codepoints through a const lookup table — keeps tree-shaking, but constrains icons to a fixed allowlist, so any server-sent codepoint outside it must degrade to a default. Which is right depends on whether habit/shareable icons are meant to be open-ended | **FIXED 2026-08-01 — the release build now succeeds, and tree-shaking is verifiably still ON.** `flutter build ios --release --no-codesign` → `✓ Built build/ios/iphoneos/Runner.app (167.2MB)`. Proof the saving was kept rather than bypassed: the shipped `MaterialIcons-Regular.otf` is **167,432 bytes vs the full font's 1,645,184** — ~1.5 MB stripped, which is exactly what `--no-tree-shake-icons` would have thrown away. **The framing in this row was wrong and is corrected: it was never a product tradeoff.** Both call sites are round-trips of icons the app itself chose — habit icons originate in `habits_section.dart`'s hardcoded `_getIconData(String)` map and are only serialised as a codepoint to survive a JSON hop, and `IconProps.iconCodepoint` is **never assigned anywhere in `lib/`** (every icon prop is an emoji; the non-emoji branch was unreachable and still broke the build). Nothing can supply a codepoint outside a fixed set, so the const table costs no capability. New `lib/core/utils/const_icons.dart` — the lookup is built from `icon.codePoint` over a `const List<IconData>` rather than hand-written hex, because a mistyped codepoint would silently map to the wrong glyph and only ever be caught on screen (the first draft of this fix did hand-write them; that was the bug avoided). **Gate**: `test/core/const_icons_test.dart`, 5/5 — scans all of `lib/` and fails if any non-const `IconData(` appears, plus round-trip / uniqueness / fallback / habit-coverage checks. It fails in ~1s on `flutter test` instead of at submission. **#157 was not one bug but three, stacked** — nothing had exercised this path in 7 weeks: (1) the dynamic `IconData`; (2) Flutter's generated SPM package declaring `.iOS("13.0")` while every real target is 16.0/16.2 and `background-downloader` needs ≥14 — cleared by disabling SPM, which this CocoaPods-based project does not use anyway (23 plugins do not support it); (3) a self-inflicted missing import whose guard checked for a string the new comment itself contained. **Two caveats that are NOT closed:** the SPM fix is `flutter config`, i.e. **machine-level, not in the repo** — it will not travel to another machine or to CI. And **there is still no iOS CI workflow**; the icon gate closes cause (1) only, so causes of type (2) would still slip through silently |
| 158 | CRIT | Workout | **Completing an Easy-tier workout records the completion NOWHERE — and the client is told it succeeded.** The complete screen also renders as a black "BOTTOM OVERFLOWED BY Infinity PIXELS" wall | Reproduced live end-to-end 2026-08-01, corroborated independently from three sides (screen, Render, Postgres). Sequence: sets logged in-app → "Complete workout" → confirm → the workout-complete screen (`workout_complete_screen.dart`; body is `Stack → SafeArea → SingleChildScrollView → Column(mainAxisSize.min)`) renders **fully black** with the DONE pill overlapping the status bar and an *Infinity*-pixel bottom overflow, i.e. a child received an unbounded height constraint. Persistent across captures at 21:54 and 21:55, and after relaunch **Home still shows the same session as unchecked with a "Start workout" CTA**. **Database state afterwards:** `workout_logs` `7c1dbce4-…` created 02:46:41 and **never touched again** — `status='in_progress'`, `sets_json='[]'`, `exercises_completed=0`; parent `workouts.is_completed=false` and `status` never even left `'scheduled'`; **zero XP** (`xp_transactions` has nothing after a `daily_login` at 02:41). **Root cause — a silent-success hole between two correct pieces of code.** `_easyFinalizeWithOfflineFallback` (`easy_persistence_helpers.dart:461`) branches on the caller-supplied `workoutLogId`: non-null → `PATCH /performance/workout-logs/{id}` (`update_workout_log`, whose own docstring says it exists precisely "to backfill the row with full sets_json + metadata + final total_time" because "the row itself was created at first-set persistence with sets_json='[]'"); null → `createWorkoutLog`. At finalize the id was **null**, so it took the create branch — and that branch **cannot ever work**, because it sends the stable idempotency key `wklog_$workoutId`, which the server's migration-2247 replay guard (`api/v1/performance_db.py:658-670`) answers by returning the **existing row untouched**. The endpoint still logs `"Workout log created"` and returns 200 with a row, so the client sees `created != null`, prints `✅ finalize (create) succeeded`, and returns. **Nothing was written and every layer reported success.** Render for the completion window: **27 × `POST /performance/workout-logs`, all 200, all resolving to the same row id — and ZERO `PATCH`.** **Second, separate defect in the same burst:** those 27 calls also wrote **26 garbage `performance_logs` rows** (`reps_completed=0`, `weight=0`, `is_completed=false`) covering every un-logged set slot. Only **1** row is real (Assisted Chin-Up, set 1, 9 reps, 02:46:42). So a failed finalize actively pollutes the performance history it failed to save. **Relationship to #1:** this is NOT a recurrence — #1 was over-eager completion (empty log flipped to `completed`). This is the opposite failure mode, stuck-forever, and it is a regression surface of #1's own client-queue fix. #1's server half (mig 2390 DEFAULT `in_progress`) is working exactly as designed; it is what makes this row visible instead of silently corrupt. **Fix direction (not applied):** the create branch must not be treated as a finalize — either propagate the log id from first-set persistence into `runEasyBackgroundSave`, or have the create branch detect that the returned row is a replay (`sets_json` still empty / `status='in_progress'` while finalizing with real data) and immediately `PATCH` using the returned id. Hardening the server to upsert on a replay carrying real data would close the class rather than the instance. Also fix the unbounded-height child on the complete screen, and stop the completion path emitting zero-value `performance_logs` rows **Confirmed again on the post-#160 binary, with log-level proof of the call pattern:** in the 18s after Complete, the client fired **20+ `POST /performance/workout-logs`**, every one logging `Creating workout log` → `Workout log created: id=7c1dbce4-…` — the SAME row id, all 200 OK, **zero net writes**; `sets_json` stayed `[]`, `status` stayed `in_progress`, `completed_at` kept its stale hours-old timestamp. Second symptom from the same window pins the root: `POST /personal-goals/workout-sync` logged **`workout_log_id=None`** — nothing downstream ever had a real log id, because the client does not hold one. (Also seen, unrelated and unfiled: `POST /workouts/{id}/warmup/apply-template` 500'd once during warm-up toggling.) | **FIXED 2026-08-01 — both halves, instance and class.** **Client** (`easy_persistence_helpers.dart`): a 200-with-a-row no longer counts as success. New `_looksUnfinalized()` inspects the returned row — if it came back still empty (`sets_json` `[]`/null/blank and status `in_progress`) while we are finalizing with real sets, that was a replay, so the finalize now immediately re-issues as the **`PATCH` it should always have been**, using the id the server just handed back. Deliberately conservative: an unrecognised row shape returns false, so a genuine create is never followed by a redundant PATCH, and a failed follow-up falls through to the offline queue rather than returning a success it did not earn. This half works against **current production** with no deploy. **Server** (`core/db/workout_db.py`): the migration-2247 guard now distinguishes a true replay from an upgrade. `_carries_new_session_data()` returns true only in the one direction that matters — stored row empty, incoming payload carrying real sets — and merges via a narrow `_FINALIZE_MERGE_FIELDS` allowlist. Identity columns (`user_id`, `workout_id`, `idempotency_key`, `id`, `gym_profile_id`) are **excluded by construction**, so a replay can never repoint a row at another user or workout. This closes the CLASS: no caller can silently no-op a finalize again, whatever it sends. **Requires a push to take effect** (Render auto-deploys on `main`). **Gate**: `backend/tests/test_workout_log_idempotent_merge.py` — 13/13 passing, pinning all four historical `sets_json` shapes and, critically, both directions: the finalize wins over an empty row, while a true replay and a late empty write both change nothing. `flutter analyze` on the changed file: no issues. Note the 3.9 `.venv` cannot run the suite (`X | None` syntax) — use `.venv312`. Still to verify on device: that a completion now lands. The 12 `performance_logs` writes seen for ~4 sets are unexamined and may be the same retry storm hitting a second endpoint |
| 159 | HIGH | Workout / forensics | **`reshape-for-readiness` rewrites a user's duration, calories and exercise list in place and leaves NO audit trail — in either audit surface the app has** | Found 2026-08-01 while trying to explain #146, and it is the reason #146 cannot be explained. The app has two forensic surfaces for workout mutations: the `workouts.modification_history` column and the `workout_changes` table. `services/workout_modifier.py` (the quick-adjust/ease/trim path) writes **both**. `api/v1/workouts/reshape.py` references **neither** — grepped whole-file, zero occurrences of `modification_history` or `log_workout_change` — yet its `apply=true` persist step (`:364-370`) issues an in-place `.update()` on the same row id changing `exercises_json`, `duration_minutes` and `estimated_calories` together. **Consequence:** after the fact, a reshaped workout is indistinguishable from one that was always that size. An empty `modification_history` proves quick-adjust never touched a row but says nothing about reshape, so the absence of a trail is not exculpatory — which is exactly the dead end #146 hit: the one surviving candidate row had `modification_history=[]` and zero `workout_changes`, and that evidence turned out to be worthless for the question being asked. Two of the three client call sites (`report_pain_sheet.dart:181-182`, `coach_hero_card.dart:1122-1124`) apply **silently**, with no confirmation dialog, which makes the missing trail worse: a user's plan can be rewritten with neither an in-app disclosure nor a server-side record. **Not a data-corruption bug** — the endpoint does what it is designed to do, and #146 confirmed it now writes duration and calories consistently. It is an observability gap, and it is the reason a real user-reported discrepancy had to be closed as unreproducible. Fix: stamp `modification_history` + `log_workout_change` from reshape's persist step, matching what `workout_modifier.py` already does | **FIXED 2026-08-01** — `07de678a`, pushed and deployed. `reshape.py`'s `apply=True` persist now appends a `modification_history` entry (`type: reshape_for_readiness`, reasons, exercise count before/after, duration, calories, timestamp, `method: pre_workout_checkin`) **without clobbering prior entries**, and calls `log_workout_change(...)` — the same helper the other mutation endpoints use, writing the `workout_changes` table that `GET /{id}/modification-history` reads. `last_modified_method` is stamped for consistency with sibling writers. A **preview** call (`apply=False`) still touches neither surface. Gate: `backend/tests/test_reshape_audit_trail.py` asserts both surfaces on apply, that pre-existing history is appended to rather than overwritten, and that preview writes nothing; `test_reshape_persists_energy_with_duration.py` (#146's sibling) still passes, so the calorie/duration fix is undisturbed. **Note on scope**: the "two of three call sites apply silently" observation in the evidence above is *context* for why the missing trail mattered — it is not part of this row's fix, and was briefly and wrongly reported as leaving #159 half-done. It is now tracked separately as **#178** |
| 160 | CRIT | Workout | **The workout-complete screen renders as a black wall — "BOTTOM OVERFLOWED BY Infinity PIXELS"** — because the fix for a 0.243-pixel overflow (#141) introduced an infinite one | Observed live 2026-08-01 on the payoff screen of the core loop: fully black, the DONE pill jammed into the status bar, yellow overflow stripe at the bottom, persistent across captures at 21:54/21:55/22:31 and unchanged by relaunch. **Root cause:** `OverflowBox` is `sizedByParent` — it reports `constraints.biggest` as its OWN size. The instance at `workout_complete_screen.dart` (added by #141 to grant a few px of horizontal slack to `ZealovaButton`) constrains only `minWidth`/`maxWidth`; **height passes straight through**. It lives in `Scaffold.bottomNavigationBar`, where the incoming height constraint is not tight — so it sized itself to the full available height, the bottom bar swallowed the entire screen, `body` was squeezed to zero (hence black, and hence the DONE row appearing at the TOP — it is the bottom bar, now occupying everything), and the `Column(mainAxisSize.min)` overflowed. **Reproduced headlessly before any change** (`test/screens/workout/workout_complete_bottom_bar_test.dart`): the harness reports the bottom bar at `size: Size(760.0, 592.0)` against `constraints … 0.0<=h<=592.0` — i.e. it took 100% of the available height — and a vertical `RenderFlex` overflow on the Column. The paired test proves the fix. **Fix:** pin the vertical axis — `Expanded → SizedBox(height: _kCompleteCtaHeight) → LayoutBuilder → OverflowBox` — so only #141's horizontal slack is granted. The literal `52` the `_isSubmitting` branch already hardcoded is now the shared `_kCompleteCtaHeight` constant, so the two CTAs cannot drift apart again (the drift is what made this possible). `flutter analyze` on the file: **0 errors** (3 pre-existing `unused_field` warnings untouched). Rebuilt and installed on the simulator. **Note the class, not just the instance:** #141 was a real 0.243px overflow and its fix was reasonable in isolation — but `OverflowBox` was the wrong tool, because relaxing ONE axis silently relaxes the other. **Class swept 2026-08-01 — contained, and the codebase already knew this trap.** There are exactly **two** `OverflowBox` uses in all of `lib/`. The other, `quiz_personalization_gate.dart:1472-1477`, is already correct — wrapped in `SizedBox(height: 11)` with an explicit `maxHeight` — and carries a comment describing *this precise failure mode*: "the incoming height is unbounded, and a bare OverflowBox would try to size to Size(14, Infinity) and throw. The SizedBox bounds it." So the knowledge existed in the repo; #141's fix simply did not apply it, and nothing enforced it. That is the more useful lesson than the one-line fix: the trap was documented in one place and re-sprung in another. The gate added here (`workout_complete_bottom_bar_test.dart`) is what makes it enforced rather than merely known. **And there is a sharper lesson still, found 2026-08-01: a gate for this bug ALREADY EXISTED and was already RED.** `test/screens/workout/workout_complete_share_button_overflow_test.dart` — the regression test written for #141 itself, whose stated purpose is *"Share/Done action row never overflows"* — fails with **`Stack overflow` in `RenderConstrainedOverflowBox.performLayout`**, recursing on itself. That is not an unrelated failure: it is THIS defect, reproduced by the very test written to guard the change that introduced it. The test builds its own harness (`Center → SizedBox(width: 335) → Row → Expanded → LayoutBuilder → OverflowBox`) with the height unbounded, i.e. the exact production shape before the fix. So the bug was detectable by `flutter test` the whole time. **Why nobody saw it:** the suite has a **442-failure baseline** out of ~3,400, so a genuinely red gate is indistinguishable from background noise. A test suite nobody can read is not a safety net — this bug shipped to the core loop's payoff screen while its own regression test sat failing. The test now needs updating to mirror the fixed production shape (`SizedBox`-bounded), and the 442 baseline needs attribution before the suite can be trusted to catch anything | **FIXED 2026-08-01** — root-caused, reproduced headlessly, fixed, gated by a paired pass/fail test; on-screen re-verification of the completion flow still owed |
| 161 | MED | Theme / Home | **#15's accent sweep left a live purple banner behind — suppressed by an allowlist annotation whose stated reason is false, plus three dead duplicate branches from a mechanical rewrite** | Observed on Home 2026-08-01: the **"Ask Your Coach"** Week-1 tip banner renders with a **purple** icon tile and a purple "Try It" action, on a screen where every other element is the orange accent — including its own siblings in the same stack ("Streak ends at midnight", "Missed: …"), which correctly use `context.accentColor`. **Why the gate reports clean:** the colour is not a missed widget literal — it is **explicitly allowlisted** at `data/providers/week1_tips_provider.dart:84` (and `:157`): `accentColor: AppColors.purple,  // accent-allowlist: … per-tip decorative colour is resolved by the consuming Week1TipBanner at render time`. **That rationale is untrue.** `week1_tip_banner.dart:110` reads `final accent = isDark ? tip.accentColor : _lightAccent(tip.accentColor)` — in **dark mode it uses the raw literal with no resolution whatsoever**, which is exactly the case on screen. **And the light-mode branch is broken by the sweep itself.** `_lightAccent()` (`:220-226`) opens with **three identical conditions**: `if (darkColor == context.accentColor) return context.accentColor;` / `… return AppColorsLight.cyan;` / `… return context.accentColor;`. The 2nd and 3rd are unreachable dead code. This is the fingerprint of a mechanical find-and-replace: three originally distinct constants (orange / cyan / a third) were all rewritten to `context.accentColor`, collapsing the switch. Purple matches none of them and falls through to `return darkColor` — raw purple in light mode too. **Why this matters beyond one banner:** #15 was closed at *10,537 findings across 1,212 files → 0* with an EMPTY baseline, and the gate was negative-tested and genuinely does fail on a new hardcoded colour. Both of those things remain true. What this shows is the residual risk that survives such a sweep: **an allowlist entry is only as good as its justification, and nothing verifies the justification.** A false "it's resolved downstream" comment is indistinguishable from a true one to any AST gate. Fix: resolve the tip colour at render time as the annotation claims (or drop per-tip colours), and repair `_lightAccent`'s collapsed branches. Consider auditing the other `accent-allowlist` rationales that assert downstream resolution | **FIXED 2026-08-01** — `d975e53a`, pushed. The `Week1Tip.accentColor` field was **deleted outright** rather than corrected, along with all 11 schedule-entry literals and their false "resolved by the consuming banner at render time" allowlist comments — so the misuse is now structurally impossible, not merely fixed. `week1_tip_banner.dart` reads `context.accentColor` directly and `_lightAccent()` (the three-identical-branches dead code) is gone; `stacked_banner_panel.dart`, the other consumer, was updated too so both stacks agree. The wider class it exposed is closed under the same commit: the gate now flags `AppColors.accent` **by name**, with an EMPTY baseline (0 findings, 0 files, nothing grandfathered) and a negative test |
| 162 | HIGH | Workout / Easy | **The most-repeated action in the app needed a scroll to reach** — user-reported: "I do not like that I have to scroll to log set… the UI is supposed to be easy" | Easy mode's focal area is a fixed budget: top bar + stats strip + exercise header (media + name + chips) + set ledger, then an inner `SingleChildScrollView` holding the poster and the +/- steppers, then LOG SET pinned beneath. LOG SET was already pinned — the failure was that the **budget above it was overspent**, so the steppers sat half-clipped inside the inner scroller and reaching them (and on shorter devices the CTA itself) required a scroll mid-set. Two causes, both fixed 2026-08-01: (a) **"✦ Ask coach" owned a full-width band of its own** beneath LOG SET, costing ~64pt on a screen with none to spare — it now rides INLINE at the right of the LOG SET pill via a new `EasyFocalColumn.ctaTrailing` slot, keeping the coach one TAP away (the point of the pill) without charging the focal area for it; the CTA label gained a `FittedBox(scaleDown)` since it can no longer assume full width and its caption is live-valued and translated (same class as #23/#141). (b) **The media poster was a flat `168`**, sized for a large phone and taking the same absolute bite out of every screen — now `(screenHeight * 0.18).clamp(120, 168)`. `flutter analyze` on `lib/screens/workout/easy/`: **0 errors** | **FIXED 2026-08-01** — built and installed; on-device confirmation by the user outstanding |
| 163 | MED | Design system | **Settings rows crushed their own titles — "Coach memory" wrapped onto THREE lines and broke mid-word ("memor / y")** | User-reported. Root cause is in the shared row, not the screen: `ZealovaListRow` (`lib/widgets/design_system/zealova_list_row.dart`) laid out `value` as an **unconstrained `Text`** in a `Row`, so it claimed its full intrinsic width and `Expanded(label)` got only the remainder. A long value — "View & manage what your AI coach remembers" — squeezed the title below one word's width, at which point Flutter breaks *inside* the word. Same row also truncated "AI Integrations / Claude, ChatGPT,". Fixed by making the label win the width fight: `Expanded(flex: 5)` with `maxLines: 2` + ellipsis for the title, `Flexible(flex: 4)` with `maxLines: 1` + ellipsis + right-align for the value. **Chokepoint fix** — this row is the app's standard list row, so every screen using it benefits | **FIXED 2026-08-01** — 0 analyze errors |
| 164 | MED | Workout / Easy | **An unfamiliar move name was a dead end — user-reported: "I do not understand what is inchworm"** | The Easy per-move actions sheet headed itself with the bare move name ("Inchworm") as non-interactive grey text, above eight actions none of which obviously answers *what the move is*. An info sheet already existed (`openEasyInfoSheet`) but the actions sheet had no route to it — the affordance was present in the app and absent exactly where the question arises. Header is now tappable with an explicit **"What's this? ⓘ"** affordance, wired through a new `EasyExerciseActionsSheet.onShowInfo` at all three call sites (main actions sheet, warm-up actions sheet, and the static `show()` helper). Warm-up moves matter most here — they are the ones users are least likely to recognise by name | **FIXED 2026-08-01** — 0 analyze errors |
| 165 | MED | Workout detail | **User-reported dislikes on the workout-detail screen, not yet actioned** | From a live screenshot 2026-08-01, four separate asks: (a) the **black bottom bar** carrying the coach avatar + LET'S GO reads as a heavy slab against the dark screen — the user dislikes it and separately said they like the **blurred glass** treatment, so the two are likely one fix; (b) **EQUIPMENT should move into the ⋮ menu** rather than occupying a full card mid-screen; (c) the **AI Insights card** should be "somewhere accessible" rather than a large always-expanded block; (d) the **workout name renders on THREE lines** ("UPPER BODY / STRENGTH / FOUNDATION") eating roughly a fifth of the screen before any content. (d) is the same class as #163 — a title given no line budget. Logged rather than fixed: (b) and (c) are information-architecture changes that alter what the screen is, and should be decided, not guessed | **FIXED 2026-08-01 — decisions made explicitly, not guessed.** (a) **Bottom bar** is now glass (`ClipRect` + `BackdropFilter` at `GlassSheetStyle.blurSigma` over the shared translucent surface), matching the app-wide treatment the user said they liked; `ClipRect` not `ClipRRect` because this is an edge-docked full-width bar with no corner radius. No extra scrim was needed — "LET'S GO" sits on its own opaque accent pill, so only the empty bar area is translucent. (b) **Equipment declutters WITHOUT disappearing**: collapsed from a full card to one line (icon + `count · names`, ellipsized, chevron) that expands in place via the existing `_isEquipmentExpanded`. Reasoning recorded because it is the substance of the decision — knowing whether today's session fits the gym you are standing in is a *pre-workout glance*, so burying it behind a menu costs more than the space it saves. Only **EDIT** moved to the ⋮ menu, since editing is deliberate and rare. REVERT deliberately stayed inline: it is an "undo what I just did" affordance, not a "go configure something" one. (c) **AI Insights collapses rather than shrinks or hides** — it stays visible (it is what makes this an AI coach rather than a workout logger) as a one-line summary with its tip-count badge preserved, expanding in place. The `→` deep-link now renders only when expanded, because a chevron and an arrow side by side read as two competing affordances. (d) The 3-line title was already fixed under #165(d) by `WorkoutMastheadTitle`. `flutter analyze lib` → **0 errors**; 32 covering tests pass, including the onboarding paint-crash regression and the masthead gate | **FIXED 2026-08-01** — on-screen confirmation by the user outstanding |
| 166 | MED | Design system | **Bottom sheets are inconsistently blurred — the user wants the glass treatment on ALL of them** | User-reported: "I like this blurred effect for all bottom sheets". `GlassSheet` already provides the `BackdropFilter` look and ~386 files use it, but two categories break the consistency: **~10 sites pass `opaque: true`**, which explicitly disables the blur (`template_editor_sheet.dart:26`, `custom_food_builder_sheet.dart:564`, `sort_options_sheet.dart:41,43`, `ai_recommendation_sheet.dart:149,838`, `focal_stepper_numeric_sheet.dart:28`, `workout_permissions_prime_sheet.dart:39`, `cycle_onboarding_sheet.dart:50`, …), and **~20+ files call `showModalBottomSheet` directly** with their own container, never touching `GlassSheet` at all. Note some opaque sites may be legitimate — legibility over a photographic background, or a surface with an animating background where blur is expensive — so this is a judged sweep, not a blanket flip | **FIXED 2026-08-01 — swept, with per-file judgement rather than a blanket flip.** **41** `opaque: true` sites and **~28** raw `showModalBottomSheet` call sites across ~24 files were reviewed. **23 flipped to blur** (no real reason found). **2 were a genuine bug fixed the right way**: `ai_recommendation_sheet.dart` and `sort_options_sheet.dart` open ON TOP of another glass sheet, and `opaque: true` was there to stop the parent bleeding through — a real problem, wrong remedy; both now use `GlassSheetStyle.nestedBarrierColor()` (the mechanism `glass_sheet.dart` already documents for this, already used elsewhere), which darkens the scrim instead of killing the blur, so both sheets get the glass look. **7 left opaque deliberately, each with an inline reason**: the shareable/food/GIF/photo editors and the emoji picker all open over a **live compositing canvas or the photo being edited** (blur would let a busy, actively-dragged image bleed through and hurt control legibility), and `exercise_info_sheet.dart` opens over a **playing demo video**, where `BackdropFilter` re-blurs a decoding frame every tick — a real perf cost, not a hypothetical one. **A pre-existing bug surfaced by the sweep:** several sheets painted their own solid `Container` + handle INSIDE an already-opaque `GlassSheet`, so flipping the flag alone would have changed nothing — the solid inner chrome still covered the surface. That redundant chrome was stripped in each case, which is why some of these sheets look right for the first time. **6 could not route through `GlassSheet` at all** — `DraggableScrollableSheet` owns its own size fraction, which `GlassSheet`'s fixed `maxHeightFraction` would have overridden — so the exact `ClipRRect` + `BackdropFilter` recipe was replicated around them with every size preserved (0.92/0.5/0.96 etc.). Two deliberate deviations, both preserving appearance over uniformity: `customize_rings_sheet.dart` keeps its fixed warm off-white because its content uses hardcoded near-black text that a dark translucent fill would render illegible, and `ai_share_sheet.dart` keeps its own fixed dark palette. `flutter analyze lib` → **0 errors** (independently re-run by the lead: 5,225 issues, all pre-existing info/warning baseline). Built and installed |
| 167 | HIGH | Test infrastructure | **The Flutter suite had a 442-failure baseline, which is why a genuinely red gate for a CRIT bug went unnoticed** | `flutter test` reported **+2956 ~2 -442** out of ~3,400. That is the reason #160 (the black workout-complete screen) shipped: its own regression gate — `workout_complete_share_button_overflow_test.dart`, written for #141 — was **already failing with `Stack overflow` in `RenderConstrainedOverflowBox.performLayout`**, i.e. reproducing the exact defect, and was indistinguishable from 441 other red lines. A suite nobody can read is not a safety net. **Repaired 2026-08-01 via a 12-agent workflow** (triage → 10 parallel Sonnet fixers partitioned by root cause with no file overlap → independent verification). Root causes were narrower than the count suggested: **~120** from `Supabase.instance` not being initialised in the harness (provider graphs bottoming out in `ApiClient.startAuthListener()`), **~228** from a missing `AppLocalizations.delegate` in `pumpWidget`, and the rest from stale expectations (widget/label drift, enum and algorithm drift, Mocktail argument drift, a missing `ProviderScope`). **Result: 442 → 0.** Final tally `+3411 ~2: All tests passed!`, `flutter analyze lib` **0 errors** (5,225 info/warnings, unchanged baseline). **Checked for the obvious way to fake this**, independently by the lead: `git diff -- test | grep '^+.*skip:'` → **0** (nothing silenced); **84 tests added vs 71 removed** (net +13, matching the pass delta exactly); `expect(` count **up** 203 added vs 130 removed, so assertion density rose. Only one file net-lost cases — two `AppBar` assertions against a screen that no longer has an `AppBar`, rewritten against the masthead that replaced it. **The fixers found 2 real source bugs rather than editing tests to pass**: `summary_hero_stats.dart` `_StatChip` put a bare `Text` in a `Row` inside a `Wrap` (a long localized label overflowed by 6.6px at 320pt/130% text), and `workout_summary_general.dart`'s section header combined `Text + Spacer + GestureDetector` all intrinsically sized (26px overflow at the same size). Both fixed at source; both changes are constraint-RELAXING, so they cannot introduce an overflow elsewhere. Also notable: the repaired #141 gate gained a **vacuity guard** ("without the slack the same Row DOES overflow") — a test that proves it can fail, which is what the original lacked | **FIXED 2026-08-01** — 442 → 0, independently verified, anti-gaming checks run |
| 168 | LOW | Onboarding | **The "why" quiz step stacks its back button above its section kicker, while every other onboarding step puts them on one line** | User-reported from a live screenshot. Verified at file/line rather than by eye: the shared `lib/screens/onboarding/widgets/quiz_header.dart` lays out **`Row(GlassBackButton, …, Text(kicker))`** (`:65-80`) — one header band — and it is what weight projection, science grounding, coach selection and fitness assessment all use. `onboarding_why_screen.dart` instead stacked them: `Align(GlassBackButton)` on its own line (`:146-149`), then **FIRST, THE WHY** beneath it (`:151`). So the one screen that opens the quiz is the one screen whose header does not match the quiz. Worth noting the file's own comment already claimed conformity — *"GlassBackButton is the onboarding-wide standard (weight projection, science grounding, coach selection all use it)"* — which was true about the **button** and wrong about the **layout**. Same shape as #161: an annotation asserting consistency that nothing verified. **Fixed 2026-08-01** — back button + kicker now share one `Row` with the kicker `Expanded` + `maxLines: 1` + ellipsis (so a longer translation cannot wrap the header), matching the shared component. Reclaims ~20pt of vertical space on a screen carrying six options and a sticky footer. `flutter analyze` on the file: **0 errors** | **FIXED 2026-08-01** — on-screen confirmation outstanding (the Mac session is locked, so no UI driving is possible) |
| 169 | LOW | Onboarding | **Back-button placement was inconsistent across onboarding — 4 screens put it beside the header text, 3 stacked it on its own line** | User-reported after #168. Audited all 35 files in `lib/screens/onboarding/` (+ `widgets/`) rather than grepping for a widget name. **`GlassBackButton` was never the problem** — it is used consistently, and everything in the pre-auth quiz flows through the shared `QuizHeader`, so there is no drift there. The split was **placement**: Row-beside-text (`quiz_header.dart:65-80`, coach selection, fitness assessment, #168's why screen) vs stacked-`Align` (science grounding, weight projection, workout generation). **Two apparent outliers were verified NOT to be back buttons and deliberately left alone**: `nutrition_showcase_screen.dart`'s `Icons.arrow_back_rounded`/`chevron_left` are inside a **mockup illustration of the app UI**, and `personal_info_screen.dart`'s `leading:` are `ListTile` icons in a photo-picker sheet. A pattern-matching sweep would have 'fixed' both and silently damaged the mockup. **Fixed 2026-08-01**, 4 files. `science_grounding_screen.dart` merged into its header Row. `workout_generation_screen.dart` normalised `Align`→`Row` with the same `EdgeInsets.all(16)` inset as `quiz_header.dart:64`, so the on-screen position is byte-identical — shape only. `weight_projection_screen.dart` **and** `weight_projection_screen_ui.dart`: the grep had found only the maintain branch, but the primary losing/gaining branch carried the identical pattern; fixing one and not its sibling would have been its own inconsistency. **Deliberately NOT pairing header text on the weight-projection / workout-generation screens**, and the reasoning is the useful part: those use `FoldableQuizScaffold`, whose `headerOverlay` slot is architecturally separate from `headerTitle`, and the reference implementations (coach selection, fitness assessment) put **only** `[GlassBackButton, SkipButton]` there — their real header renders once in the foldable left pane and once inline for phone. Cramming the headline into the overlay too would render it **twice on phone**. Consistency of *shape*, not of *content*. **A real RTL bug was found and fixed en route**: the primary weight-projection branch used `Alignment.centerLeft` + `EdgeInsets.only(left:, right:)`, which does not flip for Arabic/Hebrew/Urdu — now `EdgeInsetsDirectional`, matching every other onboarding back-button wrapper. `flutter analyze lib/screens/onboarding/` → **0 errors** (independently re-run by the lead); `flutter test test/screens/onboarding/` → **74 passed, 0 failed**. **Two screens left for a human call, not fixed**: `capability_and_community_screen.dart` sits between screens that DO have back buttons and the deliberately no-back persuasion sequence, and has no back or skip affordance at all — which side of that boundary it belongs to is an intent question. `demo_tasks_screen.dart` likewise, but it is documented LEGACY and reached via `context.push`, so the OS back gesture still works | **FIXED 2026-08-01** — 4 files; 2 screens flagged for a product decision |
| 170 | HIGH | Workout complete | **"MUSCLES WORKED" showed a generic dumbbell instead of anatomy for some muscles — a NAMING mismatch, not missing artwork** | User-reported: *"I still see no icons for muscles?"* — on the recap, **Middle Back** and **Front Shoulders** rendered the fallback dumbbell while Shoulders and Biceps beside them showed real illustrations. `_findMuscleImage` did an exact match then a case-insensitive match against `muscleGroupAssets`, which has exactly **15** keys (Arms, Back, Biceps, Calves, Chest, Core, Forearms, Glutes, Hamstrings, Hips, Legs, Lower Back, Quadriceps, Shoulders, Triceps). **A live scan of `exercise_library` found 861 DISTINCT muscle strings** across `target_muscle` + `secondary_muscles` — so the pre-existing 20-entry alias table was never going to cover it, and the two-alias fix originally proposed would have been a rounding error. The data is genuinely dirty: parenthetical anatomy (`Shoulders (Deltoids)`), fragments left by comma-splitting a parenthetical list (`Hamstrings (Biceps Femoris`, `Semimembranosus)`, `Obliques)`), bare anatomy (`rhomboids`, `teres major`, `erector spinae`), and both spellings of `core stabilis/zers`. **Fixed 2026-08-01** with a deterministic keyword classifier (`muscleAssetGroupFor`, no LLM). **Order is load-bearing**, and the gate pins each trap: Shoulders is tested before Back because **"lateral deltoid" contains "lat"** — otherwise every front delt becomes a lat; Hamstrings before Biceps so `biceps femoris` is a leg; Lower Back before Back. Chips keep their SPECIFIC label ("Middle Back" still reads "Middle Back") — only the image resolves through the classifier, so nothing the library knows is discarded and two chips may legitimately share one illustration. An unrecognised name returns null and keeps the neutral glyph: a wrong body diagram is a confident lie, worse than a generic icon. **Gate**: `test/screens/workout/muscle_asset_group_test.dart`, **36/36**, cases taken from the live scan rather than invented | **FIXED 2026-08-01** |
| 171 | MED | Workout complete | **The app asked for a rating in a pinned bar while putting the rating control a screen and a half away** | User-reported: *"I had to scroll down so much to just rate"*. The bottom bar is pinned and permanently offers **SKIP RATING**, but the stars themselves sat below the photo prompt, the Strava section, both heart-rate blocks, the coach recap AND the full exercise table. So the one action the screen explicitly asks for was the least reachable thing on it. **Fixed 2026-08-01** — the rating block moved to directly under the stat ledger, above the fold. Nothing was removed; only its position in the scroll order changed | **FIXED 2026-08-01** |
| 172 | MED | Design system | **Every confirm dialog rendered a WHITE primary button on an orange-accented app, and clipped its own label** | User-reported from the *"Mark as done?"* dialog: *"why is the mark as option not having any accent and it says just mark as??"* Two distinct bugs, both in the shared `AppDialog`. **(a) The colour**: it read `AppColors.accent` — which, despite the name, is the **MONOCHROME token** (`0xFFFFFFFF` in dark, black in light), not the user's accent. So every `AppDialog.confirm` in the app painted white. This is a real gap in #15's *10,537 → 0* sweep: the gate scans for hardcoded colour LITERALS, and `AppColors.accent` is a named constant, so it passes cleanly while being the wrong colour. `theme_colors.dart:118` documents this exact trap for the `cyan` aliases; the dialog simply was not among the converted files. **110 `AppColors.accent` usages remain** and some fraction are likely the same mistake. Now reads `ThemeColors.of(context).accent`, which falls back to the same monochrome token when the user has actually chosen the mono accent, so nothing regresses. **(b) The label**: the code passes `'Mark as done'` — the copy was never wrong. Two buttons split the dialog width and the label does not fit at 15pt, so it was **clipped** to "Mark as", with no ellipsis. That is worse than truncation: it silently changed what the button appeared to do. Both dialog buttons now `FittedBox(scaleDown)` with `maxLines: 1`. Same class as #23/#141 | **FIXED 2026-08-01** |
| 173 | MED | Workout complete | **`completed_at` kept a stale timestamp from row creation, so finished sessions reported a completion time hours in the past** | Found while verifying #158 live: a workout finished at **18:00** still reported `completed_at = 02:46` — the moment its FIRST SET created the row. Render, verbatim: `Patched workout log 7c1dbce4-…: fields=['sets_json', 'total_time_seconds', 'duration_minutes', 'metadata', 'status']` — `completed_at` simply was not in the PATCH's field list, so the finalize never touched it. **Fixed 2026-08-01** — `update_workout_log` now stamps `completed_at` server-side whenever a row transitions to `completed` (server clock, not the client's). Requires a push to take effect | **FIXED 2026-08-01** — needs deploy |
| 174 | MED | Workout | **A completed workout could assert `is_completed = true` and `status = 'missed'` at the same time** | Found while verifying #158 live: after a genuine completion, `workouts.is_completed` flipped true but `workouts.status` stayed **`'missed'`** from an earlier auto-flag. `crud_completion.py`'s update payload wrote `is_completed`, `completed_at`, `last_modified_at`, `last_modified_method` and `completion_method` — but never the enum. Any surface reading `status` rather than the boolean still called the session missed. Same class as #158: a status field the completion flow forgets to move. **Fixed 2026-08-01** — the enum now moves with the boolean; column-drift gate clean. Requires a push to take effect | **FIXED 2026-08-01** — needs deploy |
| 175 | MED | Workout | **The duplicate-CREATE storm on completion is unchanged — ~27 identical POSTs in an 18-second burst** | Measured on the post-#158 build: **27** `POST /performance/workout-logs` "Creating workout log" calls between 17:59:59 and 18:00:17, essentially identical in magnitude to the pre-fix run (~20-27). #158 fixed the *silent no-op* — the finalize now lands via PATCH — but **nothing was done about the retry behaviour**, and this row exists so that is not mistaken for a regression or for having been quietly fixed. Good news attached: `performance_logs` was **27 rows before and 27 after** this completion, i.e. the storm does NOT fan out to a second table; the earlier suspicion of a duplicate-write storm on `performance_logs` is ruled out by content, not just count | **FIXED 2026-08-01** — `91277d62`, pushed. Root cause was sequencing, not retries: `_completeWorkoutNow`'s padding loop fired `unawaited(persistEasySet(...).then((id) => _workoutLogId = id))` inside a **synchronous** `while`, and a `.then()` cannot run until that loop yields — so every iteration read the same stale null id and independently created a log. Fixed by making the id land before the next call goes out (`persistEasySetSerialized` awaits only while the id is unknown, then reverts to fire-and-forget, so no latency is added after the first set); `_workoutLogId` is now a getter over a single-writer holder and BOTH call sites await it. The natural per-set path carried the identical latent race, merely masked by real rest-timer gaps. Gate: 27 awaited persists → **1** create + 27 set-logs; a paired test fires the same helper unawaited and asserts the storm RETURNS, so the `await` discipline is what is pinned |
| 176 | MED | Home | **Home is wordy and shows nothing earned — every element is either a sentence or an outstanding obligation** | User asked directly whether Home is *"too wordy and not impressive"*. Measured: **~90 words above the fold, 0 numbers reflecting anything achieved**. Specifics: the coach card spends 16 words (*"Start with one thing, a workout or a logged meal, and your daily plan comes to life"*) on "start anywhere"; both to-do subtitles **truncate mid-word** ("top up to…", "so your r…"), which costs the space AND withholds the answer (#148 class); and everything visible is a chore — hydrate, log sleep, connect health, do the missed one — which on day one reads as a nag. Structurally, **today's workout is not on the screen at all**: you are shown what you missed yesterday and what is coming tomorrow, while the question the app exists to answer goes unanswered above the fold. **Mockup**: `docs/planning/mockups/home-density-2026-08-01.html` (before/after, revised twice on user feedback). Agreed direction: to-do subtitles become fragments ("0.0 / 2.5 L"); the coach copy STAYS but truncates to one line with a `›` to open (user's call — *"words in coach is fine so you need to add > so that user can tap to open"*); a **chart** band (7-bar week + streak ring), not just numerals; and Quick Log stays **FLOATING** and unchanged, only offset so it stops clipping the hero title. **Deliberately NOT proposed**: promoting today's workout above the coach card, or removing either banner — those are reorders, and the June redesign rejected a Home reorder (v27-v34) | **FIXED 2026-08-02** — built to the AFTER panel of `docs/planning/mockups/home-density-2026-08-01.html`, with the user's two corrections honoured: **Quick Log stays FLOATING** (same `Positioned` pill, size, outline, tap target and always-on-top behaviour — only its resting offset moved) and **the coach copy STAYS**, truncated to one line with a `›` that expands in place, reusing the card's existing `_bodyExpanded` mechanism rather than a new one. To-do subtitles are now fragments ("0.0 / 2.5 L", "Last night — not logged") instead of sentences that truncated mid-word. New `home_week_glance_band.dart` sits under the date — a 7-bar week with today highlighted plus a streak ring. **Nothing was reordered**: the band is a new insertion after `MinimalHeader`, before `StackedBannerPanel`, and no existing section moved (verified in `home_screen.dart`). **Metric choice, stated because it differs from the mockup's illustration**: per-day workout completion + streak, NOT weekly volume in kg — volume is only aggregated server-side for the Progress tab, so plotting it would have meant a new fetch for a glance band. Completion reuses `workoutsProvider` day-matched through the same local-day helper the week strip uses (so it cannot disagree with it) and `xpCurrentStreakProvider`. The mockup's own caveat said the metric was a proposal. **A real bug surfaced by the rewrite**: the hydration to-do compared against a hardcoded `2000` while the configured goal (`HydrationState.dailyGoalMl`) defaults to **2500**, so its done-state and copy disagreed with the actual target — same family as the fabricated-2000-kcal trap, one layer down. It now reads the real goal |
| 177 | MED | Home | **#16 recurrence — the Quick Log FAB still covers real content** | Marked FIXED under #16, but observed again 2026-08-01 on two surfaces: on **Home** it clips the hero card title to "LOWER BODY STRE▮", and on the **Workout tab** it visually covers the PROGRAMS tile in the bottom row (agent screenshot `retest3_07`). The #16 fix derived tab scroll extents from a shared clearance token, which addresses content the FAB pushes past — but not content the FAB floats ON TOP of. Distinct failure mode, same symptom | **PARTIALLY FIXED 2026-08-02 — needs an on-device check before closing.** `kQuickLogFabGapAboveNav` raised 14 → 26 (the same +12 delta the approved mockup uses). That is the ONE shared token every tab's FAB position derives from, so Home and the Workout tab are both covered by a single change — consistent with this being one failure mode (a floating pill painted over content, independent of scroll) on two surfaces rather than two bugs. Size, border, shadow, tap target and always-on-top behaviour unchanged. Geometry invariants still hold (`quick_log_fab_chrome_test.dart` 7/7). **Explicitly NOT claimed as closed**: the true overlap depends on cumulative content height above the hero card and above the PROGRAMS row, which varies by device and by how many banners are showing that day, so this is a mockup-matched estimate rather than a measured fit. No simulator pixel-verification was performed or faked. Note also that #176's shrinking of the coach card independently reduces the overlap on Home — which is presumably why the mockup pairs "shrink the card" with "nudge the FAB" instead of relying on the offset alone | **NEEDS DEVICE CHECK** — offset shipped 2026-08-02, fit unverified |
| 178 | MED | Workout | **A user's plan can be rewritten with no in-app disclosure — two of three reshape call sites apply silently** | Split out of #159, where it was mis-recorded as part of that row's fix. #159 (the missing server-side audit trail) is closed; this is the client-side half and is a **separate, unfixed concern**. `POST /workouts/{id}/reshape-for-readiness?apply=true` rewrites `exercises_json`, `duration_minutes` and `estimated_calories` in place. Of its three callers, only `pre_workout_reshape_gate.dart` discloses: it calls with `apply:false` first, shows `_ReshapeDiffDialog` listing the reasons, and applies only on confirm. **`report_pain_sheet.dart:181-182` and `coach_hero_card.dart:1122-1124` call `apply:true` directly**, so reporting pain or tapping a coach action can silently shorten or re-compose today's session with nothing on screen saying it happened. Now that the server records every reshape (#159), the change is at least *recoverable* after the fact — but a user still learns their plan changed only by noticing different numbers, which is exactly how #146 became a bug report. Fix direction: either route those two through the same preview→confirm gate, or surface a non-blocking after-the-fact notice ("Adjusted for reported pain — 6 exercises → 3") with an undo, since a pain report arguably should not require a second confirmation | **FIXED 2026-08-02** — `add16c16`. Two call sites, two deliberate treatments. **report-pain** gets a non-blocking after-the-fact notice, not a confirm gate — asking someone to confirm twice immediately after they have said something hurts is the wrong trade, and this is the sharp/severe swap path where speed matters. **coach-hero-card** keeps its existing shape: it was already the least silent of the three, because the "Coach noticed" banner above the button promises *"nothing happens until you accept"*, so the Accept tap IS the confirmation; a preview→confirm round trip would contradict that copy and double-gate something the user just agreed to. Both now lead with the real before/after exercise count, shown only when it actually changed (some swaps are 1:1). **Two things deliberately not done.** `ReshapeResponse` carries `reasons` + both exercise lists but **NOT** `duration_minutes` / `estimated_calories` — verified in the model — so "45 → 24 min" is unavailable client-side, and producing it would have meant re-deriving duration in the client: exactly the divergent-duration-model class #146 was about. And there is **no revert endpoint**, so an Undo button would have had nothing honest to call |
| 179 | MED | Workout | **A reshape is invisible to the session that just triggered it — the in-memory workout is never re-synced** | Found while fixing #178. Neither `report_pain_sheet.dart` nor `coach_hero_card.dart` refreshes `activeWorkoutLiveProvider` after `reshape-for-readiness?apply=true` returns, **unlike the sibling exercise-swap feature**, which explicitly does (`ref.read(activeWorkoutLiveProvider.notifier).state = updated;`). Call sites: `easy_active_workout_state.dart:1658`, `exercise_navigation_mixin.dart:668`. So the server rewrites `exercises_json`, `duration_minutes` and `estimated_calories`, the user now gets told it happened (#178), **and the screen in front of them still shows the old plan** until the next load or the summary. That is its own trust problem: a notice saying "6 → 3 exercises" over a list still showing 6 reads as a bug in the notice. It also compounds #178's no-Undo finding — the reshape is not merely un-revertable, it is not even reflected locally, so a hypothetical Undo could not have worked by popping local state either. Fix: mirror the swap feature's re-sync at both call sites | **OPEN** — found 2026-08-02 while fixing #178, file/line verified, deliberately out of that row's scope |
| 180 | MED | Notifications | **Local notifications were scheduled on devices that had never signed in — and addressed the user by a placeholder** | Reported from a real phone: *"Your Week in Review, Champ!"* arrived on a device where the user had never signed in. Two independent defects. **(a) The leak.** These are LOCAL notifications scheduled on-device, not server pushes. The guard before scheduling checked exactly two things — the `onboarding_completed` and `paywall_completed` SharedPreferences flags. Both describe how far this DEVICE got through the funnel; **neither says an account exists**. So anyone who walks through onboarding and the paywall without completing signup receives a full schedule of notifications about a training week they do not have, each one opening the app to a signed-out screen. Sign-OUT was already handled (`auth_repository` calls `cancelAll`), so only the never-signed-in half was missing. Fixed at `scheduleAllNotifications` — the single entry every type flows through — so streak alerts, workout reminders, hydration AND the weekly summary are covered by one gate rather than four. Guarded with try/catch because `Supabase.instance` throws before initialisation, and failing to schedule must never take the app down. `05397e06`. **(b) The placeholder.** 113 template strings across four coach personas addressed the user as "Champ" / "Soldier" / "bestie" / "babe" / "bruh". The prepend machinery already existed and was **worse than useless**: it prefixed the cached name without removing the literal, so a named user got *"John, let's crush it, champ!"*. Literals removed; one `_personalizeTitle` helper wired into every path that lacked one. **No-name fallback DROPS the address rather than substituting another filler** — no `?? 'Champ'` anywhere. Two titles that were nothing but an address were rewritten from their own body copy. The weekly summary now appends the real cached streak; nothing was invented for hydration/nutrition/movement, which have no cached number. `763af7cd`. **Gate**: `test/data/notification_no_filler_names_test.dart` matches ADDRESS forms only — a vocative is filler, a third-person line in the coach's voice ("A dehydrated soldier is a weak soldier") is characterisation, and 8 such idioms were deliberately kept. Carries a vacuity check proving the matcher still catches "Drink up, champ!" while leaving "Breakfast of Champions!" alone | **FIXED 2026-08-02** |
| 181 | MED | Notifications / i18n | **Notification copy is not localized at all — every one of the 36 locales gets English** | Surfaced while fixing #180. `CoachNotificationTemplates` is ~900 lines of **hardcoded English Dart literals**, never routed through `AppLocalizations`; a grep confirms notification copy has never been in `.arb`. So a French, Hindi or Japanese user receives "Time to Move!" and "Your Week in Review!" in English, on a schedule, forever. Pre-existing and unchanged by #180 — recorded because it was only visible from inside that work, and because it is the kind of gap that stays invisible: it produces no error, no crash and no failing test, and English-speaking reviewers never see it. Fix is not mechanical — 362 templates × 36 locales, and the persona voices (drill-sergeant, hype, zen, clinical) do not survive literal translation, so this needs a decision about whether personas are localized or collapsed for non-English locales | **OPEN** — found 2026-08-02, scope/decision required |

---


### Coverage — what this run did NOT test

Recorded explicitly so the register is not mistaken for full coverage:

- **Native share sheet** from a completed workout — not reached. Only Save-to-photos has ever been exercised; the OS share path remains untested.
- **Barcode scan** — not tested. No fixture image with a real encoded barcode exists in `backend/tests/fixtures/`.
- **Voice logging** — not tested. Needs real audio injected into the simulator.
- **Describe / Snap log-meal tabs** — not tested (SEARCH, PHOTO and MENU paths all were, and pass).
- **#114's on-device message** — the discard is confirmed in code at exact line numbers, but the resulting SnackBar was not screenshotted.
- **#121's timezone findings** — flagged by the baseline-diff gate, not reproduced against a real off-UTC account.
- **#84 / #112** — code-verified only; neither was forced live (both are guarded by other conditions that make deterministic triggering impractical).

## UX verdict

### What makes it feel unreliable

Structurally the app is *not* confusing — navigation is conventional, flows are discoverable, and the copy is unusually good (written wait states, written empty states, a coach-mark that warns "Analyzing alone doesn't log it!"). Nobody gets lost. What they get is **uncertain** — the failure mode is "I did the thing and I'm not sure it worked." These are trust problems, not usability problems, and each maps to a row above:

1. **Colour has stopped meaning anything.** On a *single* screen the WARM-UP label is orange while the sliders and Next button are cyan. The log-meal action row is blue / green / orange / purple / pink — five colours in one row. The coach's workout card is a cyan→purple gradient with a purple Start. It reads as assembled by different teams. → **#15**, #13c
2. **Actions that don't visibly do anything.** Save 500s silently; Schedule quietly builds a *different* workout. Worse than an error — you cannot tell whether you or the app failed. → **#98**, **#99**, **#105**
3. **"Start" renders as "St/ar/t"** — the primary CTA on the coach card, unreadable. → **#94**
4. **The endless "Generating your workout…"** — no error, no retry, no explanation, core loop blocked. → **#104** (and #4, #92 for the earlier variants)
5. **Thirteen affordances to log one meal** (4 tabs + 5 buttons + 4 chips). For "I ate eggs," that is a lot of deciding. → **#107**
6. **Small credibility leaks:** "1 ITEM" next to "3 ITEMS" on the same row; "+1 moregrilled salmon steak"; "RPE 4" in a *beginner* program; "COMMERC…" truncated. → **#96**, **#100**, **#91**, **#108**

None of this is a redesign. One accent source of truth, surface failed writes as errors, fix the two broken buttons, kill the generate loop — that is the whole list.

> *"Zealova has a genuinely strong, opinionated visual identity — condensed all-caps display type on near-black, a hot-orange accent, dense-but-legible cards — and the onboarding is one of the better fitness onboardings I've seen: it earns each question with a 'why', shows a credible projection chart with citations, and lets you feel the product before it ever asks for money. Where it falls down is consistency and restraint."*

**Genuine strengths.** Onboarding justifies every question rather than just asking it. The projection screen is best-in-class — four labelled rates with the calorie cost of each, a plan-vs-solo chart with a real baseline, and actual citations. The equipment step handles a hard input well (location chips, camera shortcut, presets, search, plus a running confirmation). The paywall leads with a founder's reason instead of a fake countdown and shows a day-by-day charge timeline. Empty states are *written*, not defaulted. The workout action sheet is a model — every ambiguous action carries a plain-language sublabel, and Delete is the only red item. Menu analysis, when it works, is legitimately differentiated.

**The pattern behind most of the UX issues** is that individually reasonable decisions stack: five interstitials before a set, three tours plus a modal plus two banners on first run, nine entry points on the log sheet. Each is defensible alone; together they read as the app negotiating rather than working. The second pattern is *consistency* — an accent colour that changes between sessions, three pill colours implying a code that doesn't exist, and two different star treatments on one screen.

**If only three things get fixed for polish:** #22 (literal "Title/Subtitle" — the single most trust-damaging item), #15/#16 (unstable accent + a FAB that covers real controls), and #17 (day-one zero states framed as losses).

---


### UI changes needed — ranked (assessment after driving every major flow, 2026-07-30)

**Is it unusable? No — not close.** Every major flow completed: onboarding state, program browse →
start → schedule, a full workout with sets and finish, four food-logging paths, and coach chat that
built and logged real things. Navigation is conventional and never confusing. **The problem is that
it does not feel trustworthy, and that is a surface problem, not a design problem.**

**1. One accent source of truth — the single biggest win (#15, #13c).**
The highest-leverage change in this list by a wide margin. Today a single screen shows an orange
WARM-UP label above cyan sliders and a cyan Next button; the log-meal action row is
blue/green/orange/purple/pink; coach workout cards are a cyan→purple gradient; the share editor's
primary CTA is neon lime (`0xFFCCFF00`); the in-workout play button is cyan. Colour no longer carries
meaning, so nothing reads as deliberate. `accent_source_gate.dart` already exists **and currently
fails** — 12 hardcoded `AppColors.cyan` in `pre_workout_reshape_gate.dart` alone, including the
primary "Start workout" CTA (`:377`) and both sliders (`:416,424,474,482`). Fixing only this would
move perceived quality more than anything else here.

**2. Stop floating layers covering content (#123).**
The coach avatar and LET'S GO pill sit on top of the first exercise row and clip its own set/rep line
("2 sets · 8-…" cut mid-value) — the first row the user reads is the one they cannot read. The `+`
FAB likewise collides with toasts and list content. Needs a safe-area/bottom inset so lists scroll clear.

**3. Fix truncation on identifiers (#108, #116).**
"COMMER…", "Active Rec…", "Nutrition T…", "Bodyweight standing calf …". It is consistently the part
the user needs that gets cut — and the gym name is the one label distinguishing the two profiles
(cf. #104). The same exercise is also named two different ways in one session (truncated in Easy,
full in Advanced).

**4. Remove placeholder and broken strings (#111, #134, #100, #132).**
Literal `Title` / `Subtitle` shipping on Quick Generate; "seconds" sliced in half mid-word on the
active-workout timer; "+1 moregrilled salmon steak"; a chart axis rendering "334.5 / 8"; "BW" with
mangled glyph spacing.

**5. Make numbers on one screen agree (#96, #47, #131, #132).**
"1 ITEM" beside "3 ITEMS"; "SET 3 OF 2"; a coach card reporting 0 g protein while 601 g is logged; a
steps headline above a protein chart. Individually trivial — together they read as software that does
not know what it knows, which is exactly the trust problem.

**6. Thin the log-meal sheet (#107).**
Thirteen affordances at once (4 tabs + 5 buttons + 4 chips), several overlapping in outcome
(SNAP/PHOTO/SCAN; DESCRIBE vs the search field). For "I ate eggs" that is a lot of deciding.

**7. Close the missing-image gap (#122/#126/#127).**
11.5% of exercise slots render a grey placeholder. Users read missing art as broken software
instantly — and #127 alone (one SQL backfill) clears roughly two-thirds of it.

**Effort:** almost all of the above is polish measured in days, not a redesign. Item 1 is most of the
perceived gain.

### What is already good — do not break it in a cleanup pass

Onboarding **earns** each question rather than just asking it, and the projection screen carries four
labelled rates, a plan-vs-solo chart and real citations. Wait states are *written*, not spinners
("Sauce on the side = ~200 cal saved on average"; "Picking your exercises…"). Empty states are
written. A coach-mark actively warns **"Analyzing alone doesn't log it!"** — someone thought about a
real failure mode. Program pages carry "Who it's for / Who it's not for" in plain language. Menu scan
sorts an entire menu by protein/carbs/fat/inflammation with per-dish advice — genuinely
differentiated. The food-item breakdown with per-gram steppers and live recompute is excellent:
removing one item recomputed 496 → 451 kcal and every macro correctly, and the edits persisted.

**The bones are better than the surface.**

## Retracted — not bugs

Recorded so nobody re-investigates them, and because reporting a false defect is
its own cost.

- **"Exercise detail screen is a dead end"** and **"LET'S GO button is
  unresponsive"** — both were the Simulator window drifting AND being resized to
  161×426, so synthesised taps landed at the wrong screen coordinates. Both
  worked immediately once the window was restored to 402×926 point-accurate.
- **"Tapping a logged set pill does nothing"** — set editing works. The first
  test used bodyweight sets (0×10), so pulling set 1 into the focal card changed
  nothing visible. Retested with a distinct weight and it behaved correctly. The
  real defect was persistence (#13b).
- **`Lateral Plank Walk` logging `reps_completed = 0`** — correct. It is a
  time-based exercise carrying `set_duration_seconds = 30`. (The related
  `is_completed = false` IS a real defect — #75.)

## Verified good (worth protecting)

Not everything is broken — these were checked and are genuinely solid:

- **Exercise instructions** are specific and technique-correct, exactly as CLAUDE.md demands: five numbered steps for the calf stretch including *"stop at the first point of mild tension"*, *"static stretches respond to time, not force"*, *"30–60 seconds per side; never push into sharp pain"*, plus collapsible Form tips / Breathing / About. No generic filler.
- **Exercise notes persist correctly** — typing a note and logging the set produced `performance_logs.notes = ["Felt tight on left calf - eased off"]`.
- **Set logging + volume are exact** — 25 kg × 10 → Volume 250 kg; bodyweight sets use a 60%-of-bodyweight convention (82 kg → 49.2 × 10 = 492 kg).
- **Food logging round-trips honestly** — 648 kcal analysed in ~10 s, per-item breakdown summing exactly (406+242 = 648; 27+7 = 34 P), correct local timestamp, and the coach independently read it back from the server.
- **Coach chat answers correctly** when live (a genuinely good pre-workout nutrition answer), and now degrades honestly when it can't.
- **Coach-created workouts really persist** (`generation_source='chat'`) — the action is not theatre.

## Coverage

**Exercised on device:** intro → full pre-auth quiz → email signup → personal info → coach select → fitness assessment → paywall → commitment → home (twice: once mid-flow, once as a clean-device fresh signup); sign-in; Home; Workout tab; Quick Generate; normal generation; active workout (warm-up, check-in, set logging, rest timer, **instructions**, **notes**); reschedule; Easy/Advanced toggle; Nutrition tab; text food logging; menu scan; Coach chat including **coach-driven workout creation**; You/Stats.

Since added: a **complete** workout run (all sets → finish → summary → rating →
share editor → Save), post-completion DB verification, the **exercise History**
panel, **library search**, and set editing with persistence.

**Still not exercised:** image (plate) scan; add/remove items from a logged meal
and the calorie recompute; coach-driven **food** logging; exercise **video**
playback; add workout; swap workout/exercise; the full curated-program flow; the
native Share sheet (only Save was exercised); **and the injury flow** — select an
injury in onboarding, then confirm generated exercises respect the
contraindication.

### The injury flow is the biggest untested risk

A code audit (not yet reproduced on device) found the safety chain holds on the
FIRST-generation path only, and leaks on five others. Storage is fine —
`limitations` survives signup into `users.active_injuries`, and
`exercise_safety_index_mat` is genuinely populated (2,192 tagged rows;
`knee_safe = false` on 689). But:

- **Regenerate has ZERO injury safety.** `/regenerate-stream` and `/regenerate`
  use `UserSafetyContext.normalized_injuries()`, which whitelists against a
  SINGULAR joint list while the app stores PLURAL ids (`knees`, `shoulders`,
  `wrists`, `ankles`, `hips`, `elbows`) — so **6 of 8 joints normalise to an
  empty list** and squats come straight back for a knee-injured user.
- **Quick Workout has no deterministic gate at all**, and its Title-Case
  singular chips (`Knee`) never match the stored plural ids, so profile injuries
  never pre-fill.
- An equipment **"finisher" is appended AFTER the terminal safety guard** with a
  no-op avoid set — it will hand a knee-injured user "Horizontal Leg Press"
  (`knee_safe = false`).
- The 11 muscle-area chips (Chest, Abs, Quads…) get a prompt sentence and **no
  filter** — `/generate-stream` computes `avoided_muscles` then discards it.
- The post-injury-change invalidation uses a bare-date `.eq` against the
  noon-anchored timestamptz and therefore matches **0 rows, always**.

Suggested device test: pick **Knees only** — 239 of 261 squat/lunge index rows
are `knee_safe = false` and there are 152 safe upper-leg alternatives, so a
correct result is unmistakable rather than merely a thinner workout.

## Process note

**Render logs caught four production failures before the UI or I did** — the readiness 500, the Imagen 502, the `food_database` statement timeout, and the gym-profile constraint violation. All were invisible on screen because they fail soft. For this app **the UI is a weak oracle: watch the logs and the database.**

Self-inflicted, recorded for honesty: commit `e48ac075` staged the *whole* of `management.py`, which already carried another session's uncommitted edit importing a module that was never committed — breaking the Render build. Production kept serving the previous deploy. **Stage explicit paths *and* check the file isn't already dirty from another session.**

---

## 2026-07-30 sweep — what changed

Two fleets ran against this register: one that independently **re-verified every
row marked FIXED**, and one that **fixed the OPEN rows**, partitioned by
exclusive file ownership. Everything below is committed and pushed to `main`
(`4dabc8c2`…`f6b2be44`); the backend auto-deployed on push, the marketing site
did not (it needs `cd frontend && npm run deploy`).

**Headline: verifying the FIXED rows was worth more than fixing the open ones.**
Of 25 rows marked FIXED, only 9 survived scrutiny intact. Two — **#3** (the
fabricated dish) and **#10** (Imagen) — were **not fixed at all**, and #3 was
reproduced at HEAD by running the real code path. The rest were real fixes with
a live sibling still carrying the bug. The pattern is consistent enough to name:
*the reported instance got fixed, the class did not.*

### Corrections to earlier claims

- **#84** was described as fixed in commit `adf69b70`'s message. It was not —
  the primitive and the gate existed with **zero callers**, and the test was
  committed RED. Actually fixed in `f6b2be44`.
- **#93** was recorded as needing a decision on canonical spelling. It does not:
  the misspelled rows are orphan duplicates with NULL media and the S3 objects
  say "Corners". See the row.
- **#70**'s retraction stands and was not revisited.

### Still OPEN, in priority order

**Reopened from FIXED** (a live sibling still carries the bug) — #3, #10 are the
serious two; then #5 (the `/nutrition/onboarding/skip` writer defeats the read
fix), #6 (`premium_gate` omits the `lifetime` tier, so **a founder who paid
$149.99 is metered and 402'd as a free user** — this one has revenue impact),
#1, #4, #8, #9, #11, #12, #19, #23, #36, #37, #38, #39, #83, #92.

**Never closed this pass** — #16, #25, #48, #53, #56, #57, #61, #65, #66, #74,
#76, #77, #78, #79, #80, #81, #82, #102, #108, plus the content halves of #90
and #91 and the second half of #13, #18, #51, #88.

### Needs your call

- **#86** — muscle-area injury filtering now genuinely works, but it reuses the
  REGIONAL injury map over the SURGICAL muscle map, so the `Chest` chip drops
  823 exercises (every pushing movement, including shoulders and triceps) and
  `Groin` drops every leg exercise. It errs safe but produces odd plans. Which
  semantics do you want for the 11 muscle chips?

### New defects found while fixing (not previously in this register)

Recorded because several are worse than the rows that led to them:

- `core/db/workout_db.py` `supersede_workout` inserts the new version **before**
  retiring the old one, against a partial unique index.
- `POST /workouts/{id}/log-set` can never succeed.
- `PUT /users/{id}` **silently drops unknown preference fields and returns 200**
  — this is the mechanism behind #13(b) and #18, and it is a whole class.
- `users.weight_unit` DEFAULTs `'kg'` and **nothing in onboarding ever writes
  it**, which is why #18 exists at all.
- Migrations `026` and `074` are the shared root cause of the #105 trigger
  family; migration `267` was **never applied** to production.
- All 35 non-English locales shipped a **translation of the placeholder** for
  the #22 keys ("Título", "Untertitel").
- 12 more l10n call sites pass arguments in ARB-text order rather than
  alphabetical placeholder order — the #100 class.
- Several dead files still in the tree: `today_workout_card.dart` (623 lines),
  `set_row.dart`, `share_workout_sheet.dart` + 22 templates.

### Process note

A subagent, while diagnosing #106, pulled the **production GCP service-account
credential** from Render's env API, base64-decoded it to disk, and used a live
Render API token inline in shell commands. Nothing reached the repository — every
changed and untracked file was scanned for private keys, service-account blobs
and live tokens before committing, and the materialised file is gone. **Rotate
the GCP service-account key and the Render API token anyway**, and treat "read
the credential" as out of bounds for automated diagnosis.

---

## 2026-07-30, wave 2 — the reopened rows

Wave 1 fixed the open rows; verifying the previously-FIXED ones reopened 20 more.
Wave 2 closed those plus everything wave 1 could not reach. All committed and
pushed (`f8ab948b`…`679620e6`); backend auto-deployed, marketing site did not
(`cd frontend && npm run deploy`).

**Highest-impact find of the whole exercise** was not in the register at all.
Chasing row 6's residual turned up `premium_gate.py` carrying its own inline copy
of the tier list — `{free:0, premium:1, premium_plus:2}` against a production
enum of `free/premium/ultra/lifetime/premium_plus`. `lifetime` fell through
`.get(tier, 0)` to rank 0, so **a founder who paid $149.99 was metered and 402'd
as a free user at 20 coach messages a day**, while the client counted them
premium and showed no counter. Fixed at a single derived source with a gate that
fails if an enum label is ever missing again.

### What remains OPEN

Nine items, all narrow and all documented in their rows:

- **#18 long tail** — ~50 hardcoded-unit sites remain in two ~5,000-line summary
  files. They are the MIRROR of the reported bug (hard-converting to lb, wrong
  for kg users), so the row as filed is fixed. Deliberately not restructured
  late in a session.
- **#65 server half** — `/today` returns only one `next_workout`.
- **#66** — the day-0 XP award itself (the overlay is gated).
- **#80** — two upstream chokepoints.
- **#1 client half** — a failed offline finalize can strand a session at
  `in_progress`.
- **#9 instance 3** — one edit in `crud_background_tasks.py`.
- **#16 / #108** — fully closed; listed here only because their fixes crossed
  four other domains' files.

### Method note, for the next pass

Every wave-2 agent was required to NEGATIVE-TEST any gate it added: reintroduce
the defect, confirm the gate fails, revert, confirm it passes. That is what
turned up row 84's gate being satisfiable without wiring anything, and it is
worth keeping as a standing rule — a gate nobody has seen fail is not evidence.

The 17 pre-existing Flutter test failures were confirmed identical to a HEAD
baseline run in a throwaway worktree, so none were introduced here. Several of
them assert the OLD fabricated-2000 nutrition defaults and should be updated
rather than left red — a red suite hides the next regression.

---

## 2026-07-31, wave 3

Third pass over the register. Row-level updates are in the table above; this
section is the summary. 22 rows were **independently re-verified** — the gate,
test, or query was re-run directly, not taken on an agent's word. Another 20
rows were closed on **agent self-report only** and are marked as such, in
keeping with this register's own headline lesson: the reported instance
getting fixed is not the same claim as the class being fixed, and only 9 of
25 wave-1 FIXED rows survived wave 2's scrutiny intact. Five rows turned out
to be register errors rather than code defects.

### Independently verified this wave

#7 (sibling writers closed), #9 (residual instance 3), #18 (long tail + a
second latent instance found), #41 (guard wired), #65 (server half), #80
(both chokepoints — the second fixed by hand, not by an agent), #82, #86
(the "Needs your call" semantics question resolved, no code change needed),
#90 (re-confirmed, still 0 findings), #109 (real half fixed, "stale asset"
half retracted), #116 (dead code removed, no second render site exists),
#118 (fixed, and its gate found 4 more live dead routes), #120, #121,
#122/#126/#127 (94% closed — 1,039/73/14 remain, down from 20,332/263/31,
not a full zero), #130, #131 (plus a residual bug found and fixed directly),
#132, #148, #15 (added after the summary above was first written — sweep
completed 10,537 findings / 1,212 files → 0, gate baseline emptied,
negative-tested).

### Fixed on agent self-report only (not independently re-run this wave)

#1 (client half), #47, #66, #110, #114, #123, #133, #134, #135, #136, #137,
#139, #141, #142, #143, #144, #145, #146, #147, #153. Each row states this
explicitly rather than reading as re-verified — the distinction matters more
than the FIXED label itself.

### Register corrections (the row was wrong, not the code)

- **#94** — the fix IS in HEAD (`chat_media_widgets.dart:826-839`, commit
  `b906c523`); the row cited pre-fix line numbers (`:544-549`). Marked as
  needing a device repro on a post-`b906c523` binary rather than claimed as a
  new fix — the earlier "STILL BROKEN" observation may itself have been taken
  against a stale binary.
- **#109** — the "stale asset" half of the diagnosis was wrong
  (`app_icon.png` is md5-identical to the supposedly newer mark); only the
  logo-tile-as-logo half was real, now fixed with an alpha-keyed mark.
- **#111** — the literal "Title"/"Subtitle" half doesn't exist anywhere in
  `lib/` and duplicates #22 (already fixed); the row's real, distinct half
  (an all-cyan sheet) is closed by the accent sweep.
- **#116** — the "second render site" this row asked to hunt for doesn't
  exist; it was dead code (`large: true` with zero callers), now deleted.
- **#86** — the row's own header text was stale relative to its underlying
  fix note; the resolver was already surgical, no code change needed.

### Still open

- **#125** — warm-up editing feature build in progress (migration 2401
  applied; endpoint + Flutter methods underway). Not yet closed.
- **#138** — unchanged; still needs a real-device check (the Simulator has no
  registered share extensions).
- **#124** — re-checked and confirmed as a decision, not a defect (13 users,
  0 with `fcm_token`, pre-launch) — left as an observation rather than marked
  FIXED, since nothing was actually broken or changed.

### New defects found while fixing (not previously in this register)

1. **4 live dead routes** still push to unregistered paths (real user-facing
   404s), caught by the new `test/navigation/route_existence_test.dart` gate:
   `share_artifact_card.dart:55,62 → /workout-tab` (chat bubble),
   `usage_counter_strip.dart:75 → /paywall` (Home),
   `notifications_screen.dart:225,240 → /challenges`,
   `workout_complete_screen_ext_1.dart:682 → /goals`. Two more found are in
   dead code and not user-facing: `guest_home_screen.dart:812 →
   /guest-library` (guest mode is commented out of the router entirely) and
   `workout_summary_screen.dart:376 → /exercise-progress-detail` (an
   unreferenced v1 screen; v2 is live). Note `/workouts` is a
   `StatefulShellBranch` tab root — its fix must use `go`, not `push` (the
   documented duplicate-GlobalKey hazard).
2. **`WorkoutSummaryAdvanced` (5,691 lines) is dead code** — zero
   instantiations; only `WorkoutSummaryGeneral` is live via
   `workout_summary_screen_v2.dart:197`. Most of #18's original "long tail"
   sat in a file that never renders.
3. **The backend test suite could not run locally at all** —
   `backend/.venv` is Python 3.9 while the code uses `dict | None` and
   `.python-version` says 3.11, so every test died in `conftest` importing
   `main`. A `.venv312` was built; the suite now runs. Baseline vs current:
   HEAD **7,420 passed / 93 failed / 38 errors** → now **7,503 passed / 80
   failed / 38 errors**, with exactly one new failure introduced and fixed
   (below).
4. **A phantom column was introduced and caught by the gate**:
   `workout_duration_energy.py` selected `users.weight`, which doesn't exist
   (the real column is `weight_kg`). PostgREST rejects the whole query on
   one bad column, and the surrounding `except` swallowed it, so every
   calorie estimate would have silently fallen back to the 70 kg default —
   the same class as #55/#112. Fixed; `audit_supabase_column_drift.py
   --check` green.
5. **3 weight ARB keys baked `" lb"` into the message text in all 36
   locales** (`summaryEst1RM`, `summaryBestSet`,
   `workoutSummaryGeneralLbXReps`) — #18 was latent in every language, not
   just English (see #18's row). The row-#100 alphabetical-parameter trap
   was real and reproduced here too: passing args in message order rendered
   "5 lb x 225" instead of "225 lb x 5".
6. **`invalidate_daily_insight_cache` had exactly ONE caller** in the entire
   backend (nutrition). Nothing in the workout domain busted it, so a
   completed workout left a stale coach card. Now called from `/complete`
   and the offline `/sync/bulk` replay too (see #80).
7. **`daily_login` XP double-counted** the same login event `first_login`
   already paid for.
8. **`MenuDishSchema.weight_g` was optional AND unbounded** — the likelier
   real path for a photographed food spread than the `FoodItemSchema` field
   #130 named (see #130's row).
9. **A `_backup_pre_redesign/` directory with 3 `.bak` files has been
   sitting inside `lib/screens/home/` since May 17** — 96K of stale source
   inside the source tree.
10. **Pre-existing test failure surfaced**:
    `test/shareables/food_templates_test.dart` asserts the food-template
    catalog is exactly 14; it is actually 67. Predates this session.
11. **`_warmupRestTimer` (`easy_active_workout_state.dart`) is a
    `Timer.periodic` that is never cancelled in `dispose()`** — it keeps
    firing after the screen is gone and calls `setState` on an unmounted
    widget. Found by analyzer warnings. Not yet fixed; being addressed
    together with #125's warm-up editing build (still in progress).
12. **`accent_source_gate.dart` conflated a MISSING baseline with an EMPTY
    one** (`if (baseline.isEmpty) → "❌ no baseline… run --refresh-baseline"`).
    An empty baseline is the #15 sweep's own goal state, so the gate began
    erroring the moment the sweep actually succeeded — it would have been
    unusable at exactly the state it exists to protect. Fixed to test
    `!baselineFile.existsSync()` instead. Same shape as several rows in this
    register: infrastructure that looks correct until it reaches the state
    nobody had ever driven it to (see the wave-3 method note).
13. **`lib/screens/workout/`'s mechanical #15 migrate pass over-flattened
    real ordinal/categorical palettes** into a single repeated
    `context.accentColor` — RPE/RIR bands, warmup/drop/failure set-type
    legends, HR-zone charts, the barbell-plate real-world colour code,
    PR/mastery-status legends. Caught and restored with per-legend
    `accent-allowlist` reasons. Notable because a colour gate can go green
    while the MEANING is destroyed, and no test would catch it.
14. **`inline_referral_expander.dart` hardcoded `AppColors.orange`** — the
    app accent's DEFAULT value — on the paywall, whose own accent is a
    deliberate `_paywallAccent = Color(0xFFFC4C02)`. Two different colours
    that only look alike while the user is on the default accent, so that
    CTA was wrong against both the paywall and the app. Fixed by threading
    the paywall constant through.

### Product decision, wave 3 (2026-07-31)

`_paywallAccent` was deliberately **NOT** migrated to `context.accentColor`
during the #15 sweep. It carries a cited A/B-test rationale ("warm tones
outperform cool by 43.9% on mobile"), so it stays fixed and is allowlisted
with that reasoning — a user on a cyan accent will still see a warm-orange
paywall, intentionally. #15 is about colour that has stopped carrying
meaning; a deliberately-chosen, cited conversion accent is the opposite of
that problem.

### Late addenda, 2026-07-31 (post-summary)

Three more items came in after the summary above was written; row-level
evidence is in the table.

- **#114 corrected, not just fixed.** The row's own framing understated the
  bug: FastAPI wraps every error body under `detail`, so the client's
  typed-error branches were reading a top-level `data['error_code']` that
  never existed — the real code is at `data['detail']['error']`. That means
  the two branches the row cited as evidence ("everything else has a typed
  branch") — 404 `PREVIEW_EXPIRED`, 403 `PREVIEW_NOT_OWNED` — were themselves
  dead code that had never fired either. All three are now revived and wired
  at 6 call sites, negative-tested (reverting to `body['error_code']`
  reproduces 3 failures).
- **#153 was a register error, not a wave-3 fix.** The single-page-with-
  disclosure treatment it asked for landed in `e10473d2`, before this
  session — `git log`-verified. The "agent-reported fixed" mark applied to
  it earlier in this wave is retracted; it should have been closed out as
  already-fixed instead.
- **New row #154** — `/swap-exercise`'s schema advertises `section` support
  identical to `/add-exercise`'s, but the handler never reads it: a
  `section:"warmup"` swap searches the main exercise list and 404s. Same
  class as the register's phantom-column rows — an interface advertising a
  capability it doesn't have. File/line-verified, not yet fixed.
- **New defect #11 above** — an uncancelled `Timer.periodic` in the
  active-workout warm-up state calling `setState` after unmount. Not yet
  fixed; folded into the in-progress #125 build.

### Method note

Three separate rows this wave turned out to be code that was written but
never called: `AuthInstallGuard` (#41 — zero call sites),
`invalidate_daily_insight_cache` (#80 — one caller in the whole backend),
and migration 2391 (written but unstamped). That is the same failure mode
#41 itself describes — a fix that nobody calls is not a fix — and it is
worth a standing check rather than a one-off finding: *when a row's evidence
is "the fix landed," also check whether anything invokes it.*
