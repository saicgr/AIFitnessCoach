# FastAPI & Server
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Gemini AI (new unified SDK)
# google-genai>=1.56.0 required by langchain-google-genai 4.x for Gemini 3 thought_signature support
google-genai>=1.56.0,<2.0.0

# RAG & Vector Store
# ChromaDB 1.0.7+ required for Chroma Cloud API v2 (fixes KeyError '_type' bug)
# See: https://github.com/chroma-core/chroma/issues/4380
chromadb>=1.0.7,<2.0.0
langchain>=0.3.0
# langchain-core>=1.2.5 required by langchain-google-genai 4.x
langchain-core>=1.2.5,<2.0.0
langchain-community>=0.3.0
langchain-text-splitters>=0.3.0
# langchain-google-genai 4.2.0+ required for Gemini 3 thought_signature support in tool calls
# Fixes: "400 Function call is missing a thought_signature in functionCall parts"
langchain-google-genai>=4.2.0,<5.0.0
# NOTE: langchain-google-vertexai removed - it pulls google-cloud-aiplatform (~200MB)
# which causes OOM on Render free tier (512MB). langchain-google-genai 4.x supports
# Vertex AI natively via vertexai=True parameter.
langgraph>=0.6.0

# Database
sqlalchemy==2.0.23
aiosqlite==0.19.0
supabase>=2.25.0
psycopg2-binary==2.9.9
asyncpg==0.29.0

# Utilities
python-dotenv==1.0.0
pydantic>=2.7.4
pydantic-settings>=2.1.0
email-validator>=2.0.0

# HTTP Client
httpx>=0.28.0
websockets>=15.0

# Email
resend==2.0.0

# Firebase Admin SDK (for push notifications)
firebase-admin==6.5.0

# AWS Lambda
mangum==0.17.0
boto3==1.34.0

# Excel processing
pandas==2.1.4
openpyxl==3.1.2

# Rate Limiting
slowapi==0.1.9

# Testing
pytest>=7.0.0,<8.0.0
pytest-asyncio>=0.25.0,<1.0.0
pytest-cov==4.1.0
pytest-mock==3.12.0
